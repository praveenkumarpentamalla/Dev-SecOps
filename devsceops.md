
If you're a DevOps engineer planning to transition to Dev Sec Ops or if you want to step up in your organization, this is the best video that you're looking for. The overall video has seven different parts starting with introduction to dev sec ops where you'll learn what exactly is dev sec ops and you will also be introduced to a tool which shows the

current security state of your application. Then in episode two or the part two you will learn about dev sec ops for git and github. This is a very interesting part where you will learn how you can secure your gate, what are the best practices that you can follow locally and even with respect to GitHub. Then in the third part we will cover dev sec ops for terapform. Again how you can secure terapform, how you can secure your state file, what security best practices you can take while writing the

terapform manifest. Then in part four we will cover security best practices for containers. One of my favorite things in the space of DevOps because this is the part where you can completely mess up with the security that is the containers. So I've taken a lot of care in making the part four. You will master container security by watching that part. In the fifth part I've covered security best practices for Kubernetes. Kubernetes is an ocean. So it's a little lengthy part but I try to

cover most of the things that you know should know. Then in the sixth part you will learn about sea sash and dash which are currently trending in the security space. And finally once you're done with all these you need a capstone project. That is exactly what the part 7 is for a three- tier architecture application with front end, back end and a database. I'll show you how you can implement dev sec ops from zero to hero. The goal of this part if you join an organization or even during your interviews you should

be able to explain how dev sec ops can implement it from absolute basics and finally implement through pipelines through kubernetes through docker through terraform everything that is involved in the entire devops life cycle so this is what you're going to learn of course you need notes for this entire thing and I have covered the detailed notes because in every episode we have demos included even you want to perform the demos that's why you can find the GitHub repository for this in the description

and the G repository is completely updated you can just follow that at any point of the time if you need some manifest if you need some configuration or to implement the demo let's get started so today is the very first episode of our dev secop zero to her series and in this video we will focus on introduction to dev sec ops. Towards the end of this video you will learn how to generate a very detailed security report for your application and this security report can be downloaded as a PDF and it contains

all the vulnerabilities related to your application. The demo is going to be super exciting. Now before we get started just like any of our zero to her series even for this series I created a GitHub repository. So after watching the video you can head to the GitHub repository and follow the notes for the episode. Make sure you do it especially when you want to implement the demo because the notes is going to be very detailed and it will help you as a revision material. Perfect. Now let's get started because

this is a very first episode. Let's start with what is dev sec ops and why is it important. Unfortunately this is one of the most misunderstood terms in the industry. A lot of people think dev sec ops is completely different from devops. In fact some people think they are two different roles in the companies. Let me tell you this is not true. Dev sec ops is nothing but DevOps with security mindset. In a nutshell, DevOps instance who think of security in every activity that they perform is basically a dev secops inst.

That's it. Now you might have a question in that case why dev secops the term coined and why it became popular? Simple reason. Previously in the initial days of DevOps, DevOps instance focused on their activities like CI/CD, Kubernetes, infrastructure as code, but they did not focus a lot on the security while dealing with these tasks. Now this created new challenges for the organizations. Organizations started seeing security issues with respect to their infra. Now this is why organizations push

DevOps instance to think of security and because of this emphasis dev sec ops coined as a term DevOps with security mindset. This is the backstory end of the day any DevOps engineer taking care of security thinking of security while performing their role is a dev secops engineer. Now that this is clear, let's talk about the scope of dev sec ops. Again, a lot of people think dev sec ops is scope to CI/CD. This is not true. I have personally seen companies who just added two to three stages to their CI/CD

pipelines and call it as dev sec ops. But in fact, the scope of dev sec ops is very huge. We will do one thing. Let's list down common DevOps activities and let's see how a dev sec ops engineer can introduce security to those activities. We will start with Git. DevOps engineers spend a lot of time with Git infrastructure as code scripting. It can be through shell scripting. It can be through Python scripting to automate day-to-day activities. Containers. When dealing with microservices, DevOps

engineers spend almost their entire day with containers. Creating images, pushing the images, pulling the images, Kubernetes and common activities like CI/CD. So these are some very popular activities. Let's go one by one. When a DevOps instit you might ask what is security aspect with respect to Git. The tasks are very common to create a repo to create a branch to create a branching strategy. Where does security come into picture? Now this is where a good dev sec ops engineer or DevOps engineer with

security mindset would introduce things like pre-commit hooks. What exactly is this? Now when working with git developers can accidentally push their p files. Developers can accidentally commit their know files like encoded passwords. This can be eliminated if a dev sec ops engineer introduce something like pre-commit hook. Whenever a commit is created, whenever a commit is pushed, this pre-commit hook validates if there is any sensitive information. Only if there is no sensitive information, the commit

is pushed to the gate. On top of that, a dev sec ops inner can also introduce strict rback to get like ensuring only the right people can read the repo. Only the right people can make changes to the repo and administrate the repo. Let's talk about now on day-to-day basis. DevOps instance work with ISC tools like Teraphform and while using Teraphform they create infrastructure on the cloud provider. Now a dev sec ops engineer or devops engineer with security mindset whenever they install platforms like terapform

they would set up secret management solutions like vault so that any sensitive information in teraphform like cloud provider credentials can be stored to vault even p files or secret files they can also be stored in the vault secret management. scripting. Now this is where things get interesting. Lot of DevOps they don't care of security while writing the scripts. Let's take example of Python. Now you might ask what to do with Python. A good Dev sec ops engineer would not randomly use any version of the Python

packages. If they want to use flask instead of using the flask version suggested by charg or flask version that is available on the internet dev sec ops engineer would look at no vulnerabilities python package let's say there are two versions of python flask 2.3.5 2.3.6 Six. Shat GPT recommends this version. But dev sec ops engineer goes to the vulnerability database or these days companies also have their checklist. Companies also provide the list of packages that employees can use. So they would go there see that okay

this is a vulnerable version. So let me use 2.3.6 containers. There is so much to do with respect to container security. As simple as while writing a docker file, a dev sec ops engineer would not run the container with a root user. So in the docker file, they would not use root user. A good dev secops engineer would go with distro images. A good dev sec ops engineer could go with multi-stage builds so that the build size is small and even the end package or the end container has less number of packages.

Kubernetes let's take one example here a normal DevOps engineer would set up Kubernetes cluster. They would go for EKS and maybe set up the cluster. But a dev sec ops engineer would first set up virtual private cloud then set up private subnet and within that they would set up the Kubernetes cluster so that the nodes of the Kubernetes cluster and entire Kubernetes cluster is secure. On top of that they would set up secrets management solution in Kubernetes and other best practices. CICD you know this is where a lot of

people start their journey with dev sec ops that is unfortunate they should not do it but yeah anyways people start with dev sec ops in CI/CD so it's very popular they would introduce stages here right multiple stages like static code analysis stages like SAS stages like container security using platforms like 3v platforms like snake or even docker today supports lot of security related to containers. So this way the scope of dev secc ops is not limited to cicd but every activity that devops engineer

performs. Now you might have seen me interchanging the term sometimes I must be calling devops sometimes I might be calling dev sec ops. To be honest, I don't see the difference. I personally feel every DevOps engineer should be a dev sec ops engineer. Every DevOps engineer should think of security right from the point they start the project. This is what dev secops also says. One of the fundamental principle of dev sec ops is shift left principle. What is shift left? Every organization has two points. the

left point and the right point. Left point is where the application development starts and right point is where application is deployed to the production. Earlier in the previous days, this is where security was considered. Once the application is deployed to production, DevOps instance used to think of the security. Maybe if the application is deployed to Kubernetes at this point DevOps engineers used to think of API gateway at this point they used to think of securing the Kubernetes cluster or

adding any best practices to Kubernetes. The shift left principle says just like how application development starts at the left point, just like how DevOps instance start creating infra at the beginning of the project. Just like how they create git repository at the beginning of the project, how they create Kubernetes cluster here. In the same way, they should also think about security at the left. So this is called shield right approach. The very wrong approach which was used in the past. This is called as shift

left approach. By shifting security to the left organizations can benefit. Very very simple right? When companies create kubernetes cluster at the point they have to create a secure cluster. When they install terapform they should install terapform with vault. when they create GitHub repository, they should create pre-commit hooks while they create the repo. This is the shift left principle. Now, while we talk about all of this, some of you might have this question. Abishek, do I really need to upskill in

2026? Because people keep saying in 2026 AI will take over all the jobs. So what's the point of learning dev sec ops? Let me tell you dev sec ops is one skill that has reverse effect on AI. That means as AI keeps scaling as companies start using more and more AI, dev sec ops adoption will only keep increasing. Why? Today AI is writing most of the code. If you look at companies in majority of the companies 60% code is written by AI the latest code and 40% code is written by the developers in

some companies 80% code is written by the AI itself like chat GPT or any inbuilt models that are supported by the organizations. Now what happens with AI? Of course, it writes the best code. But a lot of times it uses outdated packages. Lot of times it use a package that is not supported by your company or which is against the complaints of your company. A lot of times AI copies the code from other sources like it is inspired by the code that is in Stack Overflow or it is inspired by the code that is in the

Reddit and it might provide you the code that is working but a buggy code a code that has lot of vulnerabilities. Now how will you overcome this situation? You might say but Abishek developers are still in the loop. Developers are still reviewing the code that is written by AI. Yes, they are doing it but developers mostly review the code to see if the code is working. They test the code locally. They see the flow of the code. They might not look at outdated packages. They might not see the compliance or they might not or they

might oversee the buggy code. This can be eliminated by dev secc ops. What organizations can do? They can counter the bugs introduced by AI by building strong dev secops pipelines. One easy use case strong CI/CD pipelines which can potentially look at all of these bugs. tools in the CI/CD pipelines something like no we learned about quite in the past there is a video on the channel quite is something that can identify AI related issues if possible we will look at quite even in this series as well or

maybe use tools like snake use container security platforms like tree it can tell you during the runtime if your organization will be impacted by any outdated package. It will scan the vulnerability database and it will tell you any issues with the packages. So this way dev secops has a counter effect on the AI or anti- effect on the AI. Okay, we learned about the what the why and you know the good and bad of dev sec ops. Now let's see how to start with dev sec ops. Okay Abishek, no I will learn Dev

sec ops but once I join the company how it would look like? What would be my first step? There are two possibilities here. The first possibility you would work in a company which is a startup and let's say they don't have any DevOps culture there. The other possibility you would go to a company which is a legacy company and in the legacy company they have the DevOps workflows in place and mostly they will be reluctant in following any new practices. Now legacy companies they have DevOps practices.

Sometimes they have security issues in the practices but still if it is working they would be hesitant in upgrading the practices especially MNC's. So this is one possibility and this is the other possibility. If this is the possibility you are lucky enough because you will start the DevOps in the company and you will take care of all the best security practices. But what if you will go to a company with all the legacy pipelines, all the legacy DevOps tools and it might have some buggy code as well as buggy

infrastructure. How will you convince your company to onboard dev sec ops practices? So this is where you have to implement threat modeling. Now I'll show you how to use threat modeling and how to generate a security report for your existing infrastructure as well as your applications. So that way you can share this PDF with your management. You can share this PDF with your development team and you can set up a meeting and discuss the existing infrastructure might lead us to potential issues. So let's onboard dev

secops practices. What is threat modeling? No, it's a process where you identify the issues. It's a process where you analyze the issue and potentially you also mitigate the issues right that is what we will see now. So for that there are good number of tools in the market but most of the tools are commercial licensed. What we will do in this lecture? We will go with OASP thread detection or OASP thread dragon. So OAS thread dragon is completely open source. You can do your proof of concept. What you can do? You can paste

your or you can draw your architecture diagram of your infrastructure within thread dragon and it will show you all the possible issues. It will give you a PDF report with all the bugs that are related to your infrastructure, typically vulnerabilities. It's going to be super interesting. What you have to do, you have to first download this. So go to OS Thread Dragon. Click on the download installers button and install thread dragon on your laptop. Now I already have it. I used it in the past. So I'll search for thread dragon.

There you go. So this is the thread dragon interface. Now let's start with creating our architecture diagram. Let's go with create a new empty thread model. I mean I'll do it from the scratch. Let's do it for a simple two-t tire web application. Two tire web app owner Abhishek previewer is Namala. I leave these options default and I will provide a diagram. So click on add a new diagram. Save the record. A JSON file will be saved to your local machine. Just save it. Okay. Close this. And you will have an

option to draw the diagram. Don't worry, you don't have to be good at drawing the diagram. You just have to explain the thread dragon how your application flow looks like. It will take care of everything. So let's say we have a simple two-tier architecture, right? Where we have a web application and that web application connects to the database. So your company what did they do? So every time a user so I change the property here scroll down you can also do it. So actor I said a user

does not provide any authentication and this user talks to a web application. Okay. So I'll take the there are only few components here. One is the process component, actor component, the data stores component and data flow component. That's it. Using these four components, you can draw any architecture diagram. Now process again I'll change the property. I'll call this as a web application. There is an option for web application. Take this. It will ask you few questions. What is the privilege level? Let's leave

it default. Is this web application related to card payment? Leave it default. Goods and services, leave it default. Okay. Finally, a database. For that, I will take the store option and put that here. Again, I'll change the property. So, I'll call this as MongoDB. Okay. Let's say my web application talks to MongoDB and it stores the inventory. There are a few questions here. Does it store any credentials? Let's say yes. Is it encrypted database? Let's say no. Does it store a log? Let's say no. That's

all. We will provide this piece of information and we will say these components are connected. How? Let's take the data flow. Connect user to the web application. Connect web application to the database. And then let's say using this trust boundary that user is on the internet. So there is a boundary between user and the web application. Okay. So user is from the internet and these things are within the internal network. Of course, I did not put them in the same network, but it's fine for

our simple diagram. User is on the internet. Web application web application talks to the MongoDB. Now, you might say, Abhishek, everything is looking fine. What is the issue with this architecture? Let's see what OAS thread dragon says. Save this. Okay, now just scroll down. Once you save it, just scroll down, select the particular component. Okay, I'll just close it. Open it again. I'll select MongoDB component and click on new thread by context. There you go. So, it says there is

information disclosure vulnerability on your database because you did not detect any fake or compromised accounts. You did not ensure information is accessible only with authentication and authorization. It's true in the architecture diagram my web application is connecting to MongoDB. I did not provide any authentication or the architecture diagram does not say there is any authentication that is set up. Okay, let's see what are the other vulnerabilities. This is one I applied it. It says there is another vulnerability

elevation of privileges. There is another vulnerability for information disclosure where you did not stop any denial of service attack. Now there can be any number of attacks on the database further further and further. So you can see there are eight different bugs that are identified on my database. Instantly on the architecture diagram MongoDB became red. Now these are not ordinary bugs like you might say abishek these are some random bugs that are generated by thread dragon. My organization would

ask for the explanation. That's good. Just go to any of this. Like if you go to scraping so you can see this is OASP automated threat level. What does it say? If you go to the internet and if you search for OASP automated threat level there is a complete information. You can share this information with your developer. You can share this information with your security expert. Anyone who asks just share them. This is the definition of the vulnerability. This is the description and this is how we can

mitigate the issue. That's all. Similarly, for all the tools, enough information is provided. Let's go back to thread dragon. You can see all the eight threads here and you can work as a team once you all agree like let's say your development and you agree that okay this is not a potential threat for us you can open it and you can change this as mitigated change this to mitigated say the severity as low and if you want you can delete it or if you want you can just apply it so you can see this is changed

to green just like Jira you can work as a team. So over the next few weeks you can complete all the other issues as well. Now not only on database you can see some issues on the web application as well. So if you click on web app click on new thread by context you can see there is tampering issue on the web application. There is information disclosure issue elevation of privileges issue. Again another information disclosure issue poor vulnerabilities on the web application as well that's why web app also turned

to red but how will I share all this information to my team right now they don't have access to thread dragon on my local machine don't worry save this close and you can extract the report as well once you click on the report you can the detailed report a PDF file that is generated by thread dragon. It says okay overall there are 12 threads on your application one threat is mitigated which we closed you have zero critical vulnerabilities but you have to be decided severity that is you have to work with your

development team to decide the criticality. This is the architecture diagram that you provided. These are the vulnerabilities on the web application. These are the vulnerabilities on the MongoDB database. A complete security report is provided. Perfect. Now, what about dealing with more complex applications? You can deal with any complex application. In fact, what Thread Dragon does the best, it provides you some sample threat models. If you want to learn how to draw the architecture diagram, if you want to see

how detailed the report is going to be, just go to the examples, let's use one. So, we have here online game. Click on it. You can see a complex online game. Okay, I'll just decrease the size here. This is the complete architecture. So you have the players local station, you have the production network and the corporate network. A well definfined architecture diagram usually this looks like everything is perfect. But again if you go to the players database you can see OS thread dragon says there are

multiple issues with this architecture. One issue second third three issues on the player database. Similarly on the stats database again there are some issues. Now it does not confirm these issues are not mitigated by your company but it says at least by the architecture diagram you did not solve this issues. If you think you solved it outside the architecture you solved it within your source code just come here and close the bug. Mitigate the issue and once everything is done save this. You can save it and you can generate a

detailed report. Right? Again the same way you can see the architecture diagram and you can see the bugs because I did not save the bugs. Right? Now it says there are no issues. Otherwise just save all the bugs. You can get the detailed report as well. So this is about threat modeling. One thing which might be too early at this point of time but just to tell you in threat modeling there are different frameworks that can be used oas thread dragon use a framework called stride str r i t. So this is a framework that is

created by Microsoft and internally thread dragon uses this framework basically to identify issues on your architecture. I hope you learned something exciting today. You not only learned about the fundamentals of dev sec ops, but you also learned how to thread model your infrastructure and application. Go ahead, try it with your application. Try it within your organization and build a good dev sec ops culture. See you all in the next video. Take care. Hello everyone. My name is Abhishek and welcome back to my channel.

Today is episode two of dev sec ops zero to hero series and in this episode we will focus on dev sec ops for git. Just a backstory in the first episode we focused on introduction to dev sec ops where we learned what is it why is it used and most importantly what is the scope of dev sec ops and while learning the scope we learned that dev sec ops is not only for CI/CD but it is for every single activity that DevOps instep performs. Now one of the most common activities is git. That's why in today's lecture we

will focus on securing git. Now you might think but g is just a utility. Git is just to store source code. So what's in dev sec ops for git? Let me tell you this is very very important and there is a lot to do when it comes to securing git. In today's video, we will focus on 10 most common ways in which enterprises secure git or the best dev sec ops instance secure their git repos. We will start with git ignore. One of the most easiest ways and the first things that you can do. Then we will learn about pre-commit

hooks. an interesting thing but something that you should also implement in your organization. We will first learn pre-commit hooks using a custom script. Then we will go a step ahead and we will learn how to implement pre-commit hooks using a common utility. Then we will focus on repository scanning. So a lot of times existing repos in your organization they might also have secrets or sensitive information. We will perform scanning on the repositories using kit leaks. Not only the latest commit but also the

commits that are made 3 months even 6 months before. Then we will learn how to integrate git links into GitHub actions, git links into CI/CD so that even through pull requests accidentally as well users cannot commit secrets to the repo. After all this we will switch the gears and we will move towards arbback. We will learn branch protection rules, how you can secure your main branch and other important branches in the repo. Of course we will talk about arbback itself. How you can grant different

roles in the g repository. Easy one. Then we will learn about mandatory reviews. Something that not a lot of people talk about but in many companies you want certain engineers or certain AI agents to review every single PR on the Git repo. How do you achieve that? You can achieve that using mandatory reviews. We'll also talk about then we will learn about code owners file something that is becoming a standard these days followed by opensource repositories from a long long time and these days enterprises also started

adopting it. Finally we will learn about dependabot. So dependable is again a popular thing which can auto fix the security issues on the repo. So these are 10 things that we are going to learn in this lecture. Now you don't have to be overwhelmed because we will go topic by topic and for most of the topics here we will also perform the demos. After watching the video, just spend 15 more minutes and go through the readme file that I created. I created this readme file with every step that I'm going to execute and every

script that I'm going to use so that you not only watch the video but also get the practical hands-on experience. It's a free readme file. It's in the same repository in the dayto folder. Perfect. Now that the context is clear, let's start with get ignore. As I told you, a very very simple security thing that you can implement for your repos. Let's understand this with an example. Imagine there is an organization and this organization has a micros service and this service is within their g

repository. So obviously the source code of the service is within the repo. Now developers working on this repo they clone the source code and they work on the local changes. Let's say one of the developer is working on this repo and this developer is basically working on database changes. So developer made some changes to the Python scripts that updates the database and developer created av file or updated a existing env file on the developer's local machine with DB password. So developer made the changes, developer

tested the changes, everything is fine. Now what this developer did accidentally along with the Python script along with the DB changes also committed the env file. Now this becomes P 0 issue for your organization the most critical or issue for your org because a secret especially the database password is committed to the G repo. Why is it critical issue? Because git never forgets things. Git is a version control system. Even if you revert the commit, you can perform git revert. By the time you perform git revert, you

never know. A lot of other people must have cloned the repo. If it's an open source repository, some people must have even forked the repository. So they must have created a private or public copy of the repo. you don't know. So you can perform revert, you can use some repo cleaners, you can use some external scripts, even you can get in touch with GitHub support. You cannot do anything because the password is already shared with others. Only thing that you can do is change the database password. You

never know what are the implications of it. So what I'm trying to say here instead of all this you should avoid the situation you know instead of talking to the GitHub support reverting the commits failing to do it checking who cloned it why not prevent something of this sort. So the first step to do it or the easiest thing to do it is to use the g ignore file. I should say ignore file. Now what does this thing do? Very simple. Git ignore file tells git hey git I will provide you with list of

files that you should not track. You know the default behavior of git is to track the files right. What does G do? It will track every single file in your repo or on your local machine. But this is a special file which tells G, hey, don't track the files that I have with me. So the dev sec ops engineer will create a dotit ignore file and within the git ignore file, dev sec ops engineer will make entries of the files that should not be tracked by git. for example envile or if working on terapform tf state file

maybe ID RSA file this way a good dev sec ops engineer will list down even 100 common files that should not be committed to git I'm not joking you know to make it clear to you if you look at our dev sec of zero to her repo I have a realtime.git ignore file that enterprises use. Look at the doggit ignore. You will find a lot of entries. Now, of course, you know, I exaggerated this file. I added things that not a lot of organizations would have. For example, I added Python related files and mpm related files as well. Maybe

organizations would only have Python or npm but what I'm trying to tell you here a good dev sec ops snare should list down every single possibility. So in this file I added env as well. Now if someone is working on this repo imagine this is the actual repo where developer is working on. Even if developer tries to commit env file intentionally, G would not do it. Let's see that. No, we will see this practically. So what I'll do? I'll first clone this repo. Get clone followed by the repository URL. Here we

go. I'll switch the directory deck ops 0 to hero. Nice. And here intentionally I will create a env file and within this I will store a DB password. DB password is equals to Vala. Okay. Now I will add G to track it. Let's see. Get add dot env. Okay. What does it say? the following parts are ignored by one of your dotgit ignore file. So g is saying hey I cannot do this because there is agit ignore file here the one I'm talking about and this got ignore file told me that I should be ignoring elba but abishek what

happens if I try to commit it or track ask g to track it with other files for example I will create a testing.py pi and here let's say I will add a simple thing that print my bad print hello okay can we fool g by pushing both these files together so what if I do get add-y all will g be fooled let's see if you do this now what g does it did not throw you this error the error that you are seeing earlier but if you do get g status g is only tracking testing.py Pi it did not throw the error but it did not get fooled as

well. It said I will still stick to the doggget ignore file. So this way dev sec ops instance can take the first step in the organization. Introduce ignore. Now introducing is very simple. You can take inspiration from this file or you can talk to your developers. You can talk to your uh QA team. List down all the files that you want to avoid. Create dot get ignore the same file. The name has to be same. Create this file. Now you might have only five entries in it. Save the file and do get add do.get

ignore. And most importantly you have to push this file to the repo. Why? So that next time other developers clone this repo, they will also have git ignore on their local machine. So you don't have to go to every single developer and tell them, hey, please take this file and keep it in the or have it in the same folder of your git repo. You don't have to tell everyone. Just commit that file to the repo. It's a best practice that you can do. So this is about our first section that is dot getit ignore. Now let's move

to the next one pre-commit hook. The reason for learning this concept is simple. Now dotgit ignore although it is a good concept if you observe carefully here you are responsible for providing the list of files. Okay. So in the gget ignore you provided that okay don't track don't track PM file don't track XY Z file but what if secret is in the Python file itself you never know there can be a junior developer who is working on a py file this time the developer did not create but because a junior Junior developer in

junior developer junior developer hardcoded the password within the Python file itself added something called secret and that secret is still in the Python file. It's added, it's committed or there is a junior DevOps engineer working on Terraform related files that is the TF files and in thet files the junior DevOps engineer hardcoded AWS secret and forgot to revoke it tested forgot to revoke it. Now in this case ignore can't help you because in the dotgit ignore you cannot say py star. py okay ignore

every python file you cannot say it or you cannot say star tf ignore every terapform file because that's not possible these are the files that should be in your g repo what will you do in this case in this case as a dev sec ops in there you have to introduce pre-commit hooks sogetit ignore is for known files pre-commit hook is for patterns Okay, here you will rely on the pattern not on the files. So within the patterns you will write a custom script maybe you know a shell script and you will say

look for secret in all the files that developer is committing. Look for tokens in all the files that developer is committing. Look for password in all the files that developer is committing. Even if developer has 100 files, this script will look for the patterns in 100 files. And if there if the pattern is matching, the script the pre-commit hook will block the commit. I'll show you in practice. Right? It's very easy to understand if we demo this. So this time what we will do is we will

add a pre-commit hook to the do.get. Where will be pre-commit hook added? It will be added to the dotgit. So if you go to cd.git here you will find a folder called hooks and in the hooks folder you have to create something like precomit. So you can call it pre-commit.sh. Even if you don't call pre-commit.sh understands by default it's a shell script. So what you do here you write a shell script something like this. Again don't worry the script is in our GitHub repo. I'll walk you through the script.

Okay. Shebang /bin/bash the starting point of the shell script. echo running a native pre-commit hook. This is something that I added. And here I am looking into the gif. So every time developer runs g commit g diff is performed and on the g diff I am looking for the secret keyword if there is a secret keyword instantly I'm saying secret detected block the commit if there is no secret okay go ahead and pass it I don't have any problem that's what pre-commit hook says now I'll show you this in action first

of all I'll save this chod plus x pre-commit. You have to do it because it's a shell script. Go back. Perfect. Now I'm on the root of the repository. Now I'll do one simple thing. I will create a text file. Okay. So this is a text file I'm creating. And this text file has something called secret. Just like a Python file, I created a text file and it has cat test.txt txt. Okay. So you can see it has a keyword called secret. Now I'll do get add test.txt. Let's say developer did it and

developer tried to commit it. Get commit- am test commit. Okay. Now if what we learned is right, this commit should be blocked. Enter. There you go. Secret detected. Commit is blocked. Now what does a developer do? Very simple. Okay, developer understood. Oh, I did a blunder. Let me just fix it. So developer goes to test.txt. Okay, developer removes this line. Developer will have other lines of the file. And now if developer tries to commit test.txt txt followed by get commit command. This will go through.

This is the purpose of pre-commit hooks. Once again, very simple difference. Git ignore that we learned is working on the files that will avoid committing of secure files to the g repository files that are sensitive likev or terraform files. Whereas pre-commit hook as the name suggests it's a hook that you are writing. It's a script that you are writing that will see for a particular pattern. In our case, I looked for the secret pattern. Now you might say but Abishek what if you know instead of a single

thing right here you are looking for a secret pattern. What if developer commits token or what if developer commits DB password? What if developer commits something like let's say API keys or Teraphform related AWS credentials? How do you prevent that? You know, I'm good at writing shell script but I cannot handle so many conditions in the shell script. Going back. So in the dotget in the hooks in the pre-commit hyphen. Okay here I just handled one condition that is secret grap hyphen i. What if I have to

look for token as well? Then you have to write another if condition. What if you have to look for db password? You have to write another if condition or you have to enhance this. This way you don't know what are the common patterns or maybe you are not good at writing the shell script itself. You are a DevOps engineer but you don't know how to write good scripts. Don't worry even in this case there is something which is pre-commit framework. So if you go to the pre-commit website so this is a framework. What this

framework does it use something called git leaks right get leaks and using git leaks it will help you you don't have to write the script now I'll show you that okay same thing what we learned but this time without writing a script so first of all I will delete the script that I wrote rm - rtget followed by hooks followed prey-en commit. Okay, I deleted the shell script that I wrote. Now, the first thing that you have to do is to install the pre-commit framework. If you're on a Linux machine, just run

pip install pre-commit. If you're on Windows, you can go ahead and try it. But most likely, it is recommended to use a Linux environment. go for something like WSL because a lot of times these framework won't work on your Windows machine. Anyways, you can give it a try but in your organization you will work on a Linux environment. So you don't have to worry. Okay. So pip install pre-commit. I'm on my Mac. I will run brew install pre-commit. Okay. So the framework is installed. I

have the framework ready. Now all that I have to do is I have to just add it's in our g repository. Go to the readme file. So you have to add a pre-commit YAML file. So just add this pre-commit YAML file. Create this vim dot pre-commit config.l and within this add these four lines. As I told you this is using git leaks internally. What is git leaks? Git leaks is a opensource project. So it's a free project that anyone can use. Pre-commit is calling this git leaks and git leaks is doing the job for you. This is basically

handling the script that you wrote in the previous case but in a much much much better way. And one more advantage of gitlinks is that it can work on all the repositories that you have. You don't have to go to every single doggget. You can just use the pre-commit framework and use this configuration file for all the repositories that you have just to pre-commit install. Okay, I will run pre-commit install and it says okay I created dot get hooks pre-commit file for you what we did in the previous

case we wrote this now what pre-commit hook did it wrote this script for us or the pre-commit framework wrote this script for us now let's verify I will create a secrets env file okay where in the secrets env. I am adding AWS secret access key. I will add this get add secrets env. And I will try to commit this get commit. Adding some secrets. Let's see what happens now. Initializing the environment. You can see it. It called the git leaks. It is verifying. This will do on every commit. Sometimes it might take time. You have

to be patient but for security I think you can do it. There you go. So it says hey I'm blocking your commit because you added AWS secret access key and this secret access key is added to the secrets.env file. So it is saying what is the secret and it is also saying the file in which you added the secret. Again what a good developer would do oh I accidentally committed the AWS access key to the secrets env let me go ahead and delete it. So both the git leaks and your custom script is doing the same thing but in

this case you are writing the script whereas with pre-commit framework you are not writing the script and it can handle the things in much much much better way. It provides you the information. It provides you the name of the file and it can also identify not only regular patterns but different patterns that are vulnerable. So we learned about dog ignore, we learned about pre-commit script and we learned about pre-commit framework. Now let's see how to scan repositories. Now we are slightly switching the gear.

What if you joined as a dev sec ops engineer and you want to see Abhishek I have this repository from 3 years I want to see 2 years ago if anyone committed a secret to this because right now if I look at these files all these files are okay there is no sensitive information in it but what if I go to the commits I go to commit that is one year ago and in this commit developer made a secret or committed a secret. Now because this is git, git never forgets things. A hacker can look at all your commits. A hacker

can look at all the files in the repo history along with the latest and if the hacker finds the secret then that's it. You know someone might some developer might accidentally committed a secret developer realized it just to be safe. developer immediately reverted it and developer thought it's all fine hey I did a mistake and I covered the mistake but no g does not allow you do that as a dev sec ops thinks that it is a good thing to scan the repose once in a while perform the scanning how will you do it

what you can do is you can use the same git leaks git leaks detect hyphen v or you can just run Get leaks detect. That's it. One single command and you can see it scanned all the 11 commits and it says one leak found. Now this is because we have added the secrets enving from there. But anyways what git leaks detect does it scans all the 11 commits and it gets the information to see if in any of these previous 11 commits if a developer committed a secret. So this is what you need to do as a dev sec ops

engineer. Plan it as you know once in 2 months activity or once in 3 months activity. You can also run it as a chron job. Just clone all the repositories. If you have 10 repositories, clone all the 10 repositories and run it leaks detect. Perfect. Now, okay, Abishek, this is all on the local. Okay. Now, a developer might follow the instructions. Some developers might not follow. Even if I share ignore file, okay, some developer might not add it. A developer is new, they might ignore or I will share the

pre-commit framework but developer might not install the framework. What can I do? I still want to secure my repository. Now the only possibility that you have is to create a CI/CD and make sure every time CI/CD runs on the repo. So I'll do that now. Basically on this repo, let's create a new file that is a CI/CD file. How to do that? Add a new file dot my bad dot GitHub. Now this is in your control as dev sec ops inner workflows and here add a workflow something like this. Okay, there you go. So the name of the

workflow is git leaks. It runs on every pull request. It runs on the push request even on the workflow dispatch. And this is the job. Now don't think I'm writing this. This is copied from the git leaks documentation itself. I haven't written even a single line of code here. I just copied it from official GitLinks documentation. And anyone would do the same because I don't know this action. I have to get this action from git leads documentation. I just copied the entire thing. And when

you do secrets dotgithub token, you don't have to provide the token. By default, GitHub actions, it has the default token. It will use the default token. Now let's see how this works. Okay, I will commit this change. Now imagine your developers did not add anything and they try to push a password to the main or they try to push the same AWS secret file whatever we used they try to push the same thing to the repository secrets env let's see what happens I will create a branch okay so

let's do that create a new branch let's call this as test branch from the main branch I'm creating the test branch and in the test branch I am adding this file I'll call this as secrets envsenv I am adding the same sensitive information echo or AWS secret access key perfect now I will commit this file file. This should go. I'm committing this to the test branch. Ideally, I should not do it, but just to show you, I'm implementing this. Now, from the test branch, I will create a

pull request to the main branch. Let's see what happens. Okay, we will create now on this pull request what will run the GitHub actions. the GitHub actions for the git leaks uh flow workflow. Okay, we will create the pull request. Instantly the git links workflow should run. Okay, my bad. You know I did a very simple mistake. So here I directly added a workflow file. What I should be doing? I should create a folder, right? The folder name should be workflows. My bad. So, workflows and within this let's add

something like kit leaks.ml. Okay. So, I missed that. Anyways, commit. Perfect. Okay. Now, let's see. We'll go back to our pull request. This is our pull request. Okay. So because the pull request is already open, what we will do? We will simply close the PR and create a new one. Okay, close the pull request. And this time go back new pull request from test to main ability to merge. Okay, create pull request. Okay, now the git leaks pull request check is working. And you can see here the check clearly failed. Let's

see the reason why run get leaks and it says leaks detected. Perfect. So this is in your hands. So that's why although you recommend git ignore file, you can recommend the common patterns. You can recommend the pre-commit hook. But as a dev sec ops engineer, make sure you add this CI/CD as well to your repositories so that you are 100% sure that even if someone tries to break the rules, even if a developer is willingly or unknowingly trying to push a secret, it can be through pull request, it can be

through commit, it is blocked by your CI/CD workflow. So now we are done with the first five concepts. So we learned about git ignore, pre-commit hook, git leaks. We learned about repository scanning and we also learned how to integrate git leaks into GitHub actions. Now let's learn about branch protection rules. Very simple. So branch protection rules you must have seen whenever you're working on the main branch. You must have seen the main branch sometimes throwing up a pop-up that your

main branch is not protected. The reason is simple. So you can automatically push changes to the main branch which should not be the case in the enterprise. For example, if you notice, I was running git push and I was pushing the changes directly to the main branch. For example, I created a lot of files here, the abc.py, text.txt, test.py, and I directly push them without any pull request, which is a bad practice. No developer, no devops engineer should be able to do that. On the main branch, a

pull request should be created and that pull request should always be reviewed by the fellow engineers. The review process is mandatory. Now to do that you need to protect the main branch. How do you do that? Go to the settings and in the settings head to branches and here just add a new branch rule set. It's very very simple. Add the branch rule set call it as branch protection. For example here you can enforce the branch protection status. So if you make it active that means most of the branches

will stick to the branch protection. All that you have to do in the target branch provide the name of the branch. So you can say the default branch which becomes the main branch. Along with it you can also include branches with the patterns. For example all the release branches, all the feature branches. That way you can also add. Now what will you add in the branch production? As I told you the most important thing that you'll do require a pull request before merging. The description also explains here

require all commits to be made to a non-target branch and submitted via pull request before they are merged to the main branch. Just check this box, scroll down and click on the create. That's it. branch production is now enabled to the main branch. Even if I try to push to the main branch now, it will not be pushed. Now let's take a look at arbback. What is arbback? Role based access control. Fortunately, when you are dealing with public git repositories, for example, you are on GitHub or you are on bid

bucket, arbback is very very simple. So if you are a DevOps engineer, you can grant yourself admin access. For developers, you can grant write access. For Q engineers, you can grant read access. So on how will you do that? Just head to collaborator section. If you are doing it for your entire organization within the organization settings, you will find option to do that. But because we are working on a repo, you need to invite the collaborator. Let's say I invite I amala which is my GitHub user

ID for example. Okay, of course I cannot invite myself but if I invite myself during the time of invitation I can grant the read access, write access or admin access. So this way whenever someone joins your organization you can invite them and you can grant them with the required permission. That's all. It's very simple when you are dealing with public git repositories on platforms like GitHub or Bitbucket. Now let's look at the most important thing which I told you mandatory reviews.

So what if you want two mandatory reviews every time a pull request is created. Right now for example this is a pull request that funny has created. Now I can directly go to the pull request and I can merge the pull request. Right? Maybe the CHX will run on the PR but I can merge it without a peer reviewing the pull request. That is without a fellow colleague reviewing the pull request. What if you want to enforce two mandatory reviews? Only after two mandatory reviews and only after the CI checks are passing the pull

request has to be merged. You can do that by using the mandatory reviews option on this GitHub or Bitbucket. Again, very simple. Go to the settings. In the settings, go to the branches, the branch protection rules that we created, right? Similarly, if you scroll down, when you enable the automatic pull request, require a pull request before merging. Here you can say number of reviews that are required. So if I say two reviews are required, the pull request will be merged only when two approvals are provided. And of course I

can also say require review from specific team require review from specific person as well. How do you define the person? You provide the name of the person in the code owners file. This is what I was talking about. I said we will learn about code owners. What is code owners? On open source repositories, you will have 300 to 400 contributors. Even within your organization, you might have a lot of contributors, but only certain people in your organization should merge the pull requests. Right? There can be 20

developers, but only five senior developers. What you can do? You can check this option require review from code owners and you will create a code owners file simple the name of the file is code owners just like how we created ignore here we will create a code owners file and within the code owners we will provide the github usernames of those persons if you go to popular opensource repositories you will find the same practice they will add the code owners and then they will check that option so

that whenever a pull request is created, GitHub automatically requests the code owners to review the PRs. Now, finally, let's talk about dependabot. Okay, so what is dependabot? Now, this is a very very important thing especially when it comes to dev sec ops. This is introduced by G distribution systems like GitHub. What dependabot does? It is capable of looking into the packages that are used by your applications. For example, your application is a Golang based application. dependabot can continuously

look at your go domod file and it can see what are the versions of the packages in your go domod and whenever there is a vulnerability in the version let's say you are using xyz package and the xyz package version that you're using is 1.0 zero. This morning a vulnerability is introduced on this particular version. Now obviously you will be late because you know you cannot know all the vulnerabilities that are happening across the world. So you might take 2 to 3 days to fix this vulnerability.

Someone has to look at it. Someone has to report it. But what dependabot is capable of? It's a bot. It's an application that will constantly monitor your repository. It will constantly look at your go.mod file. Not only go.mod, it can be pom.xml or it can be the npm related packages. Anything even it can be your docker related packages. It will monitor all of them. It will look at vulnerability database. a database that has all the vulnerabilities in the world and if there is a package that is

vulnerable it will automatically create a pull request and it can also merge the pull request. So within the minutes a vulnerability is disclosed in the world dependabot automatically creates a pull request and updates the version in your repository so that you are safe from the vulnerability. Let me show you an example. popular opensource projects do it. So this is Argo CD operator project which I used to contribute. If you look at GitHub along with the CI files you will find dependabot.yamel.

Now this dependabod.yamel because this is a g repository. The devops in here added this particular lines. So look for go.mod it's in the root repository. check every day and create a pull request. Pull request limit is 10 so that dependabot will not spam the repository. Similarly, let's say you have npm. All that you have to do is change the file. That's it. Okay, docker as well, docker registry and all the folders where you have the docker related content. So dependabot is very very useful if you are working with

multiple applications. You cannot look for vulnerabilities in different applications. Developers cannot do it. But dependabot can do it because it is a bot. One more time. What it will do? It will basically look at your yl file pom.xml XML or go domod npm related file and it will compare the version with vulnerability database. If your version is vulnerable automatically it creates a pull request and updates your good file. It can also merge the file for you so that by the time you realize dependabot

already fixed the issue for you. So these are 10 common patterns that dev sec ops engineers follow within the organization. Dogget ignore pre-commit hooks git leaks repository scanning integrating gitl git leaks into cicd branch protection rules arbback mandatory reviews code owners and dependabot. If you have any questions, do let me know in the comment section and make sure you go through the readme file for the quick revision. See you all in the next video. Take care. Hello everyone, my name is Abhishek and

welcome back to my channel. Today is episode 3 of Dev Sec 0 to her series and in this episode we will focus on infrastructure as code security. In the first two episodes, we learned about fundamentals of delicacy ops and securing kit. They are very useful videos. In case you did not watch them, I would highly recommend them after watching today's video. You can go back to the playlist and complete those videos as well. I'll share the link to the playlist in the description. Now, going back to the context, we will

focus on Terraform security in today's video. This video has three primary sections. The first one is the common best practices. What are the common best practices that dev sec ops follow? Then we will learn about a very interesting utility called check out. So we will learn how to identify insecure terapform files using checkout or insecure terapform configuration. And finally we will learn about hashikor vault. So we will learn what is vault and how vault is the best companion for terraform. Basically we will learn

secret management with vault. Now you don't have to get overwhelmed. All of these concepts we will learn with demos and the detailed documentation for the demos is already in our GitHub repository. So just like every video I've created GitHub documentation. So head to our dev sec ops zero to her repo and go to T3 folder. Here you can find the checkout.md and wall.md. So this is for the insecure terapform configuration demo again. So I have provided an insecure terapform configuration. You

can use this and you can scan the configuration using checkout. And then I have the vault.md. This is where lot of people get confused because to install vault to configure it there are a lot of steps. So I made it simple. All the steps are documented in the order and to make our demo interesting I made it real time. So instead of directly using vault with terapform we will also use GitHub actions. So basically we will learn Terraform in the CI/CD pipeline. how terapform is used with GitHub actions

and how dynamically AWS credentials for Teraphform are fetched using vault. This is what we are going to cover in today's video. So please try to watch it till the end and after watching spend 15 more minutes to complete the demo. Perfect. Let's start with common best practices. Now this is very simple. if you followed our previous episode because we covered common dev sec ops practices in the previous episode. Anyways, so basically when you are working with Terraform let's say you are a DevOps engineer your

requirement is to create an S3 bucket on AWS or you have to create two to three resources on AWS. Now the common things that you have to follow number one because you are creating resources on AWS or any other cloud platform you should not hardcode the credentials. Now this is a very simple one to eliminate this. We learned in the previous episode what you need to do you should create a dotit ignore file and within the git ignore file you have to store your env I mean you have to make an entry for env in the dotg ignore

because you are working with terapform you also have to make an entry of your teraphform state file and then you commit the git ignore to the g repository so that even your colleagues or anyone else who is working in the organization they also have a reference of your git ignore this is a very simple practice so create a doit ignore file add an entry for env if you're using one add an entry for terraform state and anything like your pimp files that you are using ID RSA or sensitive information this way

you will not even accidentally push your hard-coded credentials or sensitive information. Now there is a chance of course we are all humans accidentally you might miss adding a file entry to the getit ignore file. Now how to ensure that you might be a good DevOps engineer but your colleague might be a junior DevOps engineer. They might not addit ignore to their local machine. So this is where we learned about pre-commit hooks. So even while working with Terapform repositories, it is recommended to use

pre-commit hook. So that pre-commit hook will validate your commit. Whenever you run git commit, even before you run g push, you run git commit. So when you run g commit pre-commit hook is executed and it validates it verifies if any sensitive information is being stored to your local gate before pushing to the public gate. So what will you have in the pre-commit hook? I have recommended in the previous video to go with git leaks. Let me show you. Now if any of you haven't watched the previous demo or if you have to

focus more towards terapform we will do a very simple demo how pre-commit hook can prevent accidental pushing of terapform secrets to public gate. So for that we will clone our repository or you can even create a simple folder. But what's important is to use the pre-commit framework. So this is a pre-commit framework. If you want to go through the framework in a very detailed way, the documentation is available. So instead of writing your custom script, you can just install this framework. How do you do that? If you

are on Mac just run brew install pre-commit. If you are on Linux just run pip install pre-commit. Now once you do that within your repository or in a centralized location you have to create a dot pre-commit file. So dotp pre-commit configuration file and within this configuration file provide an entry for git leaks. What is git leaks? Git leaks as an utility which will verify any insecure or any credentials that are being pushed. So pre-commit is a framework that is using this utility. We learned a lot about GitLeks in the

previous lecture. But just to repeat again, GitLinks is a simple utility that will run and verify the files that you are pushing to your local Git. And within those files, if there is any password, if there is any secret or if there is any token, Git leaks will instantly block the commit. That's all. This is what you need to do. add the pre-commit configuration file and then you need to run pre-commit install. Let's say you already have the repository then you can run pre-commit auto update.

It is better to run pre-commit auto update as well in case you have other pre-commit configuration files. Now let's see the magic. Let's say I will add a secrets env file and within the secrets env file I pushed my AWS secret access key. Imagine I'm a junior DevOps engineer. I'm working with terapform. I need AWS credentials and I created secrets. Looking at some documentation on the internet or maybe by following chat GPT. Now along with other files I added this secrets env as well. Now even if I

accidentally push this to the git if I don't have this pre-commit framework and if I accidentally push this to the public git it's a major problem for the organization because once sensitive information is pushed to get it's never lost. Once you push your AWS credentials to Git, it's almost never lost because Git is a version control system. And in big organizations, Git is used by a lot of people. Even if you try to remove using a repo cleaner or if you try to remove by getting in touch with GitHub support

there is a possibility in this time by the time you reach the GitHub support someone might have cloned the repository someone might have forked the repository so during this time they have a local copy of your AWS secret so that's why it's considered a very big disaster pre-commit hook can help us here. Now that we configured the pre-commit hook, let's see if I run get commit- am. Let's say the junior DevOps engineer is working on advanced terraform workflow. So junior devops engineer runs this

commit because pre-commit hook is configured. See what happens. It will instantly block the commit. So it says the secret is redacted and also it says one leak is found so I will not let your commit go into the repo. This is a best practice that everyone has to follow. Unfortunately you know even after using git for a lot of years not a lot of people know about pre-commit hooks pre-commit framework not a lot of people have even heard of git leaks. So it becomes your responsibility as a dev sec ops engineer to prepare this best

practices document and share it within the organization. You cannot go to every person's laptop. You cannot create a dog ignore file. Even you cannot install the pre-commit framework and you cannot install git leaks. But what you can do, you can set up the best practices document and share it within the organization. Let's say even still someone does not follow. You know, even after you sharing the document, there will be engineers who will not have time to execute these commands. What can you do? What you can

do is very simple. Just go to the Git repository and create a CI/CD pipeline. something like this. So create a git leaks workflow and run git leaks on every commit and also run it on every pull request so that even if they store the AWS credentials on their local git. Now when they try to run git push to create a pull request or maybe to create a commit directly to the branch the GitHub workflow gets triggered and it will instantly block their commit or it will block their pull request by failing the CI check. So this is the

ultimate thing that you can do as a dev sec ops instead. The CI/CD pipeline configuration is within the same repository. Go to GitHub, go to workflows, you will find the file. So these are some common best practices that you can follow. Quick revision. What did we learn? We learned about three points here. One, the usage of dotgit ignore. Every repository in your organization should have a dogg ignore file just like the one that we have here. See how detailed this is git ignore file. It has all the configuration.

So you can find terapform related ignore files here. You can find python related ones here. You can find npm ones related here. Whatever your company is using. Then the next thing is to use pre-commit hook. So you should share a document of how to use the pre-commit hook on everyone's local machine. And finally step three is to set up a CI file with the git links. Now gitaks is not the only thing. The reason why I have been talking about gitlaks, it's the most easiest one to get started. Otherwise you have options

like truffle hog and you also have many other utilities. Perfect. So this is about the first section best practices. Now let's move to the interesting utility that is checkup. What is this? Now a lot of times when people work with Terraform know especially when junior engineers work with terapform like everyone they also use terapform to create infrastructure let's say a simple task is given to a junior devops engineer the task is to create s3 bucket now let's say this junior engineer

creates the s3 bucket using terafform takes help of chat gbt or maybe takes help of internet terraform documentation and creates the S3 bucket. This is all good. But what if the S3 bucket is public? Terapform does not warn this engineer. Even the pre-commit hook will not warn the engineer because the Terraform code for S3 bucket is absolutely right. But the problem is that the junior DevOps engineer created a public S3 bucket and within this public S3 bucket developers or your existing workflows push some

logs and also some sensitive information. Now they thought other engineers and even your existing systems they thought your S3 bucket is private. But unfortunately what the junior DevOps engineer created is a public S3 bucket. How can you prevent this? You might miss this in the code review obviously because you cannot go through each teraphform field. On top of that your CI checks like get leaks or your pre-commit hook is also not catching this. What do we call this? This is called terraform misisconfiguration

or insecure terapform configuration. How do you catch that? Check out can help you with insecure teraphform configuration. Let's see how it works. So we will first create an insecure terapform configuration. Right? So we will create an S3 bucket like configuration and within that insecure configuration I mean on that insecure configuration we will run the check off utility. I'll go back to my terminal. Okay. So I'll create a folder here. Let's called mkdir test. And within this test let's add a main.tf file.

Okay. Let's add insecure terapform configuration to it. So as we discussed this is an S3 bucket creation where if you notice carefully you know if you work with terapform in the past intentionally I have disabled blocking of the public ACL. So block public ACL false public policy false restrict public buckets also as false. So basically I'm making this configuration insecure. Now imagine a junior engineer got this code from chat GPT or somewhere from the internet. What junior devops engineer

usually does verifies the syntax using terapform linting runs the terapform plan to see if terapform creates s3 bucket or not. In this case, Teraphform would obviously create the bucket. But what we need is secure configuration or the secure infrastructure. As I told you, this is where Czechov helps. So all that you need to run check-en d followed by the folder in which you want to run it. In our case, I want to run it in the same directory where I have main.tf. Now you can see the magic. So check out

will start understanding your terapform configuration. It will automatically run checks depending upon your configuration. So it ran 2 + 4 six checks. So check out initiated. It ran six checks out of which two checks are passed and four checks are failed. And all these checks are related to S3 bucket configuration. Let's say you run terraform configuration related to EC2 instance and if you run check on that checkout will run the checks that are only related to EC2 instance. In our case because we are creating S3 bucket

all the checks are related to the bucket and this is an automatic process. Okay, let's see which checks passed and which checks failed. So the first two checks passed. Basically this one ensure S3 bucket policy does not lock out all the root user. We did not break that. Ensure S3 bucket does not have global view ACL permissions enabled. Even we did not tweak anything in that. Now see in which cases it failed. Ensure S3 bucket has public ACL enabled. Yes, we did that. So check out says this is a bad security

practice. Similarly, mure S3 bucket has block public policy enabled. Yes, we did that. That's why it failed again. I mean the check another check failed and another check also failed. So how is check out helping you here? It is helping you as typically like the test cases but you are not writing the test cases. Lot of times people might also ask you in the interviews how do you define your terraform test suite. Apart from your regular tests you can also talk about check out. So you can say we

have integrated check out in our configuration. Every engineer runs it locally. Even we integrated this to the CI file. So anyone tries to push a Terraform configuration to our GitHub repo even their checkout runs and validates the configuration. Now let's see by fixing it. Okay, we got to know what are the issues. Now let me provide the right configuration in the same file. I'll open the file again. Vim main.tf. All that I'll do I'll change the block permissions or the block features from

false to true. Again the same configuration is provided in the documentation also. You don't have to copy anything from my terminal. Let's save this. So we did not allow I mean we says block public ACL true. This means this will block the public ACL. This will block the public policy and this will also ignore any public ACL. Even when we look at the configuration, it feels secure. Let's see what checks. I'll save this and rerun check out D dot and in this case all the checks are

passed which is a good case. So ensure bucket has ignore public ACL ensure bucket has public policy enabled ensure bucket has public ACL enabled. Very nice. So we covered the first two parts. We learned the teraphform best practices from security point of view and we also learned insecure teraphform configuration verification that is using check out. Now let's head to part three that is hashikord vault. Now most of you might have this question abishek why should I learn something new? Why should I integrate my existing

terraform with a new tool like vault? Let's start with this. We will understand this through a scenario. Now, most of the times when people work on a hobby project, the workflow is simple. So, on the local machine, they create some Terraform files. They create av file, they create ignore and within the envapform credentials. Basically if they are trying to create resources on AWS they add AWS related credentials to the env file. Now after this they execute Teraphform that is they execute the Teraphform files on the same local

machine. Now obviously once it's executed on the AWS the resources are created. This is the usual workflow. Now if you understand the production workflow it is totally different. So when you work in the organization you don't follow this workflow but what you follow instead of writing or executing the terapform files on the local machine you would execute that through cic or continuous integration. I'll explain the total workflow now. So basically terapform files are stored in the g repository unlike the local

machine. If anyone wants to update the teraphform configuration they clone the g repository they update the changes basically they update the teraphform files then they push the changes back to the g repository. Once they are comfortable they push the changes and once the changes are pushed here CI is executed let's say GitHub actions and through GitHub actions resources are created on the AWS. Now you might ask why what is the advantage of this approach? There are two major advantages. The first advantage

individually nobody creates resources on the AWS. Everyone goes through the standard process that is through CI. So overall CI is the one that is creating the resources on AWS. Now CI will be provided with credentials and only through that credentials the resources are created on AWS. resources are not created by Abishek user AWS credentials or XYZ user AWS credentials. Everything will be created using the CI credentials. So there is accountability here. Who is accountable? CI is accountable. On top of that,

when you add your Terraform files to Git, just like your application source code files, you get the classic features of Git in place. The first and most important thing now you can start reviewing the changes. Imagine your team has a junior DevOps engineer and senior DevOps engineer. Junior DevOps engineer is quite new. Now when junior DevOps engineer gets assigned with the task maybe the junior engineer might write the wrong code on the local machine and execute that wrong code on the AWS although there is a senior engineer but

the code or the terraform change did not go through a review. How can this be involved? This can be involved if you employ this approach where the junior DevOps engineer clones the repo, makes the changes locally, push the changes back to the GitHub repository through a pull request and then the senior engineer reviews the Terraform code only if the senior engineer is convinced and only when the CI check is succeeded. Of course once the senior engineer is convinced senior engineer you know ask CI check to run

and once the CI check is run the CI creates the infrastructure on AWS. So you can follow this approach or what you can also do once the pull request is reviewed and merged then on the push action you can run the CI. Now once the commit is pushed, CI executes AWS infrastructure. You can also follow this approach. Additionally, now when you use version control systems like Git, you also get backup or you also get previous version of your Teraphform script. What is the advantage here? What is the advantage of having

your Terapform scripts version? Let's say even if you push a wrong infrastructure to AWS your production might break but instantly you can revert the commit because everything is in git just revert the commit again the revert pull request is also merged and then CI executes and creates old version of the infrastructure on the AWS. So reverting the infrastructure also becomes simple when you integrate your Terapform code with Git. Finally, there is also proper auditing. In this case, if you want to know 6 months

before who made changes to a particular TF file, if everyone is doing it on local, there is absolutely no way you would know who created a resource on AWS and who updated a particular resource on AWS. Let's say 6 months before someone created an EC2 instance. four months before someone updated the EC2 instance in the manual action way there is no way you can understand it but if changes goes through CI/CD you will definitely know who made the changes 4 months ago and who created the EC2 instance 6

months ago so overall what I'm trying to tell you here Terraform files should also be in the g repository And who should execute the terraform files? It should be CI systems like GitHub actions. Your Terraform resources should be created on the cloud provider only through the CI. Now the question is how do you provide credentials to your CI right? So if it is your local machine, you provide the credentials of your personal user, right? If I'm working on a hobby project, I will provide my AWS

credentials. If my colleague is working on a personal project, my colleague will provide their own AWS credentials when working with the hobby project. Now within the organization, everyone is running GitHub actions. So whose user would you provide or whose credentials would you provide? So you need a common credentials. As I told you, we want GitHub actions to be a single entity that is creating the info on the cloud. So this single entity should have a single credential. Now you might say, okay, Abishi, I will

create a service account, right? I'll create a botlike account and for this botlike account on AWS I will create AWS credentials. Absolutely point on you should do this. You should create a service account basically like a dummy user and for the dummy user you should generate AWS credentials and you should provide CI with AWS credentials. But the problem is that lot of people in your organization would have access to the CI. Let's say you wrote the GitHub workflow, a GitHub action that creates

resources on AWS. And for this actions file, the common way to grant permissions, where would you store AWS credentials? You can say Abishek, I'll go to settings. In the settings, I'll go to secrets and variables. I'll go to actions and here I will provide the repository secrets. So I'll store my AWS secret access key something like this AWS secret access key and AWS access key ID and within my CI file I would reference the credentials. This is also good but this is not completely secure.

Now I'm not denying this approach but this approach is not 100% secure. You should ask why. The reason is that understand this carefully. When you are working in a startup it's okay. But if you are working in an MNC you have thousands of users in the MNC. Even within a DevOps team you might have 50 DevOps engineers. You might have 20 to 30 people in your management who has access to the repo. You might have some five developers who has access to the repo. Maybe senior developers who has

access to the repo. Now if you create a bot account the service account on the AWS and if you create AWS credentials for this bot account and if you add this credentials to the GitHub that is if you add the credentials here what if one of these people left the organization but they looked at the credentials in the past and they want to use the credentials for bad purpose or they leak the credentials to a bad actor. Now most of the security threats happen through internal resources as well. It doesn't have to be they left the

organization. They can still be in the organization. These 100 people or 80 people who has access to this repo, they can look at the AWS credentials that are being used in the CI and they can share these credentials to someone external in the organization. Now because these are bot credentials that is because these credentials are from a central user like a service account you would not really know who leaked the credentials. For example, if Abhishek's AWS credentials are leaked, Abishek is

responsible. But if B's credentials are leaked, whom will you held the responsible? 85 people has access to this B credentials. One of these 85 people they looked at the GitHub repo and they leaked the credentials to the bad actor. So again there is no accountability here. That's why you need a strong secrets management solution. The problem that we are dealing with here is basically the long lived credentials. What do we call this problem? This problem is long lived credentials. You create a service account or a bot

account. You create credentials for it on the AWS account and you put the credentials in the GitHub repo and they stay for ever and ever for 3 months, 6 months, 1 year. Most of the teams they don't rotate the secrets. Even if they rotate once in 3 months, still you are at a risk period of 3 months. Within this 3 months, someone can leak the information. So what you need instead, you need a secrets management solution that can provide you with short-lived credentials. And these credentials should only live

for 10 minutes, 15 minutes till the time your CI executes. Right? Now what happens when you introduce a secrets management solution like Hashior Vault? Let's look at the workflow. Understand this workflow carefully. Now in this workflow which is typically a production-l like workflow terraform resources are stored to the git repository. All the terraform resources are in the g repository. Let's say one of the DevOps engineer clones this repo, makes the changes that is makes the update and push the changes back to the

Git repo. Now on the Git repo, GitHub actions is triggered, right? GitHub actions what it does? It does not have the credential. It would go to hashic orb vault and it would request a hashikorb vault. Hey, I am running a terraform script to create resources on AWS but I don't have the credentials of AWS. Can you provide me with a short-lived credential? That is can you give me AWS credential that lives for 5 minutes or at the most 10 minutes. You can design this according to your GitHub action

execution time. Let's say your GitHub action executes for 10 minutes. GitHub action requests hashik or vault to give shortlived credentials that lives for 15 minutes. So GitHub actions takes this shortlived credentials execute the resources on AWS and after the 15 minutes even if someone gets hold of these credentials they are dead. that is they don't work. That's the advantage of hashagicon vault. Lot of people think it is needed to store the credentials. No, you can store the credentials here as

well. If storing credentials is problem, you have lot of places that you can store. Best example, you can store it in the GitHub repository settings. But that's not the problem what Hashi or Vault is majorly solving. The problem that it majorly solves is with long lived credentials. So it will eliminate the longlived credentials and it will generate short-lived credentials for 10 to 15 minutes for the CI/CD systems. CI systems take it and they execute the resources on the cloud provider using

the credentials. And this concept is very very interesting. Of course I don't want to go too detail in it because it involves concept like OIDC lot of networking concept and you know secrets related concept but still just to give you a simple picture what GitHub actions does here. So GitHub actions it uses the OIDC and request the vault. Now vault is preconfigured by DevOps engineer. So a DevOps engineer, a senior DevOps engineer, whoever sets up Wault, they initially provide Walt with the AWS

credentials. Walt uses this AWS credentials and creates an IM user. That IM user is short-lived. Now, GitHub actions just gets this shortlived credentials through the OIDC protocol. And for this you need to do one-time setup right. So you have to install vault on a EC2 instance or you have to install vault on maybe Kubernetes and you have to configure the vault. That's what we are going to do at this point. But two things that you have to remember here know this is very important for your interviews. One, if someone asks you

what is hashik or vault, just don't say it's a secrets management solution. You should elaborate it. It's not just a secrets management for storing the secrets, but it solves a major problem with the life of the credentials. On top of that, you should also explain them just in a oneliner. GitHub actions uses the OIDC to interact with Hashior Vault and get temporary credentials. Perfect. Now that we learned about it, now that we understood the underlying concept as well, let's go ahead and

create this setup. First thing that I'll do, I'll go to my AWS account. I'll create an EC2 instance here. All the commands that I'm going to execute including EC2 instance creation and setup are within the document that I prepared. Okay, I'll just call it as W server Ubuntu go with the freed instance as itself. You don't need an instance that lives for or you don't need an instance with more resources. A free instance is good enough. I'll use my PM file. Okay, I need a security group. I'll

create that later. For now, I will launch the instance. Okay, I use the default options. I use Ubuntu. I used create and I created the instance. Let's connect to this instance. Okay, so this is a public IP. I'll just copy it and on my local machine I'll just use the SSH protocol. Okay. SSH - I have the PM file in my downloads folder. Downloads abhishek viral.pm. This is the pen file and here Ubuntu at IP address. Perfect. So I have my local connector to the EC2 instance. Whenever you work on a new virtual

machine, always make a habit to update the repositories. We are using Ubuntu. So I'll update the app repositories. Perfect. Then just install basic utility. We will use unzip here. Now why I'm using unzip you will understand that in the next commands but for now let me install unzip utility. Okay clear the screen unzip. Okay I installed unzip as well as wget. Now using wget that I just installed I will download the vault package. Now vault package is downloaded as a zip that's why I will use the unzip utility.

Perfect. Now I have the vault folder on my local machine. Just move the vault binary to user local bin. So we are moving it to an executable location. Now just run vault version to verify the vault server I mean the vault utility executable is installed. There you go. The version of the vault is 1.15.5. Now using the same executable just run the vault server. Very simple. Run this command vault server in the development mode followed by on the address on which you want the vault server to be listening. We will use local host

followed by port 80200. There you go. So we have the world server running on this terminal. We will keep this intact and on the other terminal. So I'll just duplicate this session. I'll reconnect to my EC2 instance and here I will continue with the next steps. Till now I just downloaded the vault utility the binary and I started the vault server. Of course the server is started on 8200 port. So we need to open the port as well. Let's quickly do that. Inbound traffic rules. This is the one

here we will edit the configuration the security group edit inbound traffic and we can just add the custom port 8200 or you can just open the traffic because you know we are just doing a demo but in real time don't open the entire traffic. Save the rules. Perfect. Let's access Hashikor vault. So we will take the instance IP one more time. This is the instance IP and here colon 8200. You can access the world server. If you want the token go back to your instance or using this root token just connect it

here. Token sign in. Perfect. So I used the root token which is root in my case and entered it in the root token section and this is my hashior console. So we have the world server running. Let's run I mean on the other terminal. Let's log the root. So I exported the address and I logged into the root. Perfect. Now we will run this command vault secret enable AWS right we are storing AWS secrets. So you can see here in the secret engines you have AWS secret engine. Now here as I told you we

will store our AWS access key as well as secret access key ID. How do we do that? We will use the terminal itself instead of creating it from here. So this is the command vault write AWS configuration and here you need to provide your secret access key ID and AWS access key ID. We will generate it. Go back to AWS security credentials and here scroll down create access key. Try it after this demo. I'll delete it. So there is no problem with security. Copy. Paste it in the access key ID section

and secret key. Paste it from the terminal as well. I mean the AWS console as well. Perfect. Data return to AWS config root. In my case, I'm using the AWS root user. But let's say if you're going with a normal AWS IM role, in this case, I'm using the root user. So, by default, it has all the permissions. But let's say to this AWS credentials if you want to add some permission I mean whoever is the IM user for this AM AWS credentials if you want to grant them additional permissions you

can use this command. In our case we are basically about to create an S3 bucket through the CI/CD pipeline. So I am adding S3 full access to this IM role or the IM user. As I told you if you're using the root user you don't need to do that but most of the cases you will not be granted with root user in your organization. So the AWS credentials that you're using should be from an IM user with normal permissions. Whichever permissions you want to add, add add permissions using this vault write command.

Cool. So, we granted this permissions as well. Now, let's enable the OIDC on this server. So, we will enable vault o enable JWT that is Java web token. Abishek. Why are we enable this? Why are we enabling this authentication? Simple. We want this vault to be accessed by GitHub actions. So for GitHub actions to access the vault and to request for the credentials you need to enable an authentication method. In our case we are going to use I mean GitHub actions will use JWT Java web token. This is

default. You cannot change it. Cool. In the next step, you enable JWT, but you have to provide the OIDC configuration that GitHub is going to use. This is provided in GitHub documentation. So, you have to run this command to enable GitHub actions for the OIDC configuration. Perfect. So now we enabled Java web tokens and we enabled OIDC discovery URL for GitHub actions. Perfect. Now let's create the policy. So world policy write a simple Terraform policy and finally we will bind this policy to

the GitHub repository. Okay. What is the policy that we created? We created the read policy and now we will grant this read policy to our GitHub repository. In my case, the GitHub repository is I am Viraala dev sec ops 0 to her. Now this repository will get the read access and through the read access and through JWT Java web tokens it will make a request to the W server and W server will return it with the temporary credentials. Perfect. If you see here token time is 1 hour. If you want you can change this to

10 minutes as well. It's up to you. Perfect. Now we have the GitHub actions all set. Let's go to our Terraform folder. We will focus on the execution now. So we will go to the GitHub repository where I created a Terraform folder. In this Teraphform folder, I will use S3 bucket configuration. Okay. Provider AWS US East1. I'm generating a random ID and I'm creating an S3 bucket with name dev sec ops world demo followed by this random ID so that S3 bucket name is always unique. Now this terapform configuration should

be picked up by the GitHub actions and GitHub actions should create this S3 bucket on the AWS. For that we need to create a GitHub action CI/CD. I'll go to do GitHub workflows and here I will add a file. Let me just call it as ci.l or infra dot yiml and I will write the simple GitHub actions here. I'll paste it here for your reference and I will explain this GitHub actions. What are we doing? Terapform deployment on which action? On the push action. Every time there is a commit to this repo. The GitHub action should execute.

But what should execute? We will use the Ubuntu runner. We will switch to the Terraform directory and we will just execute the Terraform. in it the Terraform plan and Terraform approve. Only thing that you need to modify in this CI file because it is very very straightforward. We are using a GitLab GitHub runner. We are switching to the terraform directory and we are running terapform init plan and apply. But what's in between here is the most critical part. Fetch keys from vault. So you need to provide the vault IP

address. In my case, the vault IP address. I'll just copy it. You can make it a variable so that you don't have to update it manually. This is a vault IP address and that's the only thing that I have to change. So now this GitHub action will make request to this vault IP address. It will get the AWS credentials and using this AWS credentials it will run terraform in it plan and apply. The interesting part that I talked about GitHub actions will not get access to our vault AWS credentials. the one that

we gave to Wault. But I told you vault will create a temporary user on the AWS account. Let's watch that. Now before I commit this file and before the CI gets execute, let's actually go to the AWS account, let's go to IM users and see if a temporary user is being created. We'll go to the users. Okay, right now we have four temporary users which I executed in the morning just for the test purpose. Now during this execution this should change to five. Okay, we'll commit this file.

Commit. Let's see what happens. Now the workflow should get executed on the parent of I mean on the parent repo. We'll click on the workflow. Okay, it is running. Let's see what's happening. Okay, it actually fetched the credentials from Hashigor MA already. So, a user should be created. We are here still four users. If I refresh, it should be five. There you go. And the lifespan of this user, the one that is newly created is only for 10 minutes. Even if someone hacks it, there is no problem. Let's go back. The CI

execution is complete. It said Teraphform in it is done. Teraphform plan is done. Terapform apply is also done. Let's verify. We'll go to our AWS account again. But this time I will look at S3. Okay. On the S3, let's see if a bucket is created. General purpose buckets. And we have dev sec ops vault demo. So this is just created. We have other buckets as well. As I told you, I used it for the demo purpose. But every time you run this CI file, a new bucket is created because in our terraform script, we are asking it to

create a bucket. This is how things work in real time. What is the major advantage of hashicorn bolt? I'm repeating this again. I know but it's very important for your interviews to explain. Hashior vault provides a short-lived credentials and this shortlived credential can be used in the GitHub actions. How GitHub actions is using them? It gets the credentials using OIDC and Java web tokens. Now if you're worrying about the configuration that we executed, the commands that we executed, don't worry.

Even I will not remember those commands. Everything that you have here, the wall.md file I have taken from official documentation. I've collected it from different sources. So even if you're doing it in your organization in real time, you will not remember these commands. You will only get it from your documentation. So don't worry about the commands. Focus on the concept. Run every step in the world.md by yourself. understand what every command is doing. Finally, understand how Terraform works with vault.

This is the demo for today. I hope you liked basic security practices, check out and also WA integration. If you have any questions, do let me know in the comment section. See you all in the next video. Take care. Hello everyone, my name is Abhishek and welcome back to my channel. Today is episode 4 of Dev Sec 0ero to her series and in this episode we will focus on securing containers. In the last three episodes we learned introduction to Dev Sec Ops. Then we learned securing Git. In the previous

episode we learned security best practices for Terraform. In the same way, in today's video, we will learn security best practices for containers. We will focus on five different topics in today's video. Starting with what are the security challenges of running a container with root user which a lot of people do especially the beginners and how to run the containers as nonroot users. What changes do you need to make in the Docker file? Then we will learn what are advantages with multi-stage Docker builds from

security point of view. Then we will learn one of the most important security aspects for containers that is display images. What are display images and how can they add security to your containers? Then we will learn about simple but very efficient thing that is dod docker ignore. How can you introduce this to your project? And finally we will learn about hardening container runtime. So we will learn how can you add few parameters to your docker run command that can harden the security of your

container. So these are five different topics that we will focus in today's video. We will go step by step. We will learn each topic using a demo and you don't have to worry. Everything is documented within this GitHub repository. So if you go to day4 folder, I'll share the repository link in the description. Just head to the day 4 folder container security you will find the readme file and within the readme file you have all the steps commands that I'm going to use even configuration

files. So after watching the video, spend 15 to 20 minutes more and implement the demos at your end. And to add to it, we will learn using a sample application in today's video. We will create a docker file for a sample application which is a NodeJS application and we will learn how to improve the docker file to accommodate the best practices. Now what is this demo application or the sample application? Because our focus is not about application. I've taken a very simple hello world NodeJS application.

So this application runs on port 3000 and when you run the application it will just return hello from the container. Then it also returns the user ID with which container is running. Let's say you run the container with root user, it returns the user ID as zero. If you run the container with any other user, it would return the user ID accordingly. And finally, it also prints the host name. So these are three things that the application returns. NodeJS application returns when it is run within a

container or even outside. and how to run the application. Again, this is something that you need to know before you start writing the docker file. So to run this NodeJS sample application, first you will build the application using npm install and then you will run the application using npm start. That's all. This is good enough information for you to get started with the Docker file. So all the files are here. app.js is the source code for the NodeJS application. Package.json is the dependency file. So

here we have express as one of the dependencies for the application. Perfect. Now let's get started. So let's see how a junior DevOps engineer who does not care a lot about security but cares about completing the task. What is the task to containerize this NodeJS application? How would that junior DevOps engineer write docker file for this use case? Of course, the very first thing that this junior DevOps engineer does take the terminal, right? So I have a terminal and then the junior engineer clones the project because to

do anything first the engineer needs the source code so this person would clone the repository in our case the source code is in the same repository dev sec of zero to her so let's get the github URL and run g get clone inside a directory Okay, I'll go with Docker directory on my local machine. Okay, so I already have the project here. Let me just remove the project so that we can start from the basics. Get clone followed by the URL. Perfect. T4 folder dev secc ops 0 to her t4 the junior

engineer starts writing the docker file or let's say devops engineer not caring a lot about security vim docker file perfect now because this is a nodejs application right so what we are dealing with is a nodejs application so what should be the base image base image in this case should be the node nodebase image so that you don't have to worry about the NodeJS installation. Let's say you go with Ubuntu base image. In that case, you have to use the run command to update the app repositories

and then you have to install Ubuntu on the base image. Instead, you can directly go with node 25 because 25 is the latest version. If you're not sure what is the latest version of the image, just head to the docker hub and you will find the latest tag version. Perfect. Then then the junior engineer would create the work directory let's say / app so that all the commands would run within the work directory. Then the junior engineer would copy the source code in this case app.js and package.j JSON

then the junior engineer as per the instructions here the first command is npm install and then npm start this is for build and this is for running the application okay npm install expose the port 3000 and finally within the cmd or entry point the junior engineer would add the fields npm followed by start. Now this can be within the entry point as well. Perfect. So now the junior engineer verifies the docker file by running the docker build. Right? Let's see one more time. Cat docker file. Okay. The file looks good.

How do you run the docker build? Very simple. Docker build. Basically, we are creating docker image from the docker file. Docker build. Let's provide it with a tag. Let's call it as insecure image followed by dot the location of the docker file. Okay. So, I don't have docker running. I think I stopped docker sometime back. Let me just restart Docker. I'm using Docker desktop. So I just started the Docker process. Let me verify again. There you go. Now the build is running and we have the Docker image created.

Cool. So now the junior engineer verifies by running the container to see if the NodeJS application is running. How would the junior engineer do? Docker or let me clear the screen. docker run hyphen it interactive terminal or without an interactive terminal just hyphen p port 3000 bind to the local host followed by insecure image okay so the container is running as final step junior engineer would take a duplicate tab and verify by just running curl localhost colon 3000. There you go. Hello from container

host name UID. Now the task is complete right. So the junior engineer or someone who is assigned with this task and does not care about security. They would say there you go my task is complete. I created the docker file. The docker image is also created and I verified by running the container that NodeJS application is running successfully within the container. Now even you might say Abishek the task is achieved. What to do more? Let me tell you there are security challenges with the Docker file

that is created and also issues with the container that is running. The first and the foremost issue if you look at the UID the user ID is zero that means this container is running but it is running as a root user I can also show you now I'll just stop the container this time I will run the container in the interactive mode so if I just do hyphen it followed by sh to take the shell on the terminal or bash as well. Let me just do sh and here if I run id you will see the ID is the root user that means this

container is running as a root user. What are the security challenges here? What are the security issues? Very simple. Take example of your laptop. Would you give access to your laptop to someone as an administrator? Typically no. Because they can create new files. They can download anything on your laptop or even if they are bad actors, they can run hundreds of process on your laptop and technically make your laptop unusable. In the same way, now if a bad actor gets access to your container, the bad actor

can create multiple volumes within the container just like that to make you know your container look heavy or maybe to indirectly if the volumes are running this is stored on the local machine where your container is running by default. So this way they can even perform spam attack on your host machine. Same way just like volumes they can also run any number of processes. Imagine a bad actor running 200 processes within the container just to make your host the container is running on a virtual machine or a host. If 200 process are

running within a container they utilize the resources on the host. Technically your host becomes inaccessible or other containers than that are running on the host do not get enough resources. So they can perform DAS attack or most importantly what a good bad actor can do or what an efficient bad actor can do is because the container is running as a root user they can even get access to the host. Why? Because if you're using docker as runtime through the container you know they can access the docker

demon because they are a root user and from the docker demon because docker demon runs as a root user they can get access to the host as well. Understand this carefully. You are only thinking about container right? You are thinking that my container is running as a root user. What's problem? But if your container is running as a root user because the container is running on a host, right? Let's say the host is a virtual machine and the container is running using docker runtime and docker runtime always

runs as a root user. Docker demon runs as a root user. Now this bad actor can get access to the docker runtime and from the docker runtime they can get access to the host machine. Now if they get access to the host machine they are already within your network. They have complete access to your infrastructure as well. If you don't have proper virtual private cloud now this is something I'm not making it up. You can find a lot of similar stories on the internet because of using a root user within the container. Lot of

company's infrastructure was compromised. You can eliminate that. You know, if you are a good DevOps engineer, if you want to follow security best practices, just using two more steps in the Docker file, you can run your Docker container as nonroot user. Let me show you how you can do it. Two basic steps. Now use the same Docker file. There is no problem. Vim docker file. Now I will update this docker file. One I will create a user within the docker file just before you perform any instructions within the docker file

even before copy just create a user. So here group add - r user. So basically I'm creating a group and basically I'm adding a user to the group. I'm calling it as app user. If you want you can call it as abishek user, you can call it as xyz user or you can call it as a developer. Whatever you feel like and just switch the user. How can you switch the user? Use the user keyword within the docker file and the name of the user that you created. That's all. Till here commands are executed as the root user. And when you

use this user keyword from this point the user is switched to the regular user in our case app user. So now whenever someone logs into the container they will log in as a app user. This app user does not have elevated privileges like your root user. So even if they want to perform something, they will fail to do it. Let me show you. Now we will create another docker image. Okay, this time docker build t. Let's provide the tag name. Let me just call it as no rootless image or nonroot image. dot. Let's see what happens now. Okay,

the image will be built first because of the Docker cache. Docker already has the cache. There you go. Now, docker run. Let me run it as an interactive uh terminal so that I can show you. Docker run- it nonroot image sh. Okay. So I'm taking shell within the container one more time. Enter. Now let's see id. I'll run the id command again. nonroot user. So you can see this time user is the app user. Abisha can you show by running some command so that we know it does not have elevated privileges. Let's see. We will

run apt install x a random package. What does it say? Permission denied. We can also try with pseudo. Let's do pseudo apt install x. What does it say? P sudo does not exist. That means using this user you cannot perform pseudo. So this way now the end user whoever has access to this container or even a bad actor through some way if they get access to the container still it's not a big problem for you because they can only access the container as a nonroot user but a still the application runs within this

container. Yeah, we did not make any significant changes. If we just do docker run- p 3000 col 30,000 followed by insecure image. Sorry um nonroot image. We should see the container running. It says port 3000 is already in use. Let's change this to 3001. Okay. on the other terminal this time curl 3001 there you go hello hello from the container host name user ID is triple 9 so the application is running and we also ensured application is secured all with just two lines more to the docker file what we added we added the run

command where we created a user and a group and then we switched to the user before running the cmd. Now the junior devops engineer or someone without security approach they might think that the task is complete. Okay. Now I'm running the container as a nonroot user. My task is complete. Let me tell you the task is still not complete because if you do docker images you will notice something very very important. What did we create? We created nonroot image user. This is the final one that we created. The content

size that is the image size is 401 MB. Just for a simple hello world application, your container image size cannot be or your docker image size, let me put it that way, cannot be 401 mp. It's too much for your image. All that you have is this simple thing 20 lines of code. But Abishek, how does that create security issue? We are talking about dev sec ops. We are talking about security issue. How the container image size impacts my security? It does because when your image size is more, understand this carefully. If your image

size is more that means you have more packages within the container right? It can be two things. It can be system packages that is your base image packages the one that comes with note 25. This is the base image that we used or it can be because of your application packages. So we ran npm install right? When we run npm install to build the NodeJS application, NodeJS or npm can download packages from the internet and even after the application is built some of these packages can stay within your

container image. Now this is a problem because you don't need these packages at the runtime. See there are two stages for every application. One is build stage, one is the execution or the run stage. You need a lot of packages during the build stage because you want to download the packages from the internet, right? to build the application to compile the application but you don't need all of those packages during the runtime during the runtime you have if it's a Java application a jar file war file or you

have basically a binary and you just need this binary and runtime environment to run the binary that's it now if you keep this packages that you downloaded still in the container image you are not checking on the timely basis if these images are vulnerable today those images I mean packages today those packages are not vulnerable let's say if you take example of ABC package okay ABC package you must have downloaded 1.0.0 zero for building your application. Today this package version is fine but

maybe 2 months down the line there is a critical security issue with this version. Now ideally you should upgrade this to 1.0.1 but why would you log into the container? Why would you bother about checking the version? A lot of people would ignore that. So your container it is running in production and it is still running with 1.0.0. zero. Imagine a bad actor get access to your container. They might get access as a non-root user but still because there is a vulnerable version. The bad actor can attack through this vulnerable

package version. So this package can be a threat for your organization. How can you avoid this? Very very simple. Just break your docker file. Okay, just break your Docker file into two parts. This is what we call as multi-stage build. The first stage is the build stage. Second stage is the run stage. If required when you are dealing with you know a two application or something like that, you can even add more stages. But typically you can add a build stage, you can add a run stage. In the build stage, for our use case, focus

on npm install. In the run stage, focus on npm start. And from the build stage, just take the binary that you created to the run stage. And finally, the image that you created will only have the run stage artifacts. Okay, this is the advantage that Docker brings onto the table. In the Docker file, you can create any number of stages. But what Docker will consider only the last stage. I mean the image that you have will be only created from the last stage. So what you will do as a dev sec ops engineer, you will use the

build stage. In the build stage, you can download any number of packages. You can get them from the internet. You can do anything. Build the binary and to the run stage just copy the binary and have the container runtime. In our case, what is a container runtime? Node is a container runtime. So in the run stage, you can just have node container runtime and binary that you created in the build stage. That's it. Create a cmd and you can have your runtime environment ready. And when you do this, your container image size will

significantly go down. And if the container image size significantly goes down, that itself means there are less number of packages within the container. When less number of packages, less impact radius. Let's see this in live. Okay, I'll break our container image into two stages. This time I will create a new docker file or let's delete the existing docker file rm or maybe let's create a new one. Okay, I'll create a folder mkdir multi-stage cd multistage vim docker file. Okay. So we need two stages as we

discussed. The first stage we will keep it the same node 25 and I will provide an alias to this stage. I'll just call it as builder. Let's choose work directory. Build stage will look more or less the same. Work directory. This time I'll just call it as / build. Then we will copy the package.json first. Abhishek, why do you only copy package.json? I've seen this multiple times. You use the two different copy commands. The first time you copy the dependency file, you run the dependency command and

then you copy the complete source code. I've seen this a lot of times in your playlist as well. The reason is simple. Why I typically do this is because Docker builds layer by layer. That means this will be a different layer and this will be a different layer. So that let's say your source code is changed. Docker does not have to run the dependencies again and again. Typically a lot of times dependencies is the one that takes a lot of time for building. So if you want to reduce your docker build

execution time, make sure you break your dependencies in a different copy command and you take your rest of the source code in a different copy command. Perfect. So as per the concept that we learned in the build stage, we are using npm install. Now we will create a runtime docker stage. Here instead of taking note 25 I will take node 25 slim. What is the advantage here? Node 25 slim is a lightweight NodeJS image which will not have a lot of things that your regular node image has. Are you fine

with it? Yes. Why? because we can go with node 25 slim or lightweight image because in this stage we are only executing the NodeJS application or we are only running the NodeJS application here we are not building the application so we don't need lot of packages that are available in the node 25 perfect and these images are specially created per multi-stage docker builds so if you look at docker Docker hub you will find Python slim image you will find NodeJS slim image which you can use in the runtime environment or runtime

docker build. Perfect. Now again we will choose the work directory. This time we can choose / app. Copy. As I told you we will copy the binary or the artifacts that are created in the build stage. Now what are the artifacts that are created in NodeJS? Artifacts that you create are basically the node modules. Apart from that, we will also copy the app.js. Okay, which is our application source code. That's it. We will expose 3000 and finally cmd followed by node app.js. So we will not run node start. We can just run node app

dot js because we already have the node modules that are the modules required to run this application.js file. Now you will notice when you build this docker image the size of the image will be significantly less. Let's see docker build t. Now let's call this as multi-stage image followed by dot. Okay. So it says fail to build the computer cache fail to complete check sum at reference copy package.json to dot. Okay, it's a very simple mistake. Basically we have created a new directory like if

you look at our pwd I have created a new directory called multistage and here I don't have app do.js or I don't have package.json right so what we need to do either we will move this docker file a folder back or we need to copy app dot js and package.json JSON to this folder. Right? Only then your copy command works. Let's do that. Copy dot dot app do.js to this folder. Similarly, copy dot package.json to this folder. Now let's see. Okay, we will run the Docker file again. This time it should work.

It's working fine, I guess. There you go. So, we have the Docker image running. Let's see the size of this Docker image. Docker images. Okay. Multi-stage image 79.5 MB that is straight from 400 MB to 79.5 MB and most importantly now the application that we're dealing with is a very small application. It just has app.js and package.json. Imagine when you are working with an e-commerce application or a realtime application it has hundreds of files then it will be more advantage for you

in terms of the container image size. So we saw the advantage from 400 MB image size strand down to 80 MB. So it is typically 20%age of your entire size. So 100 MB to 80 MB 80% reduction. Cool. Now if you think your job is done, let me tell you your job is still not done. Why? Even now you have a chance to reduce your container image size and you have an opportunity to use even less packages on your container image. But Abish what packages? Let me tell you if you exit to this container image once

again. So if you run docker run followed by multi-stage image sh. Okay, let me exect to it by taking hyphen it. You will notice here there are a lot of system packages. One example apt. Why do you need apt in the container image? Okay, if you want to run this container in the production, what is the advantage of apt? Why do you need it? You might say abishek nobody can install packages using apt then what is the purpose you can just remove it similarly there will be hundreds of packages on this node 25 slim image okay it's a slim

image it can help you but still you have hundreds of packages in it which are not needed a good hacker will still find the ways if they have apt they will find the ways somehat the other way hook or crook to install something on your container and hack your container not only apt I'm talking about /c folder there will be hundreds of packages in it what is the purpose of it this is where dess are images which not only help you with reducing your image size from app packages point of view but they can also

help you reduce system related binaries within the container. How do you do that? You don't have to change a lot. You can just use the same container image that you used. Only change that you have to make within the docker file. Let me show you by updating it. Right from multi-stage to dist is very very small change. All that you have to do within your runtime right here from node 25 slim go with a displayless image like this GCR Google container registry it has distress images NodeJS 20 Debian 12 but Abishek why are you not

using node 25 this is one drawback when you are going with distress images you have to depend on systems like GCR where Google cloud or open-source contributors where they are creating digitalized images for you and they might not keep it up to date with latest NodeJS version. So right now we don't have note 25 note 20 is up to date so we can use NodeJS 20. So you can keep it the same way. In fact, you can remove node here and just run app.t js because the entry point for this dless image is node. So

even if you have app dot js it will run node app dot js. Let's see. I will save this docker file. This time let me docker build t followed by let me call this as distro or distro less image demo followed by dot. Okay now let's see docker images. Okay, what is the image size now? From 80 MB, it went down to 52 MB. Further reduction in the size typically looks like 85% reduction in the container image size. Great. Now, let me show you the application works absolutely fine. You might think

the container image size is less so your application might not run. It actually runs. docker run followed by hyphen p. This time let me use 3000 to 3000 followed by digitalize image demo. Okay, let's verify. I'll just do localhost 30002. There you go. Now the application is still running on your machine. Cool. What else? Abishek. Now the application is running. We verified application is running within the distless image as well. This is absolutely fine. Your docker file is now secure. You can even ship

this docker image to production. But there are few simple best practices that you can follow which can add to what we created till now. A simple best practice that devs secops instance follow is to create a dot docker ignore file. So just like how we create dot get ignore file good dev secops instance create dot docker ignore file within the repository right so within this dev secop zero to her repository where we have the code along with these files we can also create dot doer ignore file what is the

purpose of it very simple in the docker file we are usually using copy B dot dot right it's a common practice where we use this command now what does this command do this command usually copies everything within the folder onto the docker image so it copies the app dot js it copies package dojson what if no what if it accidentally copies the files likeget obviously you have git on your What if it copies it? Or what if it copies files like what if it copies node modules, right? Even why should it copy docker file,

right? This can only increase the size of your image. You can eliminate that by creating a dot docker ignore file. So just create dot docker ignore. put it in the repository where you have your source code and when you run copy dot dot what docker does it looks at the dot docker ignore file and it will ignore copying everything that you add to the dot docker ignore file very similar to dotgit ignore concept when you add dotg ignore g will look at that file and stop tracking the files that are in dot got

ignore similar Similarly, when you add dot docker ignore, when you run the copy command, docker will ignore copying these files onto the docker image. That way, you can reduce the image size and copying unwanted files to your docker image. Perfect. Finally. Okay. Now that the dot docker ignore concept is clear and you know how to build good docker images. Let me tell you one simple concept. How to use your docker run command. But Abishek we know how to use the docker run command. Now we just use docker run- p follow

followed by the port or we just use docker run- it for taking an interactive terminal within the docker that is definitely good but a best way of using docker run is by passing parameters to docker run something like this these are parameters that I usually recommend. What are these parameters? Very simple. Let me go through each and every parameter. hyphen read only. Something that I highly recommend. What this does? Docker run-y read only. That means the process that is running within the container becomes a readonly process

instead of write process. But Abishek what if my process want to write some information to the local host or what if it wants to write some information to the host system you can use hyphen temporary file system and provide /tmp or any temporary file system because this will prevent the hackers if hackers tries to create more volumes or if hackers tries to create something on the host from the container this will prevent Because your container is running as a readonly container and if at all it wants to write

something there is a temporary file system to which it can write. Perfect. Similarly you can also pass hyphen capd all and hyphen security opt no new privileges. Both of them are used typically to ensure your container does not gain any additional powers. You know a lot of times as I told you hackers they try to escalate the privileges when they get access to the container. The first thing that they try is to add more privileges to the container or they try to see if your container has any elevated permissions so that they can

not only hack the container but they can also get access to your host system. these two parameters. Even if someone like a junior DevOps engineer, someone with a lot of security knowledge, even if they get the containers, using parameters like this in your runtime can highly help you secure your containers. On top of that, hyphen P 100, this is basically to limit the processes. In case someone tries to perform DOS attack on your container, you can limit the number of processes so that they cannot

run more processes on your container. If they run more processes on your container, it will indirectly impact other containers because they cannot get enough resources like CPU and memory from the host. You can prevent that by limiting number of processes. But Abishek what if you know they try to use a single process and they try to take more resources from the host. What you can do you can limit your container resources by using hyphen memory and hyphen -y CPU. So using these parameters you can tell my container should use

only these number of parameters. So these are called hardening parameters or these parameters can help you enhance the security of your container during the runtime. Once again all the notes is available in the day four folder. Just go through the readme file. I would highly recommend that to you and also try to practice the commands along with the configuration that I provided in the readme file. So go example by example. Use the docker file, run the commands, see the output, then go to the next

step. This way you will never forget the concepts and you can become a better dev sec ops in snare. Thank you so much for watching today's video. If you have any questions, do let me know in the comment section. See you all in the next video. Take care. Hello everyone, my name is Abhishek and welcome back to my channel. Today is episode 5 of Dev Sec 0ero to her series and in this episode let's focus on securing Kubernetes. In the previous episodes we learned how to secure Git, how to secure Terraform,

how to secure your containers. In the same way we will learn what are the security best practices when it comes to Kubernetes. In today's video, we will cover six different topics in the video. Starting with what are name spaces and why are they called the first step of security. Then we will learn about arbback in kubernetes where we will learn what is arbback what are different components that can help you with arbback. Then we will learn about network policies in Kubernetes. For example,

how can you restrict access to your database that is deployed in your Kubernetes cluster. Then we will learn about advanced policy enforcement. For example, how do you set up and manage your Kubernetes cluster as per the compliance of your organization or as per security requirements. where we will learn key orno. Yes, I will also explain you how to set up and configure key or no in today's video. Then we will learn about secrets in Kubernetes. How do you store sensitive information within your Kubernetes

cluster? And finally we will learn how to store your Kubernetes secret references within version control systems like Git where we will cover a very important concept external secrets operator and I'll also explain you how can you integrate external secrets operator with vault provider. So these are the concepts that we are going to learn in today's video. Most importantly, this video is going to be completely practical. So, every concept that we are going to learn today will be followed by a demo

and you don't have to feel overwhelmed. All the documentation is covered as part of the G repository. So, this is a GitHub repository that we are using for the series Dev Sec 0 to you can find the link in the description as well. If you head to the T5 folder, you will find a readme file. And this readmi file has everything that I'm going to cover. You can see here the first section is name spaces. The commands related to name spaces. Then the second section is arbback commands configuration verification everything

related to arbback. Similarly, network policies, Kerno and even external secrets operator. So after watching the video, I would request you to spend another 30 minutes and practice everything that is covered in the documentation. That way you can never forget security in Kubernetes. Perfect. So now without wasting any time let's get started with the first concept for today's video that is name spaces. So what is a namespace? As I told you it's a very simple concept yet very very powerful.

Name spaces in Kubernetes offers the isolation that helps you manage a single Kubernetes cluster across different teams in your company. Let's try to understand this better through a realtime example. Imagine you created a Kubernetes cluster within dev environment of your company and let's say you have three different teams in your company. Payments team, shipment team and scores team. As soon as you create this Kubernetes cluster, let's say you posted on Slack that you created a Kubernetes cluster

and these are the cluster details. The DevOps instance who are managing the deployments of these teams, they start deploying their containers to the cluster. Now if you imagine the world without name spaces, the DevOps engineer of the payments team can just deploy there one of the container as pod other container as another pod and maybe a config map. Similarly to the same cluster, the shipments team DevOps engineer deploys let's say two pods and the score team DevOps engineer deploys two pods and two

config maps. Now this looks good to you but what if after some time the DevOps engineer of the payments team accidentally deletes a pod and a config map of the scores team. Now this is a big issue because there is lack of ownership on the cluster. Similarly the scores team DevOps engineet can accidentally delete the part of the shipment team. This is not only a security issue but this is also lack of ownership on the cluster. On top of that you will also start receiving the complaints. For example the scores team can complain

abishek we have two pods and two config maps but the shipments team and the payments team are utilizing 90%age of the cluster resources. Now this is another problem that is resource utilization. Ideally you might want each team to utilize 33%age of the cluster or maybe because scores team has two pods and two config maps and maybe these pods are very important for your company you might want them to utilize 40% and other teams to utilize 30% each. How do you define this? that is how do you define

resource utilization on the cluster. Now you can only do these things if you logically break your Kubernetes cluster. One thing is sure you cannot physically break a Kubernetes cluster because this is not a physical thing. Only thing that you can do you can logically break the cluster and this logical isolation is what we call as name spaces in Kubernetes. So when you have three teams in your company you might create three name spaces and you might assign one name space for a team. So this name space is

for the payments team. This name space is for the shipment team and this name space is for the scores team. Now the DevOps engineers who are working with deployments they start deploying their workloads. It can be pods it can be config maps it can be uh secrets or anything they would deploy their workloads to the relevant name space. Very simple. Previously they just run cubectl apply. They would use the same command but this time cubectl apply followed by hyphen n followed by name space that is assigned to that team.

But Abhishek okay this is fine. Now the payments team devops engineer would use hyphen and payments shipment teams devops engineer would use hyphen and shipment. But how to solve the problem with resource utilization that still exists? You logically break the cluster. But how do you define a particular team or a particular name space should only utilize let's say 30% of the cluster resources. It's actually very simple. So Kubernetes solve this problem using resource kota. So when a DevOps engineer creates a

Kubernetes cluster or an administrator creates the Kubernetes cluster and when they define a name space while creating the nameace they also create resource kota for the name space it's just a Kubernetes resource and within the resource kota they define the number of CPUs and the amount of memory that a particular name space can So if this Kubernetes cluster has let's say 30 CPUs and 30 GB RAM all together while creating the name space called payments they can also define the resource kota

and say this name space should utilize 10 CPUs and 10 GB memory. No matter how many workloads they deploy it would not exit 10 CPU and 10 GB RAM. This way resource kota helps the DevOps engineers or the administrators to maintain dedicated amount of resources for the name space. Perfect. Now let's see the nameace in action. How do you create a namespace and how name spaces are used in real time? For that we would need a Kubernetes cluster. My go-to thing is always kind. So I just installed Docker on this

machine. Now I'll just run kind create cluster hyphen name followed by let's call the name of the cluster as ks demo. So you would need docker running on the machine and you would need kind running on the machine. It just takes two steps to install Docker and kind. There you go. So, we have a Kubernetes cluster. It's that easy. Now, once we have the Kubernetes cluster, how do we logically isolate it? How do we create name spaces? So we need this command cubectl create ns which is the shortand

for namespace followed by name of the nameace. If you want to call the namespace as payments namespace you can just say payments ns name space is created. You want to create another namespace just call it as score namespace. Now if you run cubectl get ns you will find along with the default name spaces there is a payments name space and there is a scores name space. Now let's understand how workloads are deployed to the relevant name space. For example devops engineer of the payments name space wants to deploy engineix within

the payments namespace. So they can just use the command cubectl create deployment or they can prepare a YAML manifest as well. But because this is just an enginex deployment I'll show you through the command line. They can just run cubectl create deployment name of the deployment it can be engineix it can be enginex payments followed by the name of the image which is important it has to be right followed by hyphen n payments. Now you will see here okay or there should be some typo in the name space that we created cubectl get

ns okay it's payments name space so I have to just change it to payments ns now you will see if I run cubectl get ports there are no ports here but if I just do cubectl get pods hyphen n payments NS you will see the engineix pod running in it. The same way if the scores DevOps engineer wants to deploy the same engine pod within their name space they will run the same command but change the name space to score NS. So this is how name spaces are used in real time. But you might have a question

here Abishek. Okay, we logically broke the Kubernetes cluster into multiple parts. But what if you know I have a requirement where I want to grant permissions to my pod. You know I don't want my pod within the payments name space to do every action that is possible. In Kubernetes there are multiple actions like creating a config map, reading a config map, reading a secret, writing secret, reading custom resource definition, any number of actions. But I want to restrict my pod only to read the config map. That's it.

Nothing more than that. If my pod tries to write a config map to the local disk or maybe to the node of the cluster, this action should fail. How do I do that? Basically, how do I grant permissions to my Kubernetes pod? This is where our second concept comes into picture. That is arbback. What is arbback? Arbback stands for role based access control. The term arbback is not limited to Kubernetes. It's basically a security term where it is applied to different tools and different concepts. But in

Kubernetes, arbback is applied using three resources. If you know these three resources and how to use them, you can master arbback concept in Kubernetes. What are these three resources? First one is service account. Short hand is essay. Second one is ro. Third one is role binding. That's it. You just need to understand what are these resources. Let me explain you one by one. First thing is service account. Service account is just like the user account. What is a user account? For example, if I want to log into my

laptop, I need a user account. If I want to perform any action within my laptop, I have to login using a user account. It can be administrator, it can be Abishek, it can be Vala. Only then I'll be able to do something if I have permissions. But the first thing that I need is a user account. In the same way in Kubernetes, every pod should run with a service account, right? A pod in Kubernetes cannot run without a service account. But Abishek in the previous example, I created a pod, right? Using cubectl

create deployment. When I do cubectl get pods hyphen in payments name space I can see a pod is running. I did not define service account. Very simple. If you try to edit this pod and see cubectl edit pod name of the pod right and hyphen n payments name space. You will notice although you did not provide a service account, Kubernetes assigns default service account. This line here you can see if you don't assign a pod with a service account, Kubernetes automatically defines the default service account. But this is not a good

practice and this is not what you should do in real time. Every time you create a pod, you should assign the pod with a service account and it should only run with it. But service account is just an account by default. It will not have any permissions. Just like I can create a user account on my laptop and I can share it with my wife or I can share it with my friends. But if I don't give any permissions to them, they can log into my laptop but they cannot do anything. The same is with service account. Now

what grants service account some permissions? Basically that is roles in Kubernetes. What are roles? As the name suggests role is nothing but you define what a service account or user account can do. So Kubernetes administrators separately they create some roles. A role can be developer or role can be test user right or a role can be test pod or maybe test pod payments whatever you would like to define. The name of the role is anything but here you will define the permissions read write config map read write secrets read write

CRDs. So you will define whatever permissions that is required. But Abishek how do we bind a role with a service account? Okay, I created different roles. I'm an administrator. I created a role called developer. I created a role called test user. I created a role called testport service account. How do I bind the role to the service account? That is where role binding comes into picture. All that role binding does it will bind a role to a service service account. That means administrators create service

accounts, administrators create roles and they bind a service account to a role using role binding. Right? Role binding does not have any other use case in specific. It will only map a role to a service account. Now let's see this thing in action as well. If you are confused when we perform the demonstration things will be much easy for you. So we will go back to our same Kubernetes cluster and let's create a service account first. So how do we do that? We can run this command cubectl create service account

followed by name of the service account. Let's call payments user. Okay. And hyphen n payments name space. Perfect. Payments hyphen ns. Perfect. So payment service account is created. Now let's grant or let's create a role and let's grant permissions to get and list other pods. That's it. Okay. How do we do that? We will run this command cubectl apply - f and we will use this manifest kind is role metadata name of the role name space in our case it has to be payments ns right followed by the rules that we want

to grant this role. So the verbs decide what permissions will this role have on what resources. So the permissions that we are granting get and list on the pods. Now whichever user account or service account is assigned with this role they will be able to get that is they will be able to run cubectl get they will be able to run cubectl to list out all the pods within the cluster. If I change this pods to config map, they will be able to run cubectl get config maps and they will be able to list all the config maps only within the

payments name space. Again, if they try to run cubectl get on a different nameace even for the ports, their execution will be failed because this role here is only restricted to the payments name space. Let's create this as well. Okay, we created service account separately. We created RO separately. Now we have to bind this service account to the role. How do you do that? We will run this command cubectl apply f followed by the file that has rolebinding configuration. The role binding configuration as you

must have guessed it has the name of the service account and it has the name of the role. What is the name of the role? Pod reader the one that we defined. Let me just correct the name spaces here. The name space here should be payments ns. Similarly payments ns. That's it. We are good enough. Now let's do this. Okay. So now service account is created. Ro is created. Ro binding is also created. Let's verify if the service account called payments user can get the ports within the payments hyphen ms name

space which should be successful because we granted the access. This is the command to verify the permissions. cubectl o can I list pods as payments user service account in the payments hyphen ms name space what should be the output it should be yes there you go now if I try to see okay instead of list if I change this to cubectl o can I create pods the out should be no similarly If I run cubectl o, can I delete ports? Again, the out should be no. Even if I do cubectl, can I get parts within the

payments name space? You would see yes. But if I change the name space, okay, from payments name space, if I just do something like score name space, you would see the output again as no. Why? We created the role but the role is restricted only to the payments name space. Using this payments user, if you try to perform actions in a different name space, your execution will fail. But Abishek what if I want to you know grant my pod in this case if I want to grant payments user to read pods across

all the name spaces in the cluster should I create multiple roles for it will take a lot of time you don't have to do it Kubernetes also supports something called cluster role so in that case you can create a cluster role and cluster role binding Okay, basically ro and cluster role are almost the same but role is limited to a name space. Whatever permissions that you want to give in the role they will be limited to a particular name space. If you want to grant permissions across the cluster

across all the name spaces in the cluster you would go with the cluster role. That's the only difference. Perfect. So this is how arbback works in kubernetes and you should definitely implement arbback. This is one of the most important things that you have to do whenever you create a kubernetes cluster. Create name spaces and create rback for the service accounts that you use. Without this your kubernetes cluster will be a security mess. Anyone will be able to delete other pods. anyone will be able to

create config maps, read config maps, read secrets which is a security issue for your organization. Okay, now that you learned about name spaces and arbback, let's go a step ahead. Let's learn about a very interesting concept that is network policies. I'll explain this to you in very simple words. Imagine you created a Kubernetes cluster. Whenever you create a cluster, the Kubernetes cluster is set up with internal cluster network. That is all the pods in the Kubernetes cluster can by default talk to each other.

This pod can be in the payments name space. This pod can be in the scores name space because they share the internal cluster network. They can talk to each other using curl or maybe the payments part can make an API request to the pod in the scores name space. Let me show you as well. Now if I just do cubectl get pods - a that is pods in all the name spaces followed by hyphen o wide. You will notice this is the pod in the payments name space. This is the pod in the scores namespace. They share the

same C block. This is 10.244.0.6 10.244.0.7. they are in the same network. So now this payments name space pod can ping the scores pod or maybe make an API request to it which is a huge security issue. Maybe not for every company but for certain companies it is. Why? Let's take the classic example. There is a front end part in the login name space. There is a backend pod in the let's say score name space and a database in the DB name space. Now backend talking to the database it's absolutely fine. It's a very common

scenario but front end talking to the database should definitely be blocked. Otherwise, any potential hacker or a bad actor who has access to the front end can penetrate and access to the database. Or if your front end pod is compromised for any reason through the front end pod, the hacker can penetrate and access the DB pod. maybe make a curl request to it or gain access to the database and steal information from the database. How do you prevent this? As I told you, front end and DB or any other part, they share

the same cluster network. What you need in this case ideally is something like firewall, right? Something that protects your DB part. But firewall becomes complicated. What Kubernetes does? Kubernetes supports even a better solution called network policies. How does network policies work? So you can define access to your pods. In this case, you can define access to your DB. That is who can ingress to your DB. Which pods should only have access to your DB? You can even control eress access. If you try to make request from your DB

pod, the request should be blocked. Or if someone tries to make request from the front end part anything someone got access to the front end port they try to run a curl command they try to run the ping command you can block eress access or outgoing network access from the front end part you can do all of this using kubernetus network policies of course I made a very detailed video on the network policies but still because we are talking about security I will show you this through a very useful demonstration.

So what we will learn how to set up network policies unfortunately when you're using kind cluster by default kind is not capable of network policies. So we will install calico in the cluster. Calico is basically a kubernetes network interface that helps us set up network policies. And then we will take the scenario of almost the same thing a front end pod, attacker pod and backend part. So our goal is very simple. Attacker pod should not be able to make even ping request to the back end port. But if front end tries to

connect to the back end through a ping or something, this request should go through. Let's try to see this one more time. We will create three different pods. Front end, attacker pod and the back end pod. Front end pod should be able to make request to the back end pod. But if we try to even make a ping request from attacker pod, it should be blocked although they are in the same network. Firstly, I'll show you without network policy. Right? So, we will install a Kubernetes cluster that we

already have. We will set up Calico in the cluster. As the next step, we will deploy three pods. I'll show you first without network policy that attacker pod can talk to backend pod. Then I'll install the network policy to show you how to block the access. Perfect. We already have the Kubernetes cluster. I will create something called the payments name space cubectl create ns payments the documentation has commands related to the payments name space that's why instead of modifying every time to payments ns I just created

the payments name space it will be also easy for you you can run the same commands from the docs first let's create the front end port we will use this command here cubectl run front end image busy box so I have taken a very simple busy box image we are running the front end part I attached labels to the front end part ro front end why are you attaching labels because when it comes to the network policies you block the access only using labels or annotations. I will use labels here to block the

access in the future. That's why when I'm creating the pod, I'm creating the pod along with the label. So front end pod has the label called ro front end. Okay. So this should create front end pod for us. Now let's create attacker pod. So this is attacker pod cubectl run attacker hyphen image busy box hyphen n payments. If you notice here I did not add any label to the attacker pod. It's just like any other usual pod. Then I will also create the backend pod. cubectl run back end

image engineext and I have added label to the back end pod as well. Very simple. I added label to the front end pod and back end port. I did not add any label to the attacker pod because network policies work with the labels. Now let's expose the back end port that is I'll just create a service for the back end port so that we can easily make curl request to the service. Perfect. So we created a service for the back end pod as well. Now we will just verify all the pods are created and running. cubectl get pods n payments.

There you go. All the pods are running. cubectl get svc - n payments. So you can see the backend service. The reason for creating service if I use the cluster IP using the pods a pod can go down for some reason during the demo. If the pod goes down our curl request might fail that's why to use a stable IP address or maybe to use estable DNS I created service otherwise you can also use the backend pod IP. There is absolutely no problem. Step one, I'll connect to the attacker pod cubectl xc

it attacker pod hyphen n payments. I'm taking shell to it. Now firstly I'll show you we did not create anything in the sense we did not create any network policies. I'll just run this command wget qo backend svc from attacker part. I'm trying to connect to the back end pod. Let's see what happens. Request went through. Welcome to enginex. That means attacker got access to the back end pod. Of course, even front end pod will be able to connect to the back end pod. Now let's see network policies in action. So

firstly I will install Calico on this cluster. So this is a command to install Calico. It's also in our documentation. If you're using EKS cluster on AWS, you would not need Calico even because your EKS cluster will be set up with the VPC CNI and VPC CNI already has support for the network policies. If you're only using kind local Kubernetes cluster, you would need this step. Okay, all the CDs are installed. The Calico CNA is set up. Now I'll install network policy. So I'll run this command

cubectl apply f followed by the network policy manifest. Let me walk you through the manifest. So what are we doing here in the network policy? So we are saying allow only specific traffic. This is just the name of the network policy. You can grant anything. Here we are defining the pod selector. What is the pod selector? I am saying this network policy should only be applied to the pod which has the label app my app. If you remember we use this label on the back end pod. I can quickly show you as well. cubectl

get pods n payments. So this will list all the pods for us. If I just run cubectl edit pod back end n payments you will notice backend pod only has this label called app my app. So that means the network policy that we are creating will be associated with the backend part. Okay. So we defined which part should the network policy be associated with. Then policy type which policy type ingress is the policy type that is inward traffic to the back end port should only be allowed from the front end pod. The pod

that is assigned with the role front end. Any other pod which tries to access the back end pod ingress should be blocked. that is the incoming traffic should be blocked. Let's see if this works out. There you go. Network policy is created. We'll replicate the same steps. I'll again x to the back end pod using the same command cubectl xc sorry the attacker pod cubectl xc attacker pod. Now let's run the same command. You will notice the access is blocked. There is no response back. Even if I try

to do ping followed by back and svc, the ping is also not going through the ping is also blocked. On the contrary, if I try to run the same command now, I'll switch this to the front end pod. Okay, hyphen xt front end. I'll run this command wget. you will see the response. There you go. This is how network policies work in real time. Right? So we understood the concept theoretically and practically. If you want to block access to certain parts or if you want to define eress access from the pod, what

you need is network policies. Best use case is database ports or the backend pods. But Abishek, what if you know on top of the usual network policies, I want to define strict policies within our company, right? Our company has very strict policies. For example, anyone who deploys a pod on the Kubernetes cluster or anyone who deploys deployment on the Kubernetes cluster should not use the latest tag. I'm sure you know the consequences of using latest tag. Otherwise, let me explain you. Let's say you deploy MySQL pod with the

latest tag. Every day this tax keeps changing, right? So, you have deployed MySQL with the latest T. Today this might be working but tomorrow the developers of the MySQL maybe at 12:00 a.m. in the morning they might update the latest tag and now the latest tag is pointing to different version of the application right that is what the purpose of the latest tag latest is always up to date today the MySQL version might be 16 tomorrow the version is 17 but you are not using the version number as 16 or 17

but what you're using is the latest tag that means yesterday latest was pointing to version 16 of your application. Today the latest tag is pointing to version 17 of your application. This is a major problem in production because yesterday your application might work perfectly fine in the production. Today it might break because of the breaking changes in the 17 version. Of course applications like MySQL most of the times they are stable but take example of your internal applications they can break with the new versions

maybe because of lack of testing or maybe because of lack of verifying certain features in the application. So using latest tag is a very bad practice and your organization has this as a compliance requirement. Even if a developer accidentally tries to use the latest tag, the deployment should fail. How do you do this? How do you enforce policy within the cluster? This can be achieved using tools like Kerno. So, Kerno helps you enforce policies using admission control. Now, admission control is a very big concept in very

simple words. We have complete video on admission control and even as a service mesh using admission control. But because we need to know the concept at a high level at this point, let me tell you admission control is nothing but Kubernetes allows you to perform admission control that is for validation as well as mutation. So whenever you deploy a pod or you deploy a config map you deploy a service if you create an admission controller in the Kubernetes cluster you can validate the resources or you can mutate the

resources. Validation is nothing but you can verify. Okay. If developer creates a pod in Kubernetes using the validation admission controller, you can check if the pod if the image field in the pod is using latest tag. If it is using you can block the pod. You can stop creating the pod. In the same way, Kubernetes also supports mutation admission controller. If you create a mutation admission controller every time is a pod is created or every time a config map is created, maybe you can automatically add

certain labels to the pod or maybe you can add annotation to every part that is created in the cluster. So this can be achieved using admission controllers. Once again admission controllers are of two types. You can create validation admission controller. You can create mutation admission controller. But Abishek I don't know how to create these admission controllers because I don't know how to develop a controller in Kubernetes. That is where Kerno comes into picture. Kerno says you don't have

to learn this. just install me on the cluster and tell me what type of admission control that you need or what type of policy that you want to enforce. Do you want to enforce the latest image stack on the cluster? No problem. Just install me. Tell me that you are looking for a validation admission controller and you are looking for blocking parts with the latest image stack. I will do it for you. And it's very simple. Let me show you the steps how to install Kerno and how to perform policy enforcement.

Once again, these commands are in the documentation as well. First run this command cubectl apply followed by keyo install manifest. Now this will install a lot of CRDs for you. Most of these CRDs are related to admission control. Just verify the Kubernetes kerno I mean Kerno is installed on your cluster cubectl get pods hyphen and kerno. Now if you notice the pods are still in the running state. Wait for all the pods to be in the running state. Now you have to wait for this otherwise when you run the next commands you might

not see the action as complete. So I'll just do hyphen w so that I will wait for the pods to be running. There you go. It looks like the pod is initialized. In the next few seconds we should have pod in the running state. Perfect. Now the final step is to create the keyo resource. Now let's create a complete cluster policy. Okay. So this is a policy that I'm going to create disallow latest tag just a random name that I provided but what's important validation failure action and this is

enforce so we are using Kubernetes for validating web hook or validating admission controller and what does it actually validate? It is checking if any pod will be created with latest image tag. If it is created, it will fail the pod with the message image tag latest is not allowed. Let's create this first. Okay, the cluster wide policy is created. Now let's test kibbero. We will create a pod. Again, we will use the same payments name space. I will create a bad pod here. cubectl run bad

pod just the name of the pod. If you see I'm using hyphen image engineext latest in the payments name space. Ideally if what we learned is right key no should block this part. There you go. It says hey I will not allow the creation because what you're trying to do is image tag latest even before if I do cubectl get pods n payments you will notice the bad pod is not even created that means admission control is working even before your execution it just verified if you have access to the cluster Okay, you have access to the

cluster. Then you are trying to create a pod. Keyword came into picture or admission controller came into picture. It tried to run the validation web hook and it verified okay you cannot create enginex with the latest tag. You can create enginex with v_sub_1 v_sub_2 anything but you cannot do it with the latest tag. That's why I will block the access. And there you go. It blocked the access as well. It did not create engineix port with the latest tag. You can try something else. Okay. So let's say you

try to create engineix va. Now this might not exist. Your image might run into not valid image or image pull back off. But at least I want to show you in this case no will not block your request. There you go. Pod is created. Even if you try to do this action in the default name space, even there keyword will block your request because it's a cluster policy across the cluster. This action is blocked. This way you should make use of keyword in your organization. Now this has become very important in

the recent times because companies today have lot of compliance requirements not only latest tag you can say that a pod cannot be created without resource requests and limits. A pod should definitely have things like you know very simple things for example health check livess probe readiness probe. So you can define anything that is mandatory in your organization and block otherwise with Kerno. Perfect. Even I have complete video on Kerno. If you want to verify more actions, if you want to deep dive into Kerno, you can just go

through my Kerno deep dive video. But at least this information is enough if you are getting started with Kubernetes security. Now we learned about policies. Now let's move to a different concept. A very very simple concept. You know till now you learned about slightly complex topics like the arbback network policies admission control. Now let's go to a very simple concept that is secret management. Now we all deal with secrets on day-to-day basis. For example, we deal with passwords, we deal with API tokens, we deal with

certificates, we deal with temp files. How do you manage them on the Kubernetes cluster? Obviously, if you are using them on your virtual machine, if you're using them on your laptop, when you deploy your application to Kubernetes, you have to deal with these resources as well. How do you do that? This is where Kubernetes supports a resource called secrets resource. The same name, right? Your secrets are managed in Kubernetes using Kubernetes secrets resources. All that you have to do if you want to

store a password in Kubernetes. Maybe you deployed a pod and this pod needs database password. So your back end port wants to access the database part but for accessing the database part it needs the database password. You cannot hardcode that in your application. It's a very bad practice. So what you do along with the pod you create a secret resource and within this secret resource right which is bas basically a manifest. within this manifest you provide the DB username and the DB password. Now within the pod okay

once you create the secret resource within the pod manifest you read the secret as environment variable that's it now the pod has the DB username and DB password within its environment variable so it can access the information let me show you this in action as well so first of all we will create a DB secret so you will do using cubectl create secret whenever I'm using secret or whenever I'm using create my bad I can actually use a manifest as well but to make it simple I'm using the

CLA commands cubectl create secret generic secret the name of the secret is the db secret hyphen lit the username is admin password. Let's say the password is strong password 1 2 3 or password is abishek 1 2 3 anything. And let's create this in the payments name space. Okay. Now the secret is created. How can this secret be read by a pod in this name space? For that when you create the pod let's say this pod wants to read the secret. This is in the payments name space. All that it has to do it has to

provide the environment variable feed. So now this part will have an environment variable called DB user. From where does it get the DB user? It gets it from the secret reference name. Secret key name of the secret is DB secret. And within the DB secret the username is with the key username. Right. And similarly from where does it get the DB password? Value from get the value from a secret called DB secret. And within the DB secret use the field called as password. But Abishek when I do this will a secret

resource be created and will it have fields like username and password? the ones that I provided. Yes, you can also verify if you do cubectl edit secret followed by name of the secret db secret hyphen n payments you will see this is a secret manifest and within the secret manifest you have username as a field password as a field that's why when we created the pod here or when we are creating a pod here we are referencing the username and password field from the DB secret and that is using the values

from secret key reference. This is the same syntax that you have to use in any pod in any name space. The syntax will remain the same. You are reading the secret as an environment variable. The name of the secret is the DB secret and within the DB secret the username name is username and the password name is password. Perfect. Now let's see what happens. T name space is not found. Okay, that's expected. I'll change the name space here to payments. There you go. Secret is created. Now

let's look into the pod. I'll just exect the pod cubectl xc it name of the pod is secret demo n payments hyphen sh. Now if I just do env here you will find the username and the password. There you go. DB user is admin and you will also find the DB password. Somewhere here you should see the DB password. There you go. So this is the DB username. This is the DB password. Username is admin. Password is strong password 1 2 3. This is how secret should be created and secrets should be read within the ports. I'll repeat this

one more time. when you have secrets in your Kubernetes cluster or when you want one of the pod in the cluster to get some secrets like the DB secret or API token. First step create a secret resource in Kubernetes. When you create the secret resource within the secret resource provide the username, provide the password and then create the pod. within the part use environment variables. Let's say you want to use the reason you want to read the username read that within the username environment

variable. How do you do that? You will use this field called secret reference the one that we used here. Secret key reference name of the secret and field name in the secret. Similarly, if you want to read the password, provide the name of the secret and password reference in the secret. That's it. But Abishek, are secrets really secure? Because you know, end of the day, you are storing secrets within a file. Are these files encrypted? The answer is no. When you create secrets in Kubernetes,

they are not encrypted. They are only encoded in Kubernetes. That is they are encoded as B 64 values. Now this is a problem because your secrets are B 64 encoded. Anyone that has access to your nameace, they can use cubectl edit command. So if they have access to your namespace, if they have access to all the resources in your nameace, they can just run cubectl edit command and they can read the secrets which is a problem. And even okay Abishek I'll block access to the secrets you know I'll implement

arbback right by implementing arbback I will not allow anyone to perform cubectl edit on the secrets resource that's a good move in fact that is very good thing that you can do create arbback and block access to any secrets using cubectl edit but still what if You want to store secrets in Git, which is a valid thing, right? Just like how do you store your pod definition or how do you store your deployment manifest? How do you store your config manifest? In the same way, if you are implementing GitOps or if you are

implementing deployments to through git, how do you store secrets in git? Now, this is a big challenge. Kubernetes does not solve this inherently. What you need to do? You need something like ESO that is external secrets operator. But Abishek, what exactly is the problem? I still did not get it. Why can't I store my secrets in Git repository? Now, if you take the same secret here, right? You run cubectl edit secret. You copy this and you put this in a g repository. You think this is secure because abishek nobody can understand

what my password is. This is in some random string. Just copy this. Okay. Anyone from the g repository can copy this. They can run echo followed by b 64. They'll just run this command. B 64 hyphen decode and they can see your password strong password 1 2 3 this is what I'm talking about Kubernetes secrets are not secure by default they are only base 64 encoded so if you store them in the git any person who knows Kubernetes they know they're only B 64 encoded they'll copy the value and they

will run B 64-y decode how do you prevent So what you will do? You will install external secrets operator on your cluster. How does this work? So this works with three simple steps. First thing you will install secret providers like vault. What is vault? Vault is a safe place. Instead of storing your secrets anywhere, you will store your secrets in the vault. Vault can be hashior vault or any other implementation of the vault. You will install vault on the cluster where you'll start storing your secrets. It

can be your DB password. It can be your API tokens, anything. Step two, you will add vault as your secret store. That is you will tell external secrets operator hey vault is my secret store. All the secret values are in the vault. And finally you will create a manifest called external secret. What does this external secret have? It does not have the secret. It just has information of the vault reference. that is my secret is stored in the vault and it is stored with a particular reference name. So in the external secret you will

only have the reference name. Nobody who checks your manifest in the G knows what does this reference name mean. They can find the reference name. They don't know where is your vault. They don't know what is the real username and password in the vault. Only external secrets operator can understand this external secret manifest. It will read the reference because it knows your secret storage vault. By reading it, it will go to vault and it will fetch the real username and the password and it

will store those values in the Kubernetes cluster. Right? Very sophisticated approach but three simple steps. First thing install vault. Start storing your secrets within the vault. Add vault as your secret store to ESO. And then instead of storing your username and password in the Kubernetes secret directly, what you can simply do, you will create a manifest called external secret manifest where you will have the references of your vault. You will say my secret is stored in a particular path of the vault.

Only external secrets operator understands it. It looks at the reference. Okay, it knows this path should be searched in the secret store that is vault. It will search it in the vault and it will get the exact values and store the values in the Kubernetes cluster. So you can happily store your external secrets YAML file in the G repository. Now let's see this in action. When you see this in action, you will understand this better. First step, I'll simply start with installing external secrets. Right?

Installing external secrets. We will do it using helm. This is the repo for external secrets operator. Let's add the Helm repository. If you already have the Helm repository, in my case, I already have it. I'll just update the Helm repo. Now, let's run the Helm install command to install the external secrets operator. We added the repo. Now, let's run the Helm install command. There you go. The install is successful. Let's verify cubectdl get pods and external secrets. The container is still in the creating

state. Like I told you during Kerno as well, please wait while the pods are in the container creating status. They should be in the running status. Let's recheck. There you go. So the pods are in the running status. Now the second step, let's install Walt. Installing Wault on Kubernetes is very simple. You just need this manifest. We have this manifest in our uh g repository as well. Just run this manifest. What is it doing? It is creating vault name space and then it is just installing the vault 1.15 image.

Usually when you try to set up vault on a virtual machine, you might have to run set of commands. But on Kubernetes, we will just run a simple deployment manifest. and then expose the vault. If you just want to access the vault through the UI, if you don't want to access vault through UI, you can skip this step. In my case, I'll do everything from the CLI. So, I'll just stop the vault. But this was just to show you. You can also access it from your browser and perform the steps from the browser.

Cool. So, we installed ESO. We installed vault. Now the next step is to add vault as the secret store because we have to tell external secrets operator our secrets will be stored in the vault. How do you do that? First let's just exect to the vault pod the pod that we created. Okay here. How do you do that? Run this command cubectl apply f. Now I'm just trying to make it simple for you. Instead of now taking the manifest from official documentation, I try to put everything in our GitHub repository because these

manifests are not very important for you. You can get this from any sources, the official documentation, chat GPT. What's important for you is to know the concept. So this is the secret store manifest and here I'm configuring vault as the secret store. Wault is running on port 80200. I'm just providing the location of the vault part. Cool. So this should add vault as the secret store. So we are done with both of these steps. Now we will just store a secret within the vault. Okay. Because

we want to read the secret from vault in the external secrets. So first let's store a secret in the vault. How do you do that? We will exect to the vault part. Or instead of this, you can even access vault from the UI. Two things. If you are a CLA guy like me, do it from the CLA using W CLA command. If you want to do it from the browser, just use port forward and access the vault through the UI. Now you just need to export the vault IP address and provide the vault token as root. This is for connecting to the

vault instance. Now using this command that is vault KV we are creating a key value pair in the vault. Put secret called DB secret. The location of the secrets is secret/ payment/ DB. This is within the vault. This is a folder. And this is another folder where you are storing a key value pair called key is username and value is the password where the username sorry key is username, value is admin, key is password and value is super secret 1 2 3. Basically we are storing two key value pairs. This is one key value pair.

Username is the key. password value is the admin. My bad. Username is a password. Sorry, key is the password and value is secret superstar 1 2 3. Sorry for the confusion. This is a key. This is a value. This is a key. This is a value. We are creating two key value pairs here. Okay. Now, both the key value pairs are stored. Finally, let's try to create an external secret. How do we do that? Again, it's just a manifest in Kubernetes. Whatever you do, you only use the manifest. cubectl apply hyphen f followed by

external secrets manifest. One more time, why are we using this external secrets manifest? because you cannot directly store secrets in g. Now you can take this external secret and store it anywhere you want. Even if someone asks you, hey, I want to just know the DB username and password. I am the right person. Tell them take this external secret. If you are the right person, deploy this on your Kubernetes cluster. We already installed external secrets on the cluster. It will take the username and password and it will create

secret on the cluster. It will figure out. You don't need to get the username and the password. And as we learned in the external secrets, what are we doing? We are saying secret store reference is the vault. And within the vault, there is username within payments DB folder. There is password within the payments DB folder or the path. This is the path for the username and this is the path for the password where within world secret store. Cool. Let's also install this. Now let's verify. I haven't created any secret.

Right now if I do cubectl right cubectl get secret hyphen n payments you will see there is a db secret and what is the value of this db secret let's verify that cubectl edit db secret or secret/b secret hyphen n payments. There you go. So our username for the DB secret is provided and even password for the DB secret is provided. You don't have to do anything. External secrets will create this secret for you. Even if you try to delete this secret again external secrets will create the secret

for you. Now in the point instead of mentioning directly the username and password you can even provide a reference of this external secret. So this is how secret management is performed in real time. Lot of people stop their learning at the secrets which is also good in Kubernetes. Secrets can also help you store the sensitive data if you're using it with a strong arbback. Even after using the strong arbback, if you need to store your secret in a git repository, what you need is external secrets operator.

Perfect. So this is how good dev sec ops secure their Kubernetes cluster. We learned about six practices in the video. We started with understanding name spaces. Then we learned about arbback. Then we learned about network policies. Then we learned about Kivero, dynamic admission control. We learned about secrets and external secrets operator. I hope you found today's video informative. If you have any specific questions, do let me know in the comment section. Also, there are complete implementation videos

for most of these concepts. If you want to learn, go to our Kubernetes playlist. If you go to the channel, you will find multiple playlists. Go to the Kubernetes playlist, you will find most of the Kubernetes concepts there. See you all in the next video. Take care. Hello everyone. My name is Abhishek and welcome back to my channel. Today is episode 6 of Dev Sec 0 to her series and in this episode we will focus on SAS, SCA and Dash three pillars of application security and most importantly in today's video we

will learn these concepts practically and you don't have to worry after watching today's video if you want to practice the concepts you can head to my GitHub repository tree dev sec ops 0 to her. Head to the day six folder for today's video where you will find a readme file and within the readme file I've created everything that you need. So it covers the theory part and also it has all the demos that I'm going to perform in the video. Perfect. So without wasting any time let's start

with the video. First of all, what is application security and why is it important? To understand this better, let's look at a simple application development life cycle. Now, a very very basic application development life cycle. So there are three important phases. Number one is the dev phase where developers work on writing the source code of the application and they also test the application locally. Then comes the test phase where QA business they rerun the unit test cases just to verify the unit tests are

passing and they also run something called as regression testing or functional testing. Very simple. Let's take e-commerce application as example as we all use e-commerce on day-to-day basis. What happens in regression testing? The QA engineers verify if the complete user flow is working as expected and all the features of application are working together well or there are any issues. For example, a user is able to log in. User is able to look for the product, search a product, then add the product

to the catalog, perform the payment and get the application shipped there to their location. So this is what the Q engineers verify in the regression testing or functional testing. And finally if everything looks good Q engineers go the go ahead so that application is shipped to production. As I told you this is a very basic application development life cycle. Now on paper or at a very high level this actually looks good because application is tested locally by the developers. Then Q engineers also

performed unit testing. At their end they performed regression testing. They verified application features are working fine and only then application is moved to production. But if you see in this entire workflow security of the application is not taken into consideration. Now developer might have taken security into consideration while writing the application but who knows there is a possibility that developer might have overseen some use cases or developer might have ignored certain security considerations

in this entire application development life cycle. The security testing is not performed which is actually very important because now application might work well for the end users but a bad actor can actually attack the application and maybe gain the access to the database gain the access to the users information in the database and even maybe bring your application down in the production. what not because the security testing of the application is not performed. This is where application security or apps comes into picture and

appsc is one of the most important pieces of dev sec ops life cycle. So what a good dev sec ops engineer does along with the regular workflow a good dev sec ops engineer brings application security tools or concepts like sast sea and task into the organization. If the organization already has a CI/CD pipeline to the existing CI/CD pipeline a dev sec ops introduce these tools and concepts. But Abishek in our case our QI engineers also perform penetration testing. They perform different types of testings like performance testing as

well. But still that does not comes under security testing of the application. As QI engineers they might use some tools for penetration testing. They might use tools for performance testing but those tools are not designed for the security testing of the application. Now what is security testing? Okay, know I have been saying the application should be tested from security point of view. But what are some common use cases? I'll give you very very simple use cases so that you understand them. Well, as simple as what

if one of the developers hasn't taken care of SSL certificates? Well, you know, one of the developers while writing the application has used some insecure certificates, right? Or maybe disabled security verification of the SSL in the application code so that security is skipped and application can easily talk to the other applications without considering security. Or maybe developer who is working on application hardcoded some secrets. Now the peer who is reviewing another developer who is reviewing the code

might have overlooked into it and common cases like SQL injection. SQL injection is very difficult to detect at times. You know as a human if you are reviewing other developers code you might skip SQL injection you might not see it because hackers have their own ways of injecting things into your SQL queries and getting access to the databases. You definitely need some tools to verify if there is a possibility of SQL injection patterns. On top of that there can be some insecure libraries right very common thing maybe developer is

using flask and developer is using 1.2 version of the flask what if this version is vulnerable. Now this version already has some critical vulnerabilities reported on the internet and your developer did not know about it. Even somebody is reviewing the code, they might not know about it. Definitely QA engineers will not be aware of this vulnerability with a particular version because there will be thousands of packages that developers use in their code. Right? These are some cases, right? When we move forward in

the video, you will learn about other cases as well. But these are some common cases. And if you're working with JavaScript applications, cases like course right cases like CSRF again these things are very difficult to figure out in the manual code review process. That's why security testing is important. Now there are three important pieces. One is SAS, other iss how does these concepts help in the application security part. Let's go by one by one. First one, SAS. SAS is basically static application security

testing. That means in the SAS testing if you're using any tools for example sonar cube is a very popular tool in this space. So when dev sec ops engineers use sonar cube and when they connect their application to the sonar cube what sonar cube does it goes through the source code of the application and it looks at the source code to understand because it's a tool it also comes with some AI capabilities it has a database of known patterns with all of those things the tools like sonar cube can say if your

application is potentially exposable to SQL injection. It can say if there is any insecure SSL verification used in the application. If there is any issues with weak cryptography, right? Or if your developer is using any insecure modules, it can be even as simple as an insecure YAML module or maybe hard coding of secrets. All of this can be caught by the sonar cube. Now this is a very interesting space. Lot of times interviewers ask you okay you used sonar cube but what exactly are the common issues that you got in the

application using sonar cube or the common cases that sonar cube helped your organization with the reason why interviewers ask you this question interviewers feel you might have learned sonar cube from some cases but you haven't implemented sonar cube in real time now how do you tackle this situation Don't worry because this is a common case. I've documented popular sonar cube cases in our MD file. So these are 10 popular sonar cube use cases that are used in the dev sec op space. Hard coding secrets, SQL injection,

command execution risk, insecure D serialization, arbitrary code execution, you can go through them after watching the video. But what I'm trying to say here sonar cube or static application security testing is a way where a tool can help the dev sec ops inness go through the source code. It goes through the source code and it identifies any common patterns of insecure behavior in the application. Then comes SCA. Now what is SCA? Very simple. SCA stands for software composition analysis as the name suggests. What exactly it

does? It looks at dependencies of the application. Right? It looks at what direct dependencies is the application using and indirect dependencies that the application is using. Very simple. You might be using flask but flask internally might be using 100 dependencies. You might be using a particular version of the flask you feel you are using the right version but even because flask is using insecure dependency your application will be indirectly exposed to the thread. So you should not only look at direct dependencies but you

should also see if any of your indirect dependency is potentially exposable to the security threat. But Abishek this is very difficult because I'm using a Python application. In the Python application I'm already using thousand dependencies. That means these thousand dependencies each of them might be using 100 dependencies internally. So it becomes 1,000 * 100. So technically 100,000 dependencies in my application. I'm just making up this number. Usually it's not this huge. It might come around

1,000 to 2,000 dependencies. But yes, you have to ensure that direct dependencies and indirect dependencies are secure for your application. You cannot do it as a human. It's not possible. This is where SCA comes into picture. Software composition analysis where every programming language has some tools. For example, in today's video, we will look at a Python application which I intentionally I'm using old versions or deprecated versions in the application and we will use a Python utility that can help us

that will show us the dependency issues in the application. Similarly, Golang, Java, every programming language has their own set of tools. Abishek can't sonar cube do it can do it to some extent but it is always good if you rely on the SCA tools rather than relying on SAS tools for dependency tracking perfect finally dash very very important it's actually simple concept but it's important what is dash dynamic application security testing It actually does similar thing like sash but only thing is that dash is about

runtime. In sash a tool goes through the source code of your application and it looks at the common vulnerability patterns. But when it comes to dash dash tools they don't look at source code of your application. In fact, they don't need any source code of your application. What they do? They just need URL of your running application and they start performing different testing on the running application. So they make thousands of API calls to your application to verify if there are any known security issues with your

application. Why is dash very important? Because this is how hackers also work. What hackers usually do? Let's say you have an application and your application is running on the internet. Now hackers do not have your application source code. They just take a browser and they start hitting your application for different patterns. If your application is an e-commerce application, they'll try from logging into the application, they'll go to one product, they go to the other product, they try to make API

calls to your catalog service. They are not sure right which part of your application is vulnerable they are not sure because they don't have the source code but they figure out that by making thousands of API calls and they make those API calls on hundreds of websites even if one website one flow of the one website is hackable they their job is done they figured out one website and it's good for them they can get ransom from those websites The same way dash tools also work. You can deploy dash tool in your

organization. If your organization has three different websites, maybe internal, external, another external, give your dash tool, chest the URL of all these tools, all these websites I mean, and it will make 100 thousands of API calls and it will figure out okay in this application there are 10 known patterns for the hackers. In this there are five known patterns. In this there are four known patterns. It's up to you. Do you want to fix it or not? But my job is to report the issues to you. And as I

told you in today's video, the Dash tutorial is going to be very interesting. I have a demo e-commerce application, a cool one which is almost like a real e-commerce application. It has all the flows including login, including payments, everything. So we will use that application and we will use a dash tool to show you how the dash tool can figure out the patterns. Perfect. I'll repeat one more time before I go to the demos very shortly. Right? What is SAS? So we learned there are three pillars of

application security testing. SAS, SCA and DAST. What is SAS? SAS is nothing but software application sorry static application software testing that is a SAS tool just goes through source code of your application. It identifies all the known patterns in your application. This is basically part of static code analysis. SAS is a sophisticated area or a particular area of static code analysis where tools perform static testing on the apps and popular tool in this space is sonar cube. Then what is SCA? SCA is basically

software composition analysis. In short, you can just call it as dependency checkers because the tools in the space of SCA, they go through direct dependencies of your application, indirect dependencies of the application and they verify if any particular version of your app, direct or indirect is known to the hackers, I mean is potentially vulnerable in terms of CVS. Finally, dust. What is dust? Dynamic application security testing. When it comes to dust, this is performed during the runtime. So, you can just

give the tool. In our case, we will use OASP zap a very popular open-source free tool. Usually, when it comes to dash, most of the tools are paid tools. But we will go with OASPZ zap a very popular opensource and free tool. And using this tool you can just provide URL and it starts sending thousands of API calls to identify any insecure patterns in your application like the similar ones. Whatever we learned in SAS almost the same things. It tells you if there is any course related issue in your

application. If there is any CSRF issue in your application or if you're using any insecure cryptographic modules or insecure SSL almost the same things but here it tells you through the runtime environment perfect now that this is clear let's start with SAS first so we will do the SAS demo now what we will do we will take a simple Python application Okay, we will install sonar cube through docker and we will just connect the Python application to the sonar cube and we will run sonar cla to connect it we will

run the sonar cla in the place where we have the application source code. So sonar cla will talk to the or will talk to the sonar cube and send the information of the application source code. Let's do that. So first of all we need the application. Don't worry we will clone our dev secops 0 to repository because here I already have the application app.py is our python application. What is it doing? It's just now a web application that is making API call to the database. That's it. Nothing more

than that. A simple web app that makes a query to the database. Now when you look at it, it actually looks good because u you know it returns user management service on the homepage and it is rightly connecting to the database. So to the naked eye you cannot see any issues. It's generating a random token and it is connecting to the database. Let's see what sonar cube tells. So for that I'll clone this repo on my terminal. I'll create I'll move to the test folder. I have a test folder

and I'll just remove everything here and clone our dev sec ops repository. Get clone followed by GitHub immala dev secop 0ero to her. Perfect. Now this will clone the repository for us. Let's move to the repo. We have different folders. Let's move to 06 folder. And here we have the app.py. We have requirements.txt txt to run the application and we have sonar projects.properties. But Abishek what is this? Once we install sonar cube we have to create this file and we have to provide the URL

of the sonar cube. Only then sonar cla understands where is the sonar cube application. First let's install sonar cube. So for that you need to have docker running. We will run sonar cube as a container. If you have docker running, just run this command. Docker run-d basically a detachable container running in the background. We are using the port 9,000 because sonar cube runs on the port 9,000 inside the container and we are exposing it 9,000 on our local host as well. If on your machine 9,000 is

already in use, just change the first 9,000 here because this represents the host port. You can just change it to 9,0001 9,0002 up to you. Name of the container. I'm calling it as sonar cube container. Again you can change it but do not change the image. This is important. We are using a sonar cube image 9.9 community version. That is we are using free version of the sonar cube. Amish. What is the difference between free version of the sonar cube and paid version of the sonar cube? Just like any tool with the free

version, you can track only certain patterns. End of the day, what Sonar Cube is doing, it is performing static testing. It will return only for certain patterns if you are going with the community addition. But if you're using enterprise version, then it's more powerful. It can go through your application more in detail, especially the source code, and it can recognize more patterns that are not part of the community edition. So for your practice use the community edition tomorrow if you're joining a company and if you want

to recommend a company the SAS tool recommend them Sonar Cube Enterprise. Perfect. Cool. Now let's verify if Sonar Cube is running. So we will take the local host followed by port 900. There you go. Sonar Cube is running for us. All systems are operational. Home. What is the username and password for the default version? I mean, if you just install Sonar Cube, the username is admin and password is also admin. See how simple it is for community edition. You just have to run a docker command and everything is done for you.

Okay, let's change the password. Old password is admin. new password. You can set up anything. Just make sure you remember it. There you go. Okay. Now we have Sonar Cube running here. The beauty of Sonar Cube is that you can connect your application on your local machine or you can integrate it with your CI/CD as well which we will see in the next video. In today's video, let's only learn these tools. Let's not focus on CI/CD. Anyways, so you can connect with any of your g distributions like GitHub,

Bitbucket, anything or you can just connect your application manually to sonar cube which we'll do in the video. But Abishek, how do I connect manually? If I want to connect through GitHub, I'll just provide my GitHub URL. I'll provide my GitHub API token. How do I do it manually? Of course, this is important because you will use something called as sonar cube cla. So using sonar cube cla you will connect to the sonar cube interface. To install sonar cube uh CLI you can just follow the steps in the official

sonar documentation. Reason why I'm referring you to Sonar Cube CLI documentation. I can directly share you the command but the versions keep changing. Let's say you are watching this video after 6 months. I'll put the commands in the GitHub documentation. You will try to follow it and it will break because as the versions keep changing the parameters might change. So that's why I'm asking you to go to the Sonar CLI the documentation the link is in our MD file the link to the website just scroll down

all you have to do here just scroll down you will find this hover click on the hover and you will find the download button that's it just download it rest all steps you can follow from my documentation okay so now once we are here that is once we are with the sonar cube cla once you downloaded prepare the sonar properties so wherever you have your application in our case we have application here app.py in the same location prepare this file sonar projects.properties properties and what you will put in this file you can put

the sonar cube URL either you can put it here or you can pass it through the CLA parameter in my case I'm just providing the values what is the name of my project this project name will be reflected in the sonar cube UI right now there are no projects here right if I click on projects there is nothing here once we run sonar cube then a project will be created with the name vulnerable Python application and I'm saying sonar cube that my programming language is Python even if you don't say that it is intelligent

enough to detect it but it's a good practice to do it now I already have sonar cla on my machine so I'll run this command sonar scanner right sonar scanner is the CLI hyphen d sonar host There is my sonar host running on local host port 9,000. So I pass that. This is what I was talking about. You can even pass this in the sonar properties. Finally I'm saying this is my sonar login token. Abishek. What is the purpose of token? Without that your sonar CLI cannot connect to the sonar UI. Who knows you might be running this

command. Someone else might be running this command. So for CLI to authenticate to the UI, you need the Solar API token where will you get it? Simple. Just head to your settings here. Head to my account. Here you will find security. Click on the security and just create a token. I'll call this as test token, global analysis token and generate it in real time. This is generated by the admins. Perfect. Now click on the enter and see the magic. Sonar cube is reading your Python application and it will

detect any issues with your application. Okay. It says uh unknown key sonar. Key. Very simple. Actually this should be sonar project.properties not sonar projects.properties. I'll just change this from sonar projects.properties to sonar project.properties. And let's rerun the sonar cla. I think I have to change this in the GitHub repository as well. I'll change it. There you go. Now you can see analysis is successful and analysis is pushed to the sonar UI. Let's see if I click on

the projects. There you go. So here vulnerable Python application is verified by SonarQ. And what does it say? It says there are no bugs, there are no vulnerabilities. But in the hotspots reviewed, it is in the E status. That means it found out some issues with our application. There are two security hotspots. So it is saying you don't have any critical vulnerabilities in your application but there are two security hotspots which you should take a look at. When we looked at the application we

did not find that like looking at app.py everything load good but sonar cube said no there are two issues with your application. Let's see what is that the first one. So it says there is cross-sight request forgery which is CSRF. If you're into application development you understand it. If you're not into application development you know I don't want to complicate the concept because CSRF itself is a very huge concept. Just understand that within this application there is a risk that is

figured out by sonar cube. Now sonar cube does not only tell you the issue but it also says what is the risk and to what extent you have the risk. Sometimes because these are tools they just look at the source code and they think your application might have the issue but sonar cube is a step ahead. What it says, you are only exposed to this issue if you are using a web application that uses cookies to authenticate the users. In our case, we are not doing anything like that. It's a very simple Python

application and it says you know if you're performing any sensitive web operations if your application supports HTTP post request or HTTP delete request only there your application is exposed to this security hotspot. Now in my case it is not. So what I can do I can go back to the security hotspot. Now I can click on this and I can in fact say this is not issue in my case right. So I'll go here in the review I'll change the status to safe. Scroll down I'll mark the status continue review.

Now you will notice if you go back to the security hotspot you will only find one issue. 50% is reviewed and fixed. I'm just trying to show you how the issues are addressed within the organization. It's not that sonar cube identified an issue and okay you will just say it identified the issue then we'll just keep it in the sonar cube. No in real time these issues are taken into Jira these issues are worked by the developers and once developers are confident they come back here they mark

the issues as done. Then there is another issue with the application. Another security hotspot. It says you used random.random to generate a random token which can be issue in real time. So what Sonar cube is saying? You are generating a random token. But this is not a secure package. Hackers can easily figure out what is your random token. So Sonar Cube recommends go ahead and change it. Again developers work on it and if they feel it is secure. If they feel they changed it then they rerun the sonar cube

analysis. But for some reason if they feel it is already safe they mark it as safe and they close this issue as well. Perfect. So this is static analysis security testing. Okay. So now what did we learn? We learned for SAS one of the popular tools is Sonar Cube. We learned how to run Sonar Cube. We learned it through Docker. But Abishek in real time also can I run it using Docker? Yeah, definitely. If you plan to use Community Edition in your organization, just spin up a virtual machine for Sonar Cube. Run Sonar Cube

as a Docker container. There is no problem in it. just that you have to connect it with a database so that you maintain the state of sonarcode or you can run it as a Kubernetes pod as well and you make it stateful application but in most of the cases these days organizations go with enterprise sonar cube version so they don't have to install sonar cube they don't have to set up sonar cube they'll just get the enterprise sonar cube they will run the sonar scanner where they will provide

the URL of the enterprise sonar cube which is a SAS offering that is provided by Sonar Cube company themselves. Perfect. Now let's look at SCI. Right. What is SEA? As we learned SEA is nothing but the composition analysis, software composition analysis. Let's do that on the same application. We are using a Python application, right? So for this P python application there are two dependencies. If I add the requirements txt in this case developer said I'm using flask and I'm using the request module

and these are the versions I got from the internet. I'm using these versions. Now as a deop devs secops engineer what you can do you can perform SC on this application. How? First of all, create a virtual environment. Python 3 - mpip venv dot venv. That is create a virtual environment. Okay. Oh, my bad. Python 3 - m venv. Venv. Next. Once the virtual environment is created, source the virtual environment. Source. VB pin activate. Perfect. Now instead of directly installing the application which you

usually do as DevOps inner this time install something called pip install pip audit. Now for every programming language as I told you there are different utilities. For Python which you most likely work with the utility is pip audit. Now using pip audit because we installed we can just run pip audit r requirements.txt. See the magic pip audit not only goes through those two dependencies but it will also go through indirect dependencies and it will tell you hey these are the vulnerabilities in your

dependencies. It also gave you the CV numbers which is very good. Now giving the CV numbers it is very easy for you. Now you can create a zera ticket saying that these are the packages that are vulnerable and if they ask you why are they vulnerable just you can tell them as devops I'm not sure you can go to this CVA and read about it because you are using an outdated version this is a CVA go through it read it and you will understand what developers do they just copy the CV number they go to the internet and they

paste it on the Usually there are many sources like there is national vulnerability database that is you know very trusted one the NVD or they can go to redhead vulnerability database or the popular ones. Personally I look at NVD most of the times because it provides up-to-date information. So developers go through they read it. Oh okay this is why there is issue in my application. Let me go ahead and fix it. Cool. So, we learned about SEI as well. Now, let's move to the final one. SEI is very easy. That's why I'm not repeating

again. You just have to install a utility and you just have to run one command. Now, let's move to dash. What is dash? So we learned that for dash we have to run the application and then we will use a tool called OASP zap right. If you remember we used OASP in episode 1 as well where we learned about threat modeling. There we learned OASP threat module right but here we used OAS thread dragon there for modeling threat modeling. Here we are using something called as oas zap. These are just tools. Installing them is

not a big deal. What you need to know is which tool to use in the right place. We need an application for this right to verify dash. We have to do it at runtime. But which application are we going to use? The better the application better is performance of dash. only then you'll be able to explain the interviewer or even within your organization if you have to do a proof of concept for dash you need a very good application only then there will be more vulnerabilities in the application where

will you get that one so here let me introduce you to something called as oas juice shop so basically the develop one of the opensource developers they built this juice shop application It is basically a e-commerce juice selling platform and intentionally they wrote this application with a lot of vulnerabilities. Now when you run this application it creates an e-commerce platform. Let me show you. We should have it somewhere here. I'll open the application and just show you how the application looks like. So

this is how the application looks like. So you can find different juices just like how you find different products on an e-commerce platform. You can find different juices. You can read about a particular juice. You can read about customer reviews and in fact you can place orders as well. But for that you have to first log into the platform. Again just like a real e-commerce application. I'll just log into the platform. And once you log in you can select a product. Now that goes to your basket.

You can add multiple numbers of the same product. Click on the checkout. You can provide the address. In fact, after providing the address, you can also head to the checkout and provide your bank account details. Of course, you'll provide dummy details. And you can head with the shipment part as well. just to simulate a real world e-commerce platform. They developed this application. Good thing is this application is open source. So everyone started using this application for their demo purpose. Even I have used

this application as a demo application in lot of presentations. Anyways, now we have an application. So we can use this application. Good thing is that this application is made intentionally vulnerable. Now what we will do? We will install OS zap right. We will install oz on our local machine. We will run this application on our local host and connect ozap with this application and rest the zap will take care. It will make API calls to this app and it will generate an HTML report for us. So we can go through the HTML report and we

can verify. In fact, what it gave is right or wrong. Let's do that. First I'll run this application locally. How do I do that? I'll first stop the container of this application. I already had it running. Let me just stop the container. So for that I'll do all the containers. I've removed them. Now this is the docker command to run the container the juice shop container. Docker run - d. I'm running it in the background. The name of the container is juiceop. I'm running it on port 3000.

And again I'm mapping it to 3000 on my local host. You can find this command from our MD file. Okay. Now on port 3000 I have the juice shop running. Next we need the zap container. For the zap container also just run this docker command. Just like how we installed sonar cube using a docker command. We can install web zap also through a docker container. Cool. Because this is open source we can do it. Okay. Okay, there seems to be a typo here. Let me just fix it. So, if you notice the command, I'm asking it to

create an HTML file. Zapreport.html. And if you observe carefully, I'm passing my application URL as well. So, I'm saying zap my application is running on port 3000 and it is running as a docker container. That's why I give host.docer.in internal 3000 port. Abishek. Is there a way where I can actually run OAS zap through the user interface? Because when I perform a proof of concept, it will be better if my show if I show my organization through the user interface. Yeah, we will do that as well. For now, let's run

Zap as a container. Let's get the HTML file because this is the easiest way. Then in the next step I'll show you how you can run this through the user interface as well and how you can connect your application with the zap container is installed. Now it is actually using the automation framework. What does that mean? It started making API requests to our juice application. It started making hundreds thousands of API requests. So it will take time and you can see here in fact see it made

some API request that's why it said access a confidential document. So something is happening behind what we see zap is making API requests to our juice application. In a moment we should have this completed typically it takes 2 to 3 minutes. There you go. And it figured out bunch of issues as well. See this is a warning. This is a warning. How can we read it? We can read it through the HTML file that is generated. Now here we should have zap report HTML document. You can read it from your browser as

well. Just do pwd. So this is the location. I'll copy this. I can access it from my browser. file colon triple slash followed by the path followed by zap reports html. There you go. Tada. So here we have the report and spend some time reading it. I can go through some of them but if you want to read it carefully there are mediumsiz vulnerabilities, there are critical vulnerabilities and there are low vulnerabilities as well. Let's see what are the media vulnerabilities. I'll go through one of them for you. So it

says cross domain misconfiguration. So there is course issue on the application. And here it says CSP header not set. This is one vulnerability on the app. This is another vulnerability on the app. And in fact it is also providing us the location. For example, it says within your Jewish shop application, there are node modules and there is express node module. Within the express node module, there is an issue here. It says dangerous Java function is being used. I mean a JavaScript function. Dangerous

Java JavaScript version that means a vulnerable JavaScript function is being used. And it also tells you exact function in the code. If you don't trust OS zap, you can actually verify it. Copy this function. Go to the source code. This is the source code. Take this in an editor. So I clicked on dot. It opened web editor for me. I'm not cloning the repo again. And here in the search button, I pasted bypass security trust HTML which zap report shared. And you can see in different places in fact this particular

function is being used. Look how cool it is. It does not have your source code but still from the runtime application it detected that these are vulnerabilities in your application and it also gave you exact line number. It gave you the function name and it also gave you the line numbers. So that is why dash is very very powerful. You can give you you can give dash tools any number of websites in your organization and let it run. Let it run in the background. It will do the job for you. Especially ozap is also open

source. There is no limit. It will make hundred thousands of API calls and after 20 minutes 30 minutes you will get detailed HTML report. Share it with your management. Share it with your developers and let them figure out how to fix the issues. Now, okay, we learned this through CLA. Let's do the exact same thing through the UI. Okay, so what we'll do now, we will run OSAB through the user interface. Again, very simple. Just run this docker command. This command is also in our uh MD file. Docker run- it.

I'm not really sure from where these lines are coming up. Let me just fix it. So this time we are just running the zap proxy so that we can connect from the user interface. There you go. Now it says proxy is running. You can access it on port 8080. Let's do that. Local host port 8080 followed by / zap. starting the application. So now we can access it from the browser as well. But this will take some time. For the first time just say no, I don't want to purchase the session and start.

You can just expand this utility. Manage ads. Close it. Expand this utility because this is the one we are going to work with. Okay. And this is actually slow. Uh that's why I initially showed you with CLI. Now what you have to do here click on automated scan and here provide the URL. What URL will you provide? Provide the same URL that is post.docer.com followed by colon 3000 which we gave in the CLA as well. http I'll paste it here for you. http host.docer.in internal colon 3000 and click on the attack

if modern with Firefox. I'm using the Firefox browser and I clicked on the attack button. There you go. You can see here it started making the HTTP request. In fact, here it is showing you the HTTP request as well. It made get request on the juice shop build routes. uh and it is making several other HTTP requests and just like the CLA this one also will take time for you and once you have this done you can get the report from the user interface and you can verify the report one thing the report

will be exactly the same because whether you do it from the UI or CLI it does not change this is cool for your proof of concept maybe better than CLI but when you do it through the CI CD pipelines definitely C cla part is better when we integrate this entire workflow with GitHub actions or bit bucket all that we have to do is provide the docker cla command and osab will be executed but if you do from the user interface it is difficult to integrate in the cicd perfect so this is all we have for today's video this will be done in next

10 to 15 minutes in the meanwhile you can go through the revision material in our GitHub repository. So we learned about SAS, we learned about SCA and we learned about dashed. SAS is for static testing, SCA is basically for dependency check and dash is for testing your application in runtime. If you have any specific questions do let me know in the comment section and please share this video with every dev sec ops enthusiast that you know and trust me this is very very cool lot of people might not be aware of

oasap and running opensource dash tool. So if you're interested you can publish this to your LinkedIn you can share it with your network and help others also learn. See you all in the next video. Take care. Hello everyone, my name is Abishek and welcome back to my channel. In today's video, let's learn end to end dev sec ops implementation. So typically you will learn what exactly a dev sec ops engineer does within an organization or if you join an organization as a dev sec ops engineer,

what will be your roles and responsibilities? How would you take an application to a production ready state? That is what we will cover in today's video. Now obviously when you want to learn something like this, you need a demo application. But you need a demo application that is almost like a realtime application. You need front end, back end, database. Where will you get such application? So don't worry, we will use this demo application in today's video. I created this application and I have the complete

source code for the application which is in a public repository. But before that let me first walk you through the application. So this is just like any blogging application that you would use on the internet. It can be like medium or you know you can consider this as a lightweight blogging application where you know this is the front end. Whatever you are seeing on the screen is basically the front end of the application and within the front end you have an option to create a new post and there is a vibe for the post. For

example, let's say you want to post something that is trending on the internet. So you can pick up the vibe accordingly. You can just give a title for it. Let's say LLMs, you know, you want to write a very uh trending topic or you want to write something very hot which can get more views for you. So you can just say LLMs are overrated. Let's say this is the title author Abhishek and then you can just keep writing the content. For example, I'll just u you know to be decided because I'll work on the

content later. Let's say post it. So you can see you know the LLM are overrated is posted. If I go to the website back so now there are two posts here. If anybody's interested they can comment on the post. Let's say you make this blogging portal public which is what we are going to see as well. I'll show you how you can share it with your friends by the end of the video. So they can just come here, you know, they can also leave a comment. Uh they can share their opinion. This way you can use this as

your personal blogging website as well where every day you can post something and you can even share it on LinkedIn or anything that you do. So this becomes the front end which is the user interface. Where are these posts getting stored? Like whenever you are creating a post there is some content that is involved. There are some comments here. All of this will be stored in a database and what is the bridge between your front end and the database that's called as a back end. So what I'm trying to

tell you here we will be working on a three- tier architecture application. This is important because in real time also you will be working on three tier architecture applications. Let's say you join Amazon. Their e-commerce platform is a three- tier architecture application. Let's say you join Walmart. You join even a startup, you'll be working on three tier apps. If you are a beginner, I'll simplify this for you. So what is a three architecture application? There are three types here.

One is called front end which is a user interface that you see for any website. Even if you look at GitHub, whatever you are seeing here is a user interface which comes from the GitHub's user interface. Then you have a database. Database is mandatory because end of the day your data has to be stored. For example, I have created a repository here where I'll be writing some code. I'll be storing some code. So that has to be stored in a database. So database will serve as a storage and

there has to be a bridge between front end and the back end. Right? For example, if I'm storing something in the database, how does that get populated in the front end? So there is a layer between front end and back end which we usually call as back end. So this is one tire, this is second tire and this is the third tire. That's why we call this as three tire architecture applications. Some of you might already know this. You know, if you already know it, it's good. But if you're a beginner, this is what

we mean by three-ter apps. Sometimes you might also come across two architecture applications. Right? You might see some examples on YouTube on internet. When they say two architecture applications, that is there is only front end and back end. There can be some static applications which is also called wonder applications where there is only front end that is involved for example basic HTML pages right so you don't you don't want to bother about storing the data you just want to create a HTML page maybe you want to notify

something your you know basic personal information website or your portfolio website that can be a front end or a one application two tier where you have front end and back end when you want to make your portfolio website or when you want to make your front end dynamic you would go with two-tier architecture applications and three tier I just explained it to you perfect now where will you find the source code of this three architecture application okay Abishek you know you shared this application but where will I

get the source code so So I open sourced it. Of course you know writing such application takes a lot of time. I used LLMs. I used uh AI to get this application. And now the source code of this application is in this GitHub repository. In general when you join organization as a DevOps or dev sec ops inner your journey starts from this point. So the development team would already have a GitHub repository unless you are joining an absolute scratch team. Someone who is starting the development from absolute scratch.

You know, you would ideally get a GitHub repository like this where they have the source code of the application and they have steps to run the application locally and also some basic information about the application like in which programming language the application is written, what text stack is used to build the application, how to run the application locally, if there are any variables, what variables you need to export, all of that information is provided by the development team. So this is where you start your journey

typically as a devops or dev sec ops instar and what is the end goal? So the end goal know the end goal is huge in this case because we are talking about end to end dev sec ops implementation. You know you should first start with understanding how to run the application locally. Although the steps are sted by the development team, you have to try it by yourself. Only then you know if the steps are correct. Then creating the required infrastructure because eventually you want to host this application to production.

So you need to automate the steps to create the infrastructure. You cannot just go to AWS and create the infrastructure manually because you might have to do it multiple times across different environments like u staging production or sandbox environments whatever it is. So ideally as a DevOps you should automate the infrastructure creation that is using platforms like Terraform right by following infrastructure as code principles and then you know before you deploy the application you should containerize the

application right so typically in this case you have to create docker files first why Because when you're dealing with microservices, the today's standard is to deploy the microservices as containers. So first you have to create a docker file. Then you have to create docker images. In this case you have front end, back end and database. So you have to create three different docker images and then build the application locally. But this time as containers. Previously also you ran the application

but that was using some manual steps maybe a shell script that is shared by a developer. But here after containerizing the application you have to build and run the application using docker compose. So this is your first step of automation for the project. Now in future if there is a QA engineer that comes to you and asks you like I want to run this application locally you can just share the docker compost file with them. It doesn't matter if they're using windows if they're using Linux

docker compost would execute and would bring the application on their local machine. And once this is done you know your job is only half done till here. So once you create the docker compost file the next step would be to create kubernetes manifest because end of the day you want to deploy this blogging site onto a kubernetes platform. Okay. So even kubernetes would come under infrastructure creation. So when you create the infrastructure using terapform you would create a kubernetes cluster. But here at

this point you have to create Kubernetes manifests. Ideally it is good because it is deployed as a single application. End of the day you want this blogging platform as one single application. So it is better if you define one single Kubernetes manifest where once you deploy this Kubernetes manifest using cubectl apply all the resources that are related to Kubernetes are deployed to the Kubernetes cluster. It can be pods, it can be deployment, service, network policies, it can be uh storage related components,

anything. Don't worry, we will learn all of this. And then once this is done, you also have to implement CI/CD because every time developer creates a commit, every time developer creates a pull request, these steps should be automatically implemented. If not all the steps, at least majority of the steps should be automatically implemented. And most importantly, every action that you perform should be considered with security point of view because what we are targeting is dev sec ops implementation. Even if someone asks

you to implement DevOps, you should think about security in 2026. So that's why I keep saying whether it is devops or dev sec ops your end goal has to be the same thing in every action that you perform you have to take care of the security. So this is what we are going to implement in today's video. So we will implement all of this step by step and you don't have to get overwhelmed at any point of time. Understand one thing I've already implemented dev sec ops for this project and I have implemented dev

sec ops for hundreds of such projects. So if you find it intimidating while I run a command or you know if you feel I'm going little fast at any point of time please don't be intimidated. This comes from experience. Even if you implement dev sec ops for this project and two to three projects like this. I'll also show you at the end of the video how you can create demo applications for yourself once you're done with this even you will be able to implement dev sec ops at a very easy

pace and also you know to make it simple because I want every one of you to implement this thing after watching the video what I've done for you if you head to this project called journey you will find the link in the description the main branch we'll have the basic information about the application. But if you switch to a branch called DevOps here, you will have everything that I'm going to implement in the video. So you can see here uh I have steps for Kubernetes. I have steps for the

Terraform manifest which is going to create VPC on AWS which is going to create Kubernetes clusters on AWS. Each and every file is provided and along with uh Kubernetes and Terapform I have docker implementation, I have security best practices, uh GitHub actions implemented, I have docker compose implemented, everything is provided for you. As I told you after watching the video I want you to go back if required know take some time revise through the manifests in the uh DevOps branch and then start implementing the project at

your site. If you are struck watch the video again or get to this GitHub repository and look at the manifest. Perfect. So I hope the goal of the video is now clear. What are we going to do? We are going to implement end to end dev sec ops on a three- tier architecture application because as dev sec ops instance that is what you are going to deal with when you join a company mostly three tier architecture applications and what are we going to implement we are going to implement these steps on the

journey application a personal blogging application but Abishek when I join a company will the read file with this detail. I'll tell you one thing here. This is a question lot of people ask me you know whenever they want to learn dev sec ops whenever they want to create a project this is the first question that they have abishek I don't understand the codebase definitely yes and as a devops engineers it difficult also because today this application is written in nodejs react and uh the database that is being used

is postgress tomorrow you might work on a complete different text stack As a developer, you know, you won't be subjected to these many changes because if you are a developer, you learn Java. In the next company as well, you will be working on a Java based application because you would apply for Java based roles. But as a DevOps engineer, the only thing that is constant for you is the change. In one project, you might work on Java application. In another project you might work on Python application. In

another project you might work on NodeJS application. So what you have to rely is on the inputs that are shared by the developer. What kind of inputs? As I told you the basic text stack of the uh application as you can see here architecture of the application project structure and steps to run the application locally. And yes developers will share you these steps. If it is a public repository they would share these steps through the readme file for you. But if it is a organizational application that if it is

a private application they usually have something like confluence documents right or they usually have this information in word documents or in the shareepoint documents whatever is the internal documentation platform that your organization would use. If this information is not shared which is very very unlikely you have complete right to question the developer to share this basic information for you only then you can tell them I can start with the dev sec ops implementation let let's see what information is

provided here and I wrote this information for you um so that it's easy for you to get started so the architecture of this application whatever you saw the journey application The front end is written in ReactJS and a engineext web server is used as a reverse proxy. So basically you hit the engineix reverse proxy and from the enginex reverse proxy request goes to the react application something like this. So there is a user somewhere user would access enginex reverse proxy. So engineix reverse proxy and front end

is hosted within a container or within a pod or a virtual machine whichever way you would pick up. If you are deploying this on kubernetes a pod would have both enginex and the front end. So what happens here? The user's request would go to the enginex reverse proxy and from enginix reverse proxy the request would go to the front end. Whatever you see here and this is the general workflow as well because you can offload the configuration related things, routing related things, security related things, it can be SSL, it can be

certificates or it can be rate limiting. You can do all of that in the engineext web proxy. So that's why developers choose a proxy server first and from here request goes to the front end. So that is what is explained here. Okay. And then the request goes from the front end to the back end and back end here is written in NodeJS and express is used for the server purpose. Okay. So again from the front end request goes to the back end and back end here as I told you is written in NodeJS. What is the functionality of the back

end? Very simple. Back end acts as a layer between front end and the database. In this case we are using Postgress as the database. So every time a user makes a request request goes to the front end let's say on front end you know u user clicks on reading a blog the information related to the blog should be fetched from the database that is what backend does. Back end talks to the database. Back end gets the information from the database and back end forwards that information to the front end and from front end request

goes to the user. This is how the usual workflow works. Okay. The same architecture is also explained here along with the ports on which the application is running because these ports are hardcoded within the source code and this is the clear folder structure. Even this thing will be shared with you. So journey is the name of the application something that I have given. If you want you can change the name of the application and it has three different folders typically. One is the front end folder,

one is the back end folder and other is the deployment folder. In the front end folder you have the source code related to the front end. In the back end folder there is a source code related to back end and in the deployment folder there are steps to run this application on an EC2 instance. Ideally u the reason for choosing Linux instructions because if you're watching this video you might have a Windows machine. I'm using a Mac OS uh laptop. Lot of times when I execute the steps if you try to do the same thing the

execution might fail. So that's why I took an intentional choice to share the steps in the uh EC2 instance point of view so that you can also create a Linux machine. I can also create a Linux machine and we will execute the steps on the Linux machine to bring the application something like this. Perfect. Now you might say but Abishek what about database? No, you don't need uh any specific source code related to database because uh bringing up a Postgress database is just executing one single command. So

that's why typically within the application source code you would find backend folder you would find front end folder. Okay. So I'm in the DevOps branch. I have to go back to the main branch. Perfect. So this is back end. This is front end and this is deployment related script. What else? So we learned about architecture. We learned about project structure. These are the steps to run the application on an EC2 instance. Step one to create an EC2 instance. Basically go with Ubuntu. It is most widely used.

Then copy the source code to the Ubuntu instance. If you want to use SCP, you can do it or Ubuntu instance usually comes with Git. So you can also use Git. The reason for sharing SCP instruction here initially I had the source code on my laptop right so from the laptop I copied the source code to the EC2 instance so I use the SCP command in your case it's not the same because you already have the source code in a g repository so you can just run git clone instead of this command and then you have to SSH to the instance

and then you have to run the deployment script And this will bring the application live for you. So this is your first step. Just do this and see if you're able to get the application running. But one thing that you also have to do here in the deployment script that is setup.sh. Make sure you understand what is written. Okay, this is the only thing that is part of the source code that you have to understand. You don't have to understand the backend related files. You don't have to understand how the

React application is written. Only thing is focus on the deployment script which I'll go through for you. Okay. So first of all what is being done on the EC2 instance? The pseudo command is executed so that you get the basic packages. The packages that are required like git or you know update the packages version related to Python or existing modules on the Linux machine. Then install NodeJS. Why are we installing NodeJS here or why developer is asking us to install NodeJS? Because the back

end is written in NodeJS. Then you know install the Postgress database then install enginex then install Postgress client run some basic queries to start the Postgress database and finally you know all that you have to do is to set up EngineX configuration start the enginex server and bring the application live. These are all the Postgress related queries that we are executing and engineix related configuration that we are performing. Let's do that. Okay. So we will head to AWS console. You must

have an AWS account. If you don't have even if you have Azure account that is also fine or if you have a Windows machine just install WSN with WSL also you can get a Linux environment on your machine. Perfect. Launch instance. Let me call the name of the instance as local testing. I'll use the Ubuntu operating system as recommended by the developer. Instance type uh T3 micro is very small. Uh if you don't have enough credits on AWS, you can still use it. But if you have some credits, I would recommend at least T3

small for this because we are running a three ter architecture application. Then key value pair. Um I already have a key value pair. I'm going to use it in your case. It's up to you. You can create a new one. Allow the application to run on HTTP. Why? If you understand the architecture, you will know the answer. Just go back and if you notice developer clearly shared front end is running on port 80 and what we would access is front end only. So that's why we have to expose port 80 on the instance.

How do you expose port 80? So there is an option while creating the instance allow HTTP traffic from the internet. If developer says this port is port 5,000, then you have to create a new security group and you have to enable port 5,000 as part of the security group. Okay. Uh because you're using a Postgress instance, let's say you want to use it, you really want to use it from the EC2 instance. I'm I don't want to continue with the rest of the steps. Uh for me, this initial first step is good enough.

I'm just learning DevOps. Then do one thing because you are going to use the application from here. Just increase the EBS volume size to let's say 16 GB because your post database is going to use the volume from here. If you create 100 such blocks or if you create more number of blocks, you would note this storage. Otherwise, you can go with 8 GB storage as well. And most importantly, u once you're done with your demo part, delete the EC2 instance, go back to the volumes that is go back to EBS, search for EBS or

volumes and check all the volumes are deleted. Otherwise you will be charged by AWS not only for instances but also for the volumes. You don't have to mess up with any of the advanced details. Click on launch instance. Okay. So till now what are we doing? We are just getting started. We are just running the very first step to understand the application. And this is the most cru crucial step as well. You know before you perform anything like before you set up the infrastructure with terraform containerization docker

images this step is very very important. Why? Because only at this point you will know the architecture of your application. You will know the project structure. You have to get along this project for 6 months one year when you work in a company. So you should know the in and out of building process, deployment process and the starting point is running the application locally. Cool. So we have the instance now. Let's access the instance. So we just need the public IP address. I will copy it. I'll

go back to my terminal. You can use any terminal. U you know if you're a beginner, you can use the native terminal on your laptop. Even PowerShell works fine if you're using a Windows machine. Otherwise, just go with mobile X term or WSL also comes with its own terminal. If you have a Mac OS, go with the terminal. I mean the name of the terminal itself is terminal or it term. Cool. So, what are we going to do now? We are going to SSH to the instance. SSH. Uh let's first switch to the uh the

downloads folder. Okay. So I'll just go to downloads. You can go to the temporary directory also or OPD folder also. And I already have the application here. So let me first delete it so that we both are on the same page. The one who is watching the video and the one who is explaining the implementation. RM - RF JC I mean J sorry. Okay. Now what I'm going to do here SSH I followed by the PM file. I have it in the downloads folder followed by what is the name of the PM file? Sample I guess or what was the name of the PM

file? Let's see. So go back to the instance here I can find I'll just scroll down sample keypad. Okay sample keypad.pm followed by Ubuntu at IP address. If you have just downloaded the pen file, uh when you do this, yes, you will run into an error because by default the one that you downloaded, the pen file that you downloaded from AWS, they will come with escalated privileges. So what you have to do, you have to run this command ch 400 wherever is the location of your pen file. Let's say downloads followed by

the name of the PM file. Sample um the name of the PM file which is sample key pair. Perfect. So run this command and then your SSH command will work. Okay. So we have SSH to the instance. Now what is the first thing that we have to do? We have to update the apt repositories. Whenever you create an EC2 instance, this is the first step that you have to do. Sudo apt update. But Abishek, why do we do this? U this is a general activity that you perform because uh the instance that you created, it comes from a from

an operating system and this operating system might be bundled a long time ago. Okay. And from that time to the time that you're using this instance, there might be some new versions of the packages in your operating system. So that's why you always have to do pseudo app update so that the modules that you have on this instance or packages that you have on this instance are upgraded and up to date. Cool. So now uh let's use get clone followed by the main branch. Okay, make sure you're

on the main branch. You can copy it from here and paste it. github.com. I am viola journey.get. Perfect. Now let's switch to the journey directory. Uh cd capital J. Okay. And there you go. We have all the files that are required. Now we will switch to the deployment directory. Once again, because you just downloaded this uh or clone this repository, you have to grant the permissions to the setup.sh. This time because you are executing the setup.sh, message you can grant 700 or if you're confident only you are

using this uh instance virtual machine you can also go with trip 7 but the safest option is to go with 70 setupsh okay now let's run the script dot / setupsh uh should I take some time here to understand what exactly is is uh is in this setup.sh definitely you can do it. The reason why I did not stress a lot because there are some SQL queries here. Uh if you are a beginner you might get intimidated with the queries. You might get intimidated with EngineX configuration. That's why I did not go

through a lot of it. But you know if you feel that I can take help from the internet I can take I mean I have some prior knowledge of uh engineext configuration good enough take some time here pause the video look at the setup.sh SH and once you're good resume the video. Okay. So what is this script doing? It is executing the packages first then it will in install engineext then it will install uh the NodeJS it will install Postgress. So this might take 2 to 3 minutes of your time. Perfect. So right after 3 minutes we

have the message here journey is now live and you can access it on the local host. I mean when I say local host on the EC2 instance IP address. Okay, let's see. We will go back. We will get the instance IP address one more time. This is the one. Copy it. Take a new tab on your browser and paste it. Now you should be able to access it because it is running on port 80. Let's see. There you go. So, journey blog platform it took I think roughly around 1 second to come up because uh it has front end,

back end and database. For the first time when it loads, it will take that initial 1 second of time. Okay. So, uh you can see here this is the one that I have running on my local machine at this point which I'll uninstall in a while. And this is the one we have running on the instance. Here there are two blog posts. Here there is no blog post because this is a different application which is running on my uh EC2 instance. This is the application which is running on my laptop. So this way you can also

create an instance of yourself. You can keep blogging here and you can share it with your friends. Okay. Now what is the next step? So we are done with the first part to run the application locally. Now it's time to set up infrastructure for this application. Abishek, why do you need infrastructure? We can just run the application on an EC2 instance. No, that's not how typically it works because this is a blogging application. It's fine. I mean you are the only user. So it is okay. But imagine you are

working for companies like Amazon. Okay. Even they have front end, back end, database. But every micros service of Amazon is used by millions of users. Right? Even at this point, Amazon.com if you take any single micros service, it would be used by millions of users. Not every application is used by millions of users. There will be some startup but at least some hundreds of users would be using it. So you cannot rely on this one single virtual machine architecture. These days the popular architecture is

the microservices architecture right even the developer here wrote the application in the micros service architecture front end is individually deployed back end can be individually deployed right so for the microser architecture the recommended platform is the kubernetes platform so our end goal is to deploy this on kubernetes right so we have to set infrastructure for this. Of course, we cannot directly deploy the application to Kubernetes. There are some prerequisites. First, you have to use Docker to create containers,

right? You have to verify the application is working as containers. Then again, no, you have to define Kubernetes manifests. But all of that comes as next step. First, let's focus on creating the Kubernetes infrastructure. How will we do it? Okay, one way you can do it manually, right? Just set to any cloud platform like AWS, Azure, GCP and they all have user interface, right? Just go to AWS like this just like how we created a virtual machine. Similarly, go to EKS, create a Kubernetes cluster

by clicking on create cluster. But this is not the recommended approach. What you have to do? You have to follow the standard approach that is first you have to create a virtual private cloud in AWS. Okay. What is a virtual private cloud? Virtual private cloud is basically a private network within AWS. Because AWS is a public cloud, you don't want your application to be shared by some other in the world who is also using AWS. So first we will create a VPC, virtual private cloud. We will define subnet for

the VPC. First we will define the range for the VPC. What should be the C range? Then we will define a public subnet, a private subnet. And within the private subnet we will host our EKS cluster. Okay. So this EKS cluster is a managed Kubernetes cluster. We will use EKS auto mode. Okay. When you go for EKS auto mode, you have both the control plane of Kubernetes and data plane of Kubernetes. All the Kubernetes master node and worker node are managed by AWS themselves. This is the current trend.

This is how people are using Kubernetes at this point of time. I mean mostly. So we will do the same thing with terapform. Okay. First we will have Terapform installed on our local machine. You can use your laptop as well. And from the terraform we will first connect to AWS that is using AWS CLI. Okay. And then using this Teraphform AWS credentials we will interact with AWS APIs using Terapform modules. And then using the Teraphform module for VPC we will first create the VPC. Similarly we will create EKS on

AWS. And this AKS we will use EKS auto mode. And once we have this infrastructure we can go back and follow these steps. So we can go back create the docker file create the container then create Kubernetes manifest and finally deploy the application onto Kubernetes. And there is dev sec ops. Here we are using VPC which is a secure uh virtual private cloud. And even while using Teraphform, we will follow all the security best practices. I'll show you while creating the Teraphform manifest. But I hope this part is clear. Once

again, I will repeat this. So what are we going to do now? Instead of manually creating a Kubernetes cluster on this cloud platforms, we will start with using Terraform. So we will install Terapform on our local machine. We will configure AWS credentials with Terapform using AWS CLI. Then we will create VPC using Teraphform modules. Then we will create EKS cluster again using Terapform modules and we will use EKS in the auto mode so that we don't have to bother about upgrades related to control plane,

upgrades related to data plane. we can ensure our Kubernetes cluster is always secure with respect to vulnerabilities related to the data plane or control plane. Okay. So again where will you find the Terapform related code? Switch to the DevOps branch. You can find a folder for Terapform. Click on the folder and you have all the manifest that you are looking for. Cool. So let's start with installing Terapform. You can also use this EC2 instance. Uh for some reason if your laptop is not well equipped, you can use this EC uh

EC2 instance. I'm going to use the same thing. First I look for uh install Terraform. Okay. So you can search for this. Uh you will find Hashikop developer. Go to this and here look for terraform installation through the package manager. We are looking for u Linux. So or you can just go for uh this option here Linux pseudo appget. So we want to install on Ubuntu. So I'll go with this option. Let's start doing it. First thing again they are updating the app repositories. Cool. Once this is done, install Hashi

Cororps GPG Kink. This is just to ensure you are downloading the right uh Terraform package on internet. There can be some uh fake Terapform packages. So just to eliminate that you will use the GPG key. Okay. And you will verify that you are using the right GPG uh key fingerprint. So even if someone creates a fake hashior corp website and if they host uh like you know fake terapform package you can eliminate that by using the gpgk fingerprint. Cool. So now uh add the hashior official repository from where you will be

installing terapform. So we added hashior repository because we added a new repository. We will update our app repositories one more time. Because now our app repositories also has terapform package in it. By default it does not have like on a Linux machine if you just go and run sudo apt install terapform you will not be successful only you will be successful when you add ashic related packages to the app repositories. Now this command will work sudo apt get install terafform. So basically for this

to work we executed all the commands before this is for security best practice the GPG key signing of it verifying it otherwise you can just run this command this one add official hash account repository. So just add this one. Just run this and run this. In three steps also you can install terapform. But all the previous steps are for security best practice. Cool. Let's see if terapform is installed. Looks like it is. Now if I run terapform version there you go. So we have Terapform running now. But Abishek, how do I

connect my Terapform to AWS? Right? Because we have to create resources on AWS. Yeah, that is definitely important. How would we do that? So for that you need to go to your AWS account. In the AWS account, uh just go to this option here. The uh in the settings go for account. Okay. So in the account just scroll down. You have to find security credentials. I think I'm looking at the wrong place. Let me quickly see. So here within uh organiz Yeah, security credentials. My bad. Yeah, this keeps

happening you know when you keep working with different cloud platforms. Uh because I keep working with AWS, Azure and GCP sometimes I get uh confused with the options. Yeah. Anyways, so once again just click on this one to the top right. Then you will find the option for security credentials. Click on it and you have an option to create the access keys. You only have option to create two access keys. I already have two. I'll just delete one. Uh this one is very old. I'll just delete it. Okay. First I

have to deactivate. Sure. deactivate it and delete it. There you go. Now I'll create a new one. I'm not bothering about uh hiding the access key because after watching the after completing this video, I'm anyways going to delete these access keys. So this is my access key. This is my secret access key. So I'll run the command terapform or first AWS configuration. So AWS configure. Okay, AWS is also not installed. Fair enough. Let's do sudo apt install AWS CLI. So you can see

here we are doing things from absolutely scratch. Okay. Once again AWS CLI is also not out of the box. Let's say AWS CLA download. Okay. We'll head to AWS documentation. Installing AWS CLI. Uh install and update. Okay. Uh click on Linux one more time and go for the Ubuntu operating system. So these are the commands. First we will curl. So we will download the AWS CLA package. Curl is successful. Then let's unzip the AWS CLI. Unzip is also not part sudo app install unzip. There you go. So now we have unzipped.

Let's proceed with unzipping the package. Okay. So we have uh all the files inflated now. Now let's run the command p sudo dot AWS/install. Okay. So now we have AWS CLA. Let's rerun the command AWS configure. It will ask for AWS access key ID. Whatever you got uh here just copy paste it. Cool. Now it asks for secret access key. Again copy paste it. default region. Go with the region that is nearest to you. Make sure if you are using an EC2 instance, choose the region that is nearest to the EC2 instance, not nearest

to you. I created the EC2 instance in US East1. So, I'll also choose the default region as US East one. Perfect. Uh default output format, let's go with JSON. Now we did the second part of our uh infrastructure setup that is we connected AWS with uh or terapform with AWS APIs. Now if you write the terraform files it can connect to AWS APIs and it can create infrastructure on AWS. Cool. How do you write the terapform files? Okay. See one way of doing it is obviously you have to learn learn

terapform first. I made videos on terapform. We have terapform zero to hero series. It's definitely not possible to cover entire terapform in this video. So I'll quickly go through the files for you. Okay. I'll explain the steps on how to write these terapform files because it's not practically feasible to you know cover what are modules to cover what are workspaces to cover uh the terapform life cycle uh entire thing so I'll go through the files for you also the remote back end state file there are a

lot of concepts instead I'll share my terraform playlist link in the description cool so we'll go back. So we'll head to the GitHub repository and in the GitHub repository these are the Teraphform files in the DevOps branch. First of all, you should start writing your Terraform with provider.tF. What is provider.tf? So here terraform does not understand out of the box if you're creating infrastructure on AWS or if you're creating infrastructure on Azure if you're creating infrastructure on GCP

this solution Oracle cloud platform there are a lot of things right so we will define what Terraform wants to talk to in the provider TF so this is the file here okay so the provider TF says AWS is the provider provider. Okay, I'll go through each and every line for you. So understand carefully. First we will define the provider configuration. Okay, and then we will define the uh within the provider. So we said AWS is the provider here. Now within the AWS provider, what region to choose? Right?

Because in the AWS cloud platform there are a lot of regions. In which region the infrastructure has to be created. So you have to define that where will you define? You will define in the provider block provider AWS region we will define this as a variable because I don't want to hardcode. If I say US East1 here you all will also create the infrastructure in US East one. Instead I will variableize this just like how we variableize shell script or Python. And these are basic tags. Even if you

ignore this block, it's fine. Okay. So these are just the blocks. I mean these are just the tags to identify that you created the resources using terraform. Cool. Once you're done with the provider block, then the next thing that you have to do is to define the variables as much as possible. Okay, you might say but Abishek at this point I don't know what all variables to create. At least start with defining the variables that you already know. For example, you know the AWS region is going to be a variable. So

just say this is how you define variable in terapform. Again we learned this in our Teraphform 0 to playlist. So variable AWS region just provide the description of the variable. Uh what is the type of the variable? Is it a string? Is it a list? Um or anything. Then say what is the default value of the variable. Right? This is one good thing in terafform. When you define a variable when you declare a variable you can also say what is the default value of the variable. Is it US east one ap. So even if someone forgets to provide

the variable value here okay in the provider.tf let's say even if someone uh forgets to initialize. So it will take the default value from variable.tf. Perfect. Then you know we are also going to use the environment uh because as dev sec ops snap you will be creating the same infrastructure for uh UIT environment staging production. So another variable for environment name of the cluster should also be a variable. End of the day you are creating a Kubernetes cluster. End of the day you are creating a VPC. So you also have to

provide the names for it, right? VPC comes with a default uh ID at least. But for the Kubernetes cluster, it's better to provide a name for it. That's what I did here. So the name of the cluster, I'm just calling it as journey case. Also the version of Kubernetes, I intentionally choose 1.32 here. Uh I can go with 1.34 which is the latest version. I think 1.35 but the reason for choosing 1.32 just to show you uh how DevOps instance can mess up with the clusters you know if you don't have

right security mindset you can go with an outdated Kubernetes version I'll show you what happens u once we create the cluster you know it will show you a clear security banner that you're using an outdated version okay but yeah the goal here is to define a variable for the cluster version and VPC C range right this one here in the VPC how many IP addresses do you need how would you define that you would define using C range every time when you create variables TF there will be some level of

uncertaintity initially you might not know what all variables are required it is fine start with whatever you know then it comes to you know once Once you define provider.tf once you define variables.tf start writing the main.tf as the name suggests main.tf is the main file that you are writing. Cool now we are creating VPC we are creating EKS. That's what we are doing right. One good thing about Terapform is that you don't have to write a lot of code. In Terapform by default there are something

called modules. Okay. In the Terapform modules lecture I explained very very detailed what are modules, how to create module, how to use module. But you know if you want to understand at least for this video modules are nothing but the pre-written code or you can just assume this as pre-written functions so that you don't have to reinvent the wheel. Let's say you want to create an EKS cluster, okay, using Teraphform, you might have to run some thousand lines of HCL file, the Terapform files. But this

is a very very common thing, right? Every day hundreds of people would do the same thing, create EKS cluster using Teraphform within their organization. So why everyone has to write this thousand lines of code? you know someone can create a boiler plate or someone can create you know the how do I say it you know someone can create a function or a module that everyone else can use that's what terapform does so terapform has modules for EKS it has module for VPC it has modules for S3 Dynamob what not

terapform has thousands of modules not only for AWS for other cloud platforms as well. All that we have to do we have to use these modules right and we just have to pass the values to it. That is what is the basic difference right? I will be creating an EKS cluster. X person in the Y organization will also be creating EKS cluster. A person in B organization would also be creating an EKS cluster. The difference is primarily I would use different values. Probably name of my EKS cluster is different,

version of my EKS cluster is different, right? Or uh availability zones of my EKS cluster are different. But rest of the things are same. That is what we will do. We will use the VPC module. We will pass some values. We will use the EKS module. We will pass some values. Right? I'll show you here. So the same thing first I'm using the VPC module where am I getting this VPC module where is the boiler plate coming from it is coming from Terraform AWS modules VPC AWS so I'm using a trusted

source in fact you can find a lot of modules on the internet but not everything can be trusted right just like how you can find a lot of docker images on the internet you cannot trust everything but at least terraform AWS modules are the official modules you can believe them and then I'm passing the variables as I told you I'm passing the name of the VPC I'm passing the C range for the VPC where did I get them from you know I got them from the variables here right I initialized the variables so I'm using

them here and similarly I'm just adding adding some uh default things for the module for example enabling the NAD gateway for VPC I need it why because I'm using Kubernetes cluster in the private subnet so obviously my Kubernetes cluster has to communicate with external resources so that is needed and then I'm also enabling you know the single n gateway I'm enabling here and I'm saying a single nad gateway Okay, so that it is cost effective and these are some uh basic tags. What

is the tag for private subnet? What is the tag for public subnet? If you don't need, you can avoid this as well. Overall, instead of writing 100 lines for creating a VPC, we just wrote 17 to 30 just 10 lines of code, 10 lines of HCL file. That is the benefit of modules. Then EKS module again we will not be passing huge details just the source of the EKS module version of the EKS module what is the name of the cluster what is the name of the cluster version similarly some cluster compute configuration

uh the VPC in which the EKS cluster has to be the subnets in which EKS cluster has to be authentication mode uh what are the log types that you need basic information. Abishek where will you get this information also? See one thing is you can go to the terapform documentation but this also comes from comes with practice. Okay. If you go to terapform documentation you will find a lot of fields but you only need to take what are mandatory for you or what are important for you. These are the important fields that you have to take

per minimum EKS cluster configuration. I have used the minimum configuration that is needed. You can also use the same configuration and you can explain the same thing in the interviews as well. We used this configuration for EKS. Now finally you have to pass the actual variables right here. You only declared but you did not define. Okay? I mean you just defined the variable name. You did not declare the variable, right? You did not say what is the actual value of it. You said this is the default value. But

what if we want to pass a value for the variable? What is your actual AWS region? You can do that using terraform tf. So in the tf whatever variables you took you assign the actual values to the variable. AWS region is a variable that you define here you say the axel value of it environment okay you are saying this is a dev cluster name of the cluster you are saying journey eks cluster version you are saying 1.32 vpcc you are saying the value these values here can overwrite the default values that you have in the

variables tf right what is the difference between variabletf and Terapform TF as in variables.tf you initially define the variables you define the default value of them but in the terapform.tf you actually provide the values for the variables and whatever you provide here this will override the default values in the variables.tf TF perfect and then the outputs.tf whatever you want to see on the screen. Once your Terraform execution is complete, you have two options. One is you can go to the cloud platform. You

can see what is the cluster that is created, what is the name of the cluster that is created, is it created with the right name. You can do it from the user interface. If you're you know if you want to do it through the CLI you can define something called outputs.tf okay in the outputs.tf TF you can say print the cluster name on the screen once teraphform execution is complete print the cluster endpoint print the cluster certification authority link the VPC ID print the region of the VPC as

well as the cluster cool so now let's proceed with the execution here on this uh machine on the EC2 instance we have the basic setup ready it is connected to the AWS environment also. So what I'm going to do here, I'll just go back in the journey folder. I will switch gout devops branch. Okay, you have to check out to the devops branch because only here you have the terapform code. Then let's switch to the terraform. Okay, this is the terapform files. These are the ones start with terapform in it.

There you go. So the basic terapform initialization is going on. What is the basic terapform initialization? Which provider it has to connect to? What is the provider configuration? Right? If there is any back end where you want to store your state file, right? All that configuration is done during the initialization. If you look at my provider.tf, TF there is also something called backend configuration which we learned in our dev sec ops configuration or the playlist how you can secure terapform

state file by using a back end. So right on the terraform state file which holds the information of everything that you created using terapform will be stored on this machine. But if you don't want to store that on this machine, you can define a backend configuration so that your terapform state file will be created within that S3 bucket. Cool. Now before you execute just run terapform plan. Teraphform plan will show you what all the configuration that will be created. Only if you're conf confident with

terapform plan you can run the terapform apply it will say okay if you execute this terapform files 57 new resources will be created don't worry about the number but abishek why are 57 resources being created I'm only creating VPC and EKS there will be internal resources right within VPC you have uh subnets within subnets you have routes route tables you have n gateway you have endpoint end points then you have lot of internal VPC components and Kubernetes related components that's why it is saying 57

components will be created again you can pause the video here uh if you're executing parallelly just go ahead and see what are all the resources that are being created it will be very interesting for you and also it will be very insightful now I'm confident with this because I wrote the terraform script so I'll say terraform apply. Okay, you don't have to be confident with the script that I've written. Just look at all the resources that are being created. Only then run terapform apply. Cool. Now, this would

take a lot of time. In fact, this can take up to 30 minutes as well. So what you can do while this is getting executed you can uh go back watch the video one more time or you can look at the terapform repository. Make sure you enter the value as yes otherwise it will keep waiting. Click on yes. You will have roughly uh at least 15 minutes of time. Terraform execution is complete. It is successful and even on the AWS console I can see journey EKS cluster and importantly status of the cluster is active and the version of Kubernetes is

1.32 but let's perform an additional check by deploying a simple resource to this cluster. One thing that you have to note the banner here end of standard support for Kubernetes version 1.32. So this is what I was talking about while going through the Kubernetes version in the Terraform manifest. I told you I intentionally picked up 1.32 to show you that what happens when you pick up an older version. So right now from March 23 this version is going to be out of support. So let's say there is some

issue with this Kubernetes version AWS will not take the responsibility. So as dev sec ops engineers it is also important whenever you are dealing with kubernetes even python packages terraform make sure you use the upto-date version otherwise let's say this kubernetes cluster is created in production today there is no issue but after 13 days this will go out of support cool so now how do we connect the cks cluster to our personal machine or AWS EC2 instance and deploy a sample pod onto this cluster. So for that what you

have to do again either take your terminal or take EC2 instance it's up to you. The first thing that you have to do is use the AWS CLA command AWS EKS update cube config. So this is the command followed by hyphen -y name the name of the cluster in this case journey case hyphen region US east one. Now this will update the cube config configuration on your local machine again for EC2 instance whatever you're using but one thing is that you have to have cubectl running on the machine. I already have cubectl running now I

updated the cube config. So if I just do cubectl get pods a I will get all the pods that are running on the cluster. Right now there is nothing running on the cluster. Even if I do cubectl get nodes you would see there are no nodes on the cluster. But Abishek why is the case? We created the Kubernetes cluster using terapform. Why are there no nodes on the Kubernetes cluster? If you remember we created the cluster using EKS auto mode. What happens in the EKS auto mode? So EKS auto mode is just like

serverless and this is also managed control plane as well as data plane. So only if you create a pod on the Kubernetes cluster or you create series of pods on the Kubernetes cluster according to your requirement, EKS auto mode is going to spin up servers for you or worker nodes for you. This is taken care by carpenter. You don't have to install anything. By default, EKS auto mode comes up with carpenter. Carpenter is the one. It looks at the incoming ports and creates or sets up nodes to the EKS

auto mode cluster depending upon the load. I'll show you now. We will create a deployment cubectl create deployment. Let's say uh name of the deployment is engine image engin. Now as soon as you do this you will notice a node will be created for you. So initially when you go to the resources you would see the pod in the pending status. Click on the pod look at the description you will see no nodes available to schedule this pod. This is where carpenter is working. Now carpenter will get a status update that

okay there is some scheduling that is taking place but there are no nodes on the Kubernetes cluster. So let me create one in next 2 minutes you will have a node that is set up and the pod in the running state. Yeah there is one downside of it. This will take little time because the nodes are not uh created out of the box. If you go to a usual Kubernetes cluster, it's not the case, right? Usually when you create an EKS cluster, you would set up node pool by yourself where you might create three

node cluster, six node worker node cluster. So as soon as you create a pod, one of the node will be picked up by the scheduleuler and pod is automatically scheduled on the node. But in this case, this is very very cost effective. So that's why it will only create the node for you when you request for a port. Let's see. I think this should be 2 minutes. I'll refresh the status. There you go. Now it is in the running status. This is exactly how EKS auto mode works. Even if I do cubectl get nodes, now you

should see one node that is set up for you. If you create 100 enginex ports then it might create 10 kubernetes nodes for you right? Okay. So this part of verification is also done. So till now what did we do? So we are done with the first step setting up the application locally. Then we are done with infrastructure setup as well that is using terapform. We are successful with this step. Now let's go to the next step. Containerization of the microservices. This is where lot of devops instance get

scared. Personally I've seen lot of them worry about creating docker files. Why? Because I keep hearing abishek to write the docker file do I need to know programming or do I need to know how the application is written? Let me tell you this is absolutely not required. Because as DevOps instance you will keep working on different programming language applications. Today you might have to containerize a NodeJS application. Tomorrow you might have to containerize a Java application. Net application the list can just keep going

on unlike developers right because developers today if they work on a skill set in the next company most likely they'll work on the same programming language. So what you need to do as a DevOps engineer, you only need to learn the structure of the Docker file. If you know the structure of the Docker file, creating Docker file is a cakewalk because every time you do exactly the same steps, I'll explain you the structure here. Just remember then for the front end, for the back end, we will follow the

same structure. The first thing that you have to do is use a base image. Okay, this is the first step. How will you pick up the base image? Just go with the programming language that is used by the developers. In our case, React or npm that is part of node. So we will use node as a base image. If you are working with a Java application, you can use open JDK as a base image. If you're working with Python application, you can use Python slim as your base image. The second step, choose a work directory. It's just

like you know choosing a directory on your laptop. But in the Docker file, you choose a work directory so that all the commands that you're going to execute next, they will be executed within the particular directory. Let's say to download a file from the internet. If you choose a work directory, you are sure that the file is downloaded within a particular location. That's why you use the work directory. Okay. Then the next thing once you choose the work directory go ahead copy the dependencies.

Again if you're using a Python application copy the requirements.txt. If you're working with Java application copy pom.xml. If you're working with let's say Go application, copy go mode. In this case, we will copy package.json. Okay. Then execute the dependencies or install the dependencies. You copied them. Now you install. Then copy rest of the source code files. typically all the files and then you will build the application. Right? These are the steps that you need to follow. You have to do it step by

step because Docker caches in terms of layers. And once you do this, this is where you can actually end. You can use an entry point and you can execute the build. But what we are following in this lecture is dev sec ops practices. So from security point of view what we will do we will go with multi-stage docker build. So in the multi-stage docker build process we will use this as a first stage and in the second stage which you can also call as production stage what we will do we will again choose a base

image but this time we will go with a lightweight base image or we will go with if you are dealing with front end we will go with engineext base image. Why? In case of front end, EngineX receives the request and EngineX forwards the request to the front end. Right? We already learned that during the theory part users request goes to Engineix and Engineix forwards the request to the front end so that it can block certain requests. It can proxy the request. It can implement SSL for your front end. It can implement

DOS attack prevention anything even web application firewall. So we will use engineix as the base image for front end and we can use any other base image like a dist or lightweight base image for the back end and then we will copy the artifact that we got in the first step. So when you run the build command you would get you know something in the dist folder if you're working in the NodeJS application. If you're working with let's say Java application you get a jar file here. Right? If you're working with

let's say Golang application you get a self-contained binary. In the production stage just copy this binary here and use the nonroot user. Another good dev sec ops practice so that you don't run your container as a root user. And finally use the entry point or cmd. That's it. So this way you will not have your source code. You will not have the dependencies and any unwanted things in the production stage or in the second stage of your docker build. They are all part of your build stage of the docker

file. In the production stage, you will have a lightweight image so that there will be very less security issues potentially. You will copy the artifact that you got from the first stage. You will use a non-root user. Another important security practice and using command line or entry point you will execute the binary. This is the process that we are going to follow. If you remember this process, any docker file you can write effectively. And now we will do the same process for our front end first.

I'll take my visual studio code so that it is easy to write instead of using the terminal. I clone the repository here. I'll head to the front end folder. I'll go to the src and within the src I'll start writing the docker file. So I'll create a file here. new let me call this as docker file or I mean there is no uh hard requirement that you only have to put it within the src if you want you can also put it at the parent level for example within the front end just create a new file and call it as

docker file this is also good enough okay so let's start writing the docker file as I told you the first step is going to from we will use a base image. In this case, node is a base image. Choose a version of node. I'll go with 20 uh alpine. You can also get this from dockerhub. If you're not sure abishek, how do I get the nodejs base image? Just go to docker hub and here search for node. Okay. And you will notice so this is the official node images docker official image. Click on it. And here you have

all the versions. Abishek why did you go with 20? Because the latest version is 25. Very simple. I am intentionally taking an older version so that our CI/CD pipeline will break. I want to show you how an dev sec ops pipeline looks like. So we cannot take a perfect application. We want an application that is flawed. We want an application that breaks on our dev sec ops pipeline. That's why I'm using an older version. If you want to go with a perfectly executing uh CI/CD pipeline, then choose the latest version.

I'll provide an alias to it as build. Cool. Then the next step as I told you go with the work directory. We'll choose / app as a work directory. Then we will copy the package.json. So use the copy package dot.json. And in some cases you also have to go with package.json star. Okay. This is not required for every front-end application. At times when you have package JSON lock file or you know when you have similar files you would go with this and where you want to copy you want to copy to the work

directory. So just say dot and then the next step is to execute the dependencies. How would you do that? run npmci and also you can just do npm cache clean - force abishek how do you know these commands that these are the commands that you have to use these are actually provided in the setup.json JSON as well. If you go to deployment folder, if you look at setup.json to run the application locally, almost the same steps are followed. Okay. So, you can either follow the instructions from here or when you are initially writing the

Docker file, you can also ask the developer. Cool. Now, let's copy the rest of the files. Copy everything to the app folder and run the build process. npm run build. Right? The same thing that I explained here. I followed the exact same instructions. You give me a Golang application, I'll follow the same steps. You give me a Python application, I'll follow the same steps. Okay. Now, because we want to build a dev sec ops uh architecture, we want to do everything secure. Let me call this as

final stage or you can also call it as production stage. And this one let's just leave a comment here for easy understanding for others that this is the build stage. Abishek is it common to have only two stages? Yeah, usually you have only two stages but uh it doesn't matter according to your application. Let's say you are dealing with a complex service where you are building the front end back end as part of the same micros service. In fact, you can go ahead and you can create multiple stages as well.

Okay. So there is no hard limit that you only have to have two stages in a docker file in a multi-stage docker file. Okay. Because we are dealing with front end application we will use engineix as a base image. engine x col 27 - alpine again you can get this from docker as production what is this as this is basically a alias we are giving to this stage cool now usually when you use engineix as a base image it's a security best practice to remove any kind of existing engineix configuration because we are going to

place our application within the engineix folder or our artifact within the engineix folder. So that's why I'm removing the default one. I mean if you have used enginex in the past when you install enginex by default if you run the engineext web server you see index.html where you see something like uh hello from engineext the out of the box one. We are just deleting that one here because we don't want to see that. Instead, we want to see our application served by EngineX. Cool. But Abishek,

where is our engineext configuration? It is already here. If you look at the front end, you will find enginex.com file here that is initially written by the developer while we are working on the local development of the application. So just copy that. Copy engineix.conf cons which is here to the engineext location on the docker image / etc/engineex condefault.com this is where you have to place now it's the final step I mean the important step this particular command in the build stage would have created a

binary basically that is in the / app/ist folder so we will copy copy / app/dist the content of the dist folder to user enginex html folder. Okay, that is the most important step where we are copying the output of our first stage to the final stage. Now what happens? The front end application is in this particular location user share engine html. Now if this is in this location what Engineix does instead of serving the default index.html it will serve your front end application. Cool. So this part is clear

right? We are copying the artifact from the first stage to the final stage where we are placing our front end application in this particular location. when you place front end in this location user request enginex engineext automatically forwards the request to the front end and for security best practice let's create a user and let's grant permissions to the user so I'm creating a user called engineix I mean in the engineix base image the user is available out of the box so for the

engineix user and engineix group I'm granting permission s to the engineix HTML folder and we will say go with the user enginex so that the container runs with the engineix user instead of the root user there are several disadvantages when you run your container with the root user basically I'll give you one example I've covered that in detail in our dev sec ops playlist but if you run your container as a root user anyone who gets gets access to the container. If your container gets hacked, potentially they

can get access to your entire system. If you are running it on Kubernetes, they can get access to your entire Kubernetes cluster as well. So, it is not a good practice to go with the root user. Let's expose the port uh expose port 8080 or whatever is the right port. And in the cmd we will just say engineix right we are running enginix hyphen g. So basically I'm starting engineix here demon off. Okay so this is our final step. Now we are good to go. Engineix g demon off. So this is the docker file for your front

end. If you master this particular example, trust me, tomorrow you work with any front- end application, you will be confident. Even in the interview, if they ask you to write a docker file, you can write the same docker file because we followed security best practices and also we wrote a perfect Docker file for the front end application. We will execute this, okay, and quickly verify if this is working. So I'll go to the um folder. So the folder name here. Uh my bad. So let me go to journey first. And here let me go

to front end. We have the docker file here. Let me see if my VS code is syncing right. Okay. So they are perfectly synced. Now you need to have Docker running on this machine. If you are going with an Ubuntu instance, Docker should be available out of the box. You can use the Docker. If you're using your personal machine, just start Docker. So, I'll run this command. Docker build- t front end. I'll just call it as front end. Uh, yeah, I think it's okay. Colon latest dot. So basically I'm creating the

docker image with this particular tag front end latest or let's say journey front end even simple way of understanding. Let's see what happens. It's okay if there is any issue we will immediately try to address it. Engineix failed to resolve metadata. So it says abishek you are using a wrong base image. This one does not exist. Maybe there can be some typo here. Oh yeah, there you go. So there is a simple typo. It's not engineix 27. It's 1.27. How did you figure out? You can go to

the uh docker hub here. In the docker hub, search for engineix. You will find the official docker image for engineix. Click on it. You will find set of tags. What we are using is 1.27, right? But Abishek, why are you going with again not the latest version? As I told you, we want to see some issues with the Docker image. That's why I'm using an older version. Cool. Uh as part of Dev Sec Ops, we want to identify some issues. That's why we are intentionally using old versions. Let's rerun this.

Uh oh, is it not saved again? It says the same thing. Maybe I did not save it well. vim docker file or let me uh go back to my VS code. It's easy to address there. 1.27 uh this is cool. It's updated here. Engineix. 127 Alpine Node 20. Okay. So, let me do it one more time. Docker build. I'll just change the tag of the image in case if it is caching the information. Cool. So that part of the docker image went ahead. That part of the execution moved ahead. Now it is complaining about the package.json.

Okay, this is fair enough. If you see the files here, there is only package.json. There is no package. JSON star. That's why it is saying I could not find packages. That's a simple thing to fix. In fact, you will not run into this issue. Uh personally, why am I running into this issue? Because I used uh EC2 instance for the running the application locally to just show you. And here I'm using my personal machine. The reason for using personal machine is simple. I want to show you how to write the Docker

file using VS code. So that's why I'm just using my local machine for the next steps. Cool. How this can be fixed if you just run npm install. It will generate the package log.json. So I executed npm install. Now you know in the front end folder I have package log.json as well. Now let's retry. I'll run the docker command again. This time I guess it should pass. Okay, looks like it is going well. So it is executing the last step of the first stage. Run npmci and run npm build and then it also executed the production

stage. It says now we have the docker image ready. Cool. So the front end Docker image is ready. Now it's time to go with the backend Docker image. Again, I'll go to the back end folder now. So this is the back end folder. I'll click on create a new file and docker file. Okay. So let's start writing. It will be the same thing as I told you. We'll follow the same architecture of the writing docker file. So this one here whatever I showed you first base image second step work directory third step copy the dependency

files fourth step install the dependencies fifth step copy rest of the files sixth step execute I mean in all the applications it is not possible to execute but if there is a chance execute it and then go with the final stage okay so we will start writing the back one I'll go back to VS code first base image from node col 20- alpine we'll give an alias to this as well as build abishek why did you choose the same base image for front end as well as back end when you are working with JavaScript

based applications it's most likely you are using the front end back end everything in the JavaScript family where you would use npm as your dependency manager. So that's why we are using node as a base image. Okay, let's say you are using JavaScript for front end like React and you're using Python for your back end. Let's say you are doing that then in the Python back end you have to change this base image. This would become Python. Next step work directory you can go with / app. Next step, copy

the package.json. Along with that, copy any similar entries like package log.json and anything else to the dot that is this particular directory the / app. Then we will install the dependencies run npm CI. Okay. And again npm clean cache hyphen -y force amp percent amp% this one. Cool. So we are installing the CI dependencies we are using npm class clean force and for npmci you know if you're working with the application in production environment go with hyphen only production. What exactly this is doing? So sometimes you

might have um you know dependencies related to production, dependencies related to dev staging. So just say hyphen - production and for the back end uh especially in this case you don't have to copy the rest of the files and create the back end. At this point what you can directly do you can go with the production stage itself. So hyphen hyphen uh sorry star hyphen production stage where I'm just providing an alias to the production using node alpine image. But Abishek why are you not executing the dependencies

here in our case? Oh sorry why are you not installing the application by building it? We don't have to build the back end because our back end is very very lightweight. We can directly execute the back end in the production stage itself. Instead of creating the backend build in this stage and copying it to the production stage if it is lightweight, you can directly do it in the production stage itself. Okay. So we will create a user and a group. Last time we did not do it because we used the engineix base image.

In the engineix base image we already have something called engineix user. So we did not bother about it. Right now we want to follow dev sec ops practice. We want to run the container as an onroot user. So I'm just creating a user in this docker image. And then we will install the required dependencies. Then we will create a work directory. In this case /up is a work directory. We will copy the source code along with node modules. Where did you get the node modules? You ran the dependencies here.

So we are copying the dependencies. The node modules in the case of node family. The dependencies are called node modules. So we are copying it to the final stage along with the source code. We will use the app user the the one that we just created here to run the container. So nonroot user called app user. We will expose the port expose 5,000 and finally in the entry point as well as cmd we will directly execute the js image src index.js. I hope you understood this point. Why are we not running the npm build command

in the first stage? Because we just have a single index.js file very very lightweight one. It doesn't make sense. You create the binary here then you copy it here. If you just have to run this single command, you can directly do it in the production stage itself. Okay, cool. So this is our back end. Let's try to verify this is also working fine. Intentionally I did some security mistakes in this docker file using the old image. using the old image here as well and even the dump in it the

one that we are the package that we're installing this should also come with a specific version like 21 22 we should not be using latest because the behavior keeps changing on every docker execution let's say today the dminate version is 20 tomorrow it is 21 day after it is 22 every time you build a docker image there will be the difference in the behavior so Always you have to pin the versions uh pin the images to a particular version just like how we did here. So I did three intentional

mistakes in this docker file so that our dev sec ops pipeline the one that we are going to build will fail on the docker file. Cool. Okay. So I'll go back to the terminal docker build t let me call this as backend colon uh latest dot oh my bad I have to switch to the backend folder let me do that and rerun docker build t backend colon latest dot. Okay, let's verify. Yeah, so the build execution is complete. We have docker file for front end and docker image for front end and docker image for back end as well.

Abishek, what about database? You don't need to create docker images for databases because databases docker images are already available in docker hub. For example, we are using Postgress. So just search for Postgress. You will find the Postgress SQL official docker images. You can go ahead and use them, right? Why you have to do things from the scratch? You don't want to get the Postgress source code. You don't want to create a Docker image for it. Instead, use that is already available.

Okay. So we created Let's go back to our uh status. Where exactly are we in? So we ran the application locally. We created the infrastructure. We created the Docker files. We created Docker image for front end back end as well. For DB it's not required. We will use the one that is available. Now it is time to create a docker compost file. But Abishek why is docker compost needed? Very simple. When you are working with multiple microservices for a company or for a project in the company, it is a good

practice to create a docker compost file. Why? Individually in this case we have three containers, right? What if you have 10 containers or what if you have five containers? Every time there is a change in the Docker file, every time you want to build a new version of your application, you cannot keep running 10 different Docker files, right? And in fact, once the Docker images are created through the Docker file, in this case, we created the front end image, we created the back end image, and we are using a

DB image. But how will they interact with each other, right? How will you make sure they are same in they are all in the same network. How will you make sure front end can connect to the back end? Back end can connect to the database through the password. Right? Your database will have a password. Right? So you have to provide the password as an environment variable or you have to provide the password through some configuration. What I'm trying to say here just by creating the containers your job is not done. Your

responsibility is to make sure they all come up as a project. All the images talk to each other. They are within the same network. They have the volume requirements. They have the environment variable requirements. So all of that you can do using docker compost file. It is a one single file that helps you run multiple Docker images in the order of dependencies and with every setting and configuration that is required. Who has to write the Docker file? Docker compost file. You have to do it just

like how you created the Docker files. You have to create Docker compost file as well. Is it complicated? Not at all. There is a simple structure for the Docker compost file. I'll write it again for you. So to explain it in the easy way. So the structure of the docker compost file you will start with services. What are services? Front end, back end, database, the microservices. So under the services just start with let's say front end service. Okay. You will provide the configuration of the

front- end service. You can just say either provide the docker image directly or just say from where it can build the docker image. So just provide the path of the docker file along with that any environment variables that you want to pass. For example, the API tokens, password, secrets, anything that you want to pass and any volumes that you want to deal with. Once you're done with the front end, similarly create under the services. This is basically a YAML file. Under the services object, you created front end.

In the same way, create one for the back end. Again, either provide the image directly or provide the location of the Docker file. Then the environment variable, the password to connect to the database, etc. Finally, TB. Okay. So even for the DB you will provide the configuration and then at the services level you will create a network. If you want to create a new network you will create a network for all of these containers. If you want to create any volumes at the services level you will also create volumes that are

required. That's it. If you do this your docker compost file is ready. And when you execute docker compose using docker compost up this is a command to run. Once you run docker compost up all the containers will be started and your project will be running on your local machine. This is very very beneficial for QA engineers, developers. If they ask you, Abishek, I want to set up this project on my laptop. Just tell them install Docker, execute this Docker Compost file using Docker Compost up and

you will have everything running on your machine. You don't need to know Docker. You don't need to know how to run how to read a Docker file. You don't need to know anything related to Docker. Just install Docker and run this command. Docker compose up. That's the beauty of Docker Compose. Cool. So, how will we write the Docker compost file? So, in this main branch, you can just create a Docker compost file at the parent level folder, right? That is at the at the root of the repository here. So

we will have to create a docker compose file. Okay. So here let's create a new file docker - compos yiml or yl it's up to you. Let's start. So we will start with the services as I told you. What microservices are we dealing with? We'll first deal with the database micros service. So DB for database micros service we are directly using the image we are not using the docker file right. So let's just say Postgress what is the docker image that we're using col 16 - alpine I'm using an older

version again for the same reason but if you want you can just go with the latest version okay and then the name of the container if you want a name for the container that is created you can just say container name I'll just say journey- tba So docker compost file can also create the images for you just like how we did by running docker build t right you don't have to do it when you have docker compost it can not only start the containers but it can also build the images for you right in this case I'm

just providing the image name directly if I provide the docker file here it will create the image what we will do For front end and back end, let's give the docker file. Although we created them already, but just to show you for front end and back end, I'll give the docker file to docker compos. But for database, I'll directly provide it with the image. Cool. So image name container and there is also a cool feature of docker compost. For some reason, if your container goes down, you can say restart

the container. So unless stopped for some reason do not restart. So if my container goes down maybe because of resources because of CPU memory anything just restart the container. This is a cool feature the one that you get with Kubernetes as well. Then the environment variables um definitely we need environment variables for the database. So I'll just say the Postgress username Postgress password and the Postgress DBA. Why you need this? Obviously, you are starting the database container. So, the

database container will have the Postgress instance and the Postgress instance should be created with some uh schemas or it should be created with some uh tables. For all of that, you need a database user, database password and name of the DB that it has to connect to. Then the ports uh we usually have only one single port when you're dealing with database. So what you can do? You can map the port of the database that is running within the container to the port of your local host. How can you do that?

Let's say the port is 5432 which is the default Postgress port. We will map it with port 5432 on my local machine as well. Again you are dealing with database. So you also need volumes. So volumes only for database the number of steps are usually more. So volumes just provide the volume mount. So PG data is the volume that I'm mounting to this specific location within the container. But Abishek where will you create this volume? Like you haven't created the volume called PG data. We will do it you

know after the services definition is complete. What I'm going to do at the same level here I'm also going to create something like this volumes pg data driver as local. When you add these fields in the docker compost file, it is exactly same as docker volume creation command using your docker cla. Right? Docker compost file will not do anything extra. It will do only things that are supported by Docker CLA. But only thing is that we are not running the Docker CLA commands. We are putting them in the YAML manifest

so that all the commands can be executed at once. This one here is running the container for you. This one here is creating the volume for you. Similarly, I will add another container for front end. Another container for back end. All the steps will be executed in the same order. Okay. unless you maintain the dependencies. If you want, you can also add some health checks. So, I'm going to add the health check here to just make sure the database container is started and everything is good. This is optional

only if you want. Now, similarly, let's go with another service. So, I'm going with the backend service. Now, I'm done with database. I'll start with the back end service. So here at the same level I'll just say back end and as I told you for back end instead of directly using the docker image that we created locally I'll provide the build context I'll say build context. I'll provide the location of the docker file. It's in the backend folder. Right? Docker compos is here. This is the

backend folder. Inside the back end folder, there is the docker file. So I'm just providing the path of it. Okay. Then also say the name of the docker file because in this folder it has to know what is the name of your docker file. I'll say the same name docker file. If you have a different name you can go with it. Then just say the here let me just fix the indentation a bit. Okay. So next thing the name of the container container name. I'll just call it as journey backend. I'm following the same nomin.

I'm calling the database name as journey db. I'm calling the backend container name as journey_backend. What next? We'll follow the same practice restarting the container unless stopped. Okay. Environment variables even this time we have to pass the database related environment variables because back end will connect to the database. So I'm saying environment 5,000 DB username DB password everything that is required expose the port. So eventually back end should be connected to the front end. So I'll expose the

back end on the port 5,000 and docker compost should first start the DB container only then it should start the back end container. So I'll use this depends on tag something like this depends on colon db. Now this says backend container will only be started once the DB container is ready. And also I'll just say there is a condition not only DB container is started but the condition is that DB container should also be healthy. So service healthy I hope this is clear the depends on tag says that only start the DB container

once database is completely ready and because you are providing a condition also it will wait till the service is all good and some security related steps here so security where I'm saying no escalated privileges required just run the back end container with the user that is provided in the docker file. Now finally front end. Okay. So even for the front end we will use the same thing name of the service. I hope the indentation is correct. Yeah. Here for YAML you have to be little careful with the indentation. Front end

of course I can write this entire file using some plugins in Visual Studio Code. I can write it by taking help of uh GitHub copilot but I'm intentionally writing it so that you also understand what steps are involved. Build either provide the container image or just say context dot / front end folder docker file is here. What is the name of the docker file? The name of the docker file is with the same thing docker file itself. name of the container. Let's call this as journey uh front end. This should be the name of my

container or hyphen. Did we follow hyphen or did we follow underscore? Let me see that. Uh okay. So here we used hyphen container name journey db. Here we used underscore. Let's follow the same standard journey hyphen back end, journey hyphen db and journey hyphen front end. Restart policy uh only unless stopped. Then port mapping what ports do we want to map while running this container? Front end is running on port 80, right? So I'll just say 80 80. So this is the internal port mapping. So port

mapping of the application running inside the container and my local machine. Okay, this is also well indented. All good. Now depends on is there any dependency for front end? As you can guess a front end without back end doesn't make sense because everything that we are seeing here all the information should be populated from the database. It should be populated from I mean the logs that are created it should come from database and back end should fetch it from the database. So your order of starting the

containers you should first start database then you should start back end and only then you should start the front end. So that's why depends on back end where back end is internally dependent on the database. Just provide the security option one more time in the front end as well so that your front end does not run with escalated permissions. There you go. Your docker compost file is ready. That's it. So this is how you write the docker compost file. When you look at it initially you might feel that

oh this is a 66 line yl file but we just wrote it in 10 minutes and there is no additional thing here. the same fields that you have to follow the services field which will have the list of microservices and each microser has the same fields within the DB image container image restart environment ports volumes health check only if needed and any security related thing even in the back end build container name restart environment expose depends security settings. Same in the front end as well. So this is where people ask me

the question, do I need to learn docker compos file? Do I need to learn uh docker file? Will I be asked in the interview to write docker compost to write docker file? Answer is simple. Just remember the structure. See, nobody is a superhuman. Nobody can remember everything. But if you remember the syntax, if you remember the fields that are part of important fields that are part of docker compost, docker file, kubernetes manifest, that is good enough. Explain them. This is how I'm going to write the structure of the

docker file and you will definitely clear the interview. Cool. Now let's verify by running this docker compose as well. What should this docker compose do for us? one step one it has to build the docker file for front end and back end to create docker images. Okay, even before that it has to pull the database related docker image and then it has to build the docker files for uh front end and back end. It will first start with the database as per our order that we provided. So once the database image is

pulled then it will start the database container with all the settings that we have provided the environment variables the security settings the health check settings and once the health check is successful then it will go to the backend service and in the backend service as I told you it will first build the docker image then it will configure the environment variables it will see if it can connect to the database And if it can connect to the database successfully again it will start with the front end. Cool. Now it will create

the front end image and it will verify if front end can interact with the back end. That's all. This is what docker compose is going to do. How can we run docker compose? Take the terminal at the same location of the docker compose file. We are not there. It is somewhere here. Okay, this is the location of the docker compose. At the same location, just execute this docker compos yl file. What is the command to do it? Docker compose up. Let's do it. So going back, if you already have uh images that you created

using docker compost, you can do this. Docker compose down. So first docker compost down is going to delete everything for you. Of course I ran this application previously. So that's why I ran docker compost down. Now I'll run docker compost up hyphen d hyphen d will start these containers in the background and you will not see the logs of the container when they are starting. Okay. So it is taking time to pull the uh journey DB container but otherwise rest of them because there is some

docker caching on my machine previously executed it that's why it was very very fast. Now let's verify okay Abhishek ran the docker compos as well. Now let's try to see if on my local machine I can access the application. Okay. So all the containers are started the front end, back end, database, everything is configured, everything is connected to each other. So let's verify if the application is running through the docker compost. On which port should we access it? Front end is running on port 80. So you can

see here we expose the front end port to 80. So we will request the front end. So request goes to the front end container. From there request goes to the backend container. From there request goes to DB container. We don't have to bother about it. We will head to the local host. I'll take a new tab local host. I'll not provide any port because the port is 80. And click on the enter. There you go. So we have the application running on port 80. Okay. How is that possible? That is possible through docker compost. Now if

I click on anything for example new post so we got this emojis we got this basic information this is coming from the back end so this is coming from the database and back end fetch this information for us so overall the docker compost part is also successful where are we right now let's go back to our initial stage the one that we documented the list of steps we are done the application running locally. Infrastructure, Docker file, Docker images, Docker compost, Kubernetes cluster creation. Now only thing that we

are left with is Kubernetes manifest creation which is not going to take a lot of time and CI/CD pipeline setup. Okay, let's even take a look at them step by step. Let's start by listing down the Kubernetes manifests. Right? So what all resources that we have to create in Kubernetes from docker compose it's clear what we have to create. We have we are done with it. But for Kubernetes we have to create few more resources starting with okay let's start with the obvious resources that we have to

create. So there is front end there is back end there is database. For database, deploying database to Kubernetes requires dedicated resources. First, you can create the deployment instance. So for database, you can either go with deployment or you can go with stateful set according to your requirement. In this case, deployment is fine enough. But along with that, you can just not create the deployment uh for the database. But along with that database also needs a volume right it needs a persistent volume. How can you

create that? So with deployment you have to create a persistent volume claim and you have to create a storage class. This is how you create a database within the Kubernetes cluster. You have to create either deployment or stateful set. Then you have to create a persistent volume claim that is going to create a persistent volume for us. But for that persistent volume claim looks for a storage class. Let's try to understand what is a storage class resource. Let's say you are creating this on EKS. The way which

we are doing on EKS cluster you have volumes like EBS, right? EFS, AWS, FSX solutions. Which one is your Kubernetes port going to use? In this case, which one is the Postgress going to use? You have to provide that information. So you can say I need 10 GB of volume, but do you need EBS volume? Do you need EFS volume? Do you need FSX volume? That is defined by the storage class. So as DevOps instance whenever you are dealing with databases in your organization just first create a storage class in the Kubernetes cluster

that will tell any database deployment that it has to get the volume from these particular options. If your storage class is EBS then persistent volume claim if someone creates a persistent volume claim of 10GB right the person volume claim as a storage class storage class creates a EBS volume of 10GB. Similarly if someone creates 20GB same thing EBS volume of 20 GB is created. Now once the deployment related things for database is done you have the front end. So for the front end you will create a deployment right plus you will

create a service for the back end again you will create a deployment and you will create a service but we want to do it in the dev sec ops way right so we want to secure everything as much as possible so for the backend deployment let's say this is front end this is back So for the back end we will create a network policy as well. Network policy in Kubernetes creates an admission controller. Anyone who tries to connect to the back end. Network policy first intercepts the request and it will see

if it should allow the request to back end or not. But Abishek why do we need a network policy? Very simple. Your back end should only be accessed by the front end. Why should your back end be accessed by the hacker let's say or some other part in the Kubernetes cluster? That should not be the case. So we will create a network policy and we will only accept the request if the request is coming from the front end. In the same way even for database we will create a network policy. Any cases who should

access the database it should be back end. So in the network policy for DB we will say allow only requests from back end. For front end it is not required because front end is exposed to the internet. You don't have to do anything. And then for the back end we will also create a secret secret so that it can sensitively connect to the database. So the database credentials are stored within the secret. These are the resources that we are going to create. Let me list down here one more time. So for the front end

we will just create deployment. Then we will create SVC. This is for front end. For the back end we will create deployment. We will create SVC. We will create database secret. This can be for database instance as well. Plus we will create a network policy. For database we will create a deployment. We will create SVC. We will create persistent volume claim. We will create storage class as well. So these are overall resources that we are going to create. I might have missed one or two. Uh I think I

haven't missed but you know if I missed one or two while creating the Kubernetes manifest we can recolct that. But overall these are the important services resources. Now you have two options. one individually you can create YAML file for each of them but because we are doing the demo it will be light little difficult to create one to like almost 11 resources. So what I'll do I'll create a single YML file. Okay, it will be easy to deploy as well. I'll create a single YML file and I will call this as

journey file. Okay, in the journey doyamel file, we will have all of these resources. So, it is going to be a very huge YAML file, but that's okay. Okay, so I'll go back to my VS Code now. I'll close all the tabs. I'll just maybe create a folder called K8 here. You don't have to do it in the main branch. Okay, in my case, I'm just showing you by creating this in the main branch. But during your learning process, you know where all these manifests are. Once again, if you head

to the DevOps branch, in the DevOps branch, you can find all of them in place. I created this branch for you. CI/CD is also running. Kubernetes manifests are in the working state. Docker files are in the working state. Docker compos is in the working state. This is your blueprint. At any point if you're struck, head to the DevOps branch. Look at the manifest here and once you're comfortable, do the project without looking at this manifest. Cool. So, journey.l uh I think I just messed up a bit. Let

me move this to trash. And inside this, let me create a new file. Journey. Okay, perfect. So now you can start in any order in Kubernetes it does not matter if you are following health check you can follow any order. So I'll start with creating the resources. First thing I'll create a name space. Okay this is one thing that I forgot to create a nameace I'll use this syntax. Okay. So API version is v1 kind is nameace and I'm just providing some basic metadata the label for the nameace and uh the

name of the namespace then we will create a secret for the database password okay so this is the one that I'm creating the database secret this has the database username this has the database password Cool. So this is also fine. Next thing, how did you get this database username database password? We already have that, right? If you look at docker compost file, we have the database username, we have the database password. Just encode this in the base 64 format and paste it here or just copy it from here as well.

Okay. Then as I told you, you have to start with creating a storage class. So for storage class we will be using the EBS storage class which is very easy. Um when you use EBS storage class as I explained here if you create a percent volume claim for the database the percent volume claim will talk to the storage class and storage class within your Kubernetes will create EBS volume with the required size and this is the storage class YAML manifest name of the storage class I'm saying it should be EBS volume type should with

GP3 in EBS you have GP2 GP3 types so I'm saying GP3 is the type and I'm just saying reclaim policy as retain cool then comes persistent volume claim before creating the database related deployment I'm creating persistent volume claim and storage class as well and if you notice here I'm saying my database would need 10 GB of EBS volume cool Now the actual database deployment. So for the database deployment we will use a posgress official image because it has health checks also. You

might find the database manifest a little big but in the spec we are saying the replica count is one. We are following the rollout strategy and then we are defining here. So this is going to be the container name of the container is posgress. We are using the same image the one that we used in docker compost posgress 16 alpine. The container port is 5432. We created a secret file. We are using the secret reference uh in the deployment so that it can access username password and this is the volume

mount for the storage class that we created. So 10 GB of EBS volume will be mounted in this particular location. We did in fact same thing with docker compose as well but in the docker compose we created a local volume whereas in the kubernetes manifest we are using ebs volume. Okay then now we are done with the postgress database deployment. Let's create a service for postgress. This is the one. So in the service I'm just saying the target port is 5432 and the port the service should be accessible is also on

5432 that this is perfect now let's head to the backend deployment labels are important okay I'm not forc focusing a lot on the labels but take some time look at this manifest see how I define the labels it is unique for every resource that I'm going to create especially this is unique for the database deployment, the backend deployment, the front- end deployment because in Kubernetes service identification happens through the labels and selectors, service discovery. Okay, now let's go with the backend deployment.

You might have a question here, but Abishek, when we are dealing with the backend deployment, how did you come up with the image name? Okay, because here we created a docker compos image that is fine but the image that we created is still on your local machine right if I do docker images here I have the image but this is not pushed to the docker hub right journey backend latest or abishy journey backend latest this is not pushed to docker hub yeah we have to do it okay first of all we have to define

that what I'm going to Journey front end latest is the image right. So this is the one that we created through docker compost. I will tag this docker tag this one as abishek fi which is my docker hub username followed by the same thing journey uh we copied for the back end. Sorry this is the front end. So I'll just say journey front end latest. I already did it because I wanted to I mean before doing this entire demo I verified it on my Kubernetes cluster. During that time I already tagged this

image to abishek fi journey front end latest. I tagged this image to abishek fi journey backend latest. So both of them are already in my docker hub. I'll show you. So this is dockerhub. Go to abishek fi. Okay. So these are my repositories here. If you search for or directly let's search for abhishek fi journey front end you would find it. See it is pushed. Okay. And I verified it as well. Similarly I pushed journey back end also. In your case, please execute these commands. First, tag the

docker image. Instead of abishek file, you should go with your docker hub username and then just say docker push followed by this. In case you run into any authentication issues, first run the docker login command. I'll repeat this. With docker compos, we created the images, but the images are on our local machine. What you ideally need you need these images to be pushed to the docker hub only then you can access them within the Kubernetes cluster right so first do the docker login on your machine then tag this image the one that

you have on your machine now is journey backend latest tag it to your username/journey front end latest journey backend latest and then use a docker push command to push it to docker hub. Perfect. Without pushing it to docker hub, if you try to run the kubernetes manifest, you will see the pods are in the error state. Okay. Perfect. So now just like back end, let's copy the front end as well. Okay. Uh my bad. Before that, let's copy the uh from the GitHub repository. Let's get the backend service configuration.

This is the one. Okay. So in the back end service also what is important? The target port and the port that you're going to use. Then let's copy the front end configuration. Where am I copying it from? I'm copying it from our GitHub repository itself. Okay. This is my front end deployment configuration. Okay. So I have the front end deployment configuration. Similarly, front-end service configuration as well. How are we going to expose a front-end service? Abishek uh end of the day your

Kubernetes cluster is in EKS. It is within a VPC. How are we going to access the front end that is deployed? You have two options. Either you can create an ingress or gateway API or you can port forward the service. We will go with port forward. Right? The service that is created we will use cubectl port forward command and we will access it. If you want to use ingress or if you want to use load balancer service type you are free to do it as well. Finally network policies that I mentioned. This is the

important part. This is the dev sec ops part that we are dealing with here. Okay. So, oh my bad. I was copying in the journey ML file. I'll fix it. But the network policies are very important. So the network policy for back end and network policy for database. You have to explain this in the interview when someone asks how did you secure your Kubernetes cluster. Network policies is very important. Just say whenever we are using database for database resource we are creating network policy. It uses the admission

controller concept and it will only allow requests from the backend part. Where did you define this in the network policy? If you look carefully, this is a network policy for database. Okay, so network policy is applying for the journey DB which is my database pod and it is only allowing incoming requests. Ingress means incoming requests from the journey back end. It totally depends on the label. And where is this label? This label is on the back end part. Okay. Similarly, network policy for the back end. You can

see here this is this network policy is applicable for journey back end and it will only allow the request from ingress from journey front end. That's what we did. Cool. So this is about the Kubernetes YAML manifest. Now I'll just execute the Kubernetes.l and within few minutes we should have all the resources on Kubernetes. Let's run the command. So where we have the Kubernetes manifest, I'll just head to the K folder and run this command. cubectl apply - f journey dot yamel. So this should set up

all the manifest on our EKS. Again this will take uh maybe 1 to 2 minutes. Uh it might take more as well because we are using uh EKS auto mode. Every time the pods are created, the resources are created, Carpenter has to scale the cluster. Let's see. Uh I'll refresh the page. The resources should be in the pending status at least. Yeah. So DB has started but backend resources are still in the pending state. That's good. It's actually very fast. We have the front end running. We have the

uh backend related components running uh in the pending state. And we have DB also. Good. Cool. We'll only select the journey name space. We are not worried about the other ones. There you go. All the resources are in the running state. In fact, it's much faster than the than I thought. Maybe because I already created a uh engineix deployment. So, Carpenter created a pod. It created a node for us. It is reusing the same node. I guess if we do cubectl get nodes. Yeah. So the same node is being reused

the pods that we created they actually have enough resources on this node. That's why it was faster. If you are doing it for the first time if you haven't done engineext deployment don't worry this might take up to 5 to 6 minutes as well because carpenter has to create an EC2 instance as a node for you. Cool. Okay. Now we can verify. Uh let's see the services. cubectl get svc - n journey. I'm only looking for the front end service because this is what I'm going to access. Let me cube port forward.

Cubectl port forward followed by svc. I'll say journey-en front end. If it goes wrong, we'll fix it. We don't have to worry. Followed by the port is 80. I'll expose a very different port. Let's use 6666 colon 80. So, I'm mapping this port of the service to port 6666 on my local host. N journey address 0.0.0.0. zero. I'm mapping it with the local host. Let's see what happens. Okay. Uh the port forwarding is good on the local host. Let's do this. Local host colon 6666.

Address is restricted. So address uses a network port which is normally used for the purpose of web browsing. Okay. Uh that's fine. Let's change the port and see. uh instead of 6666 I'll use a different one. I used 8172. The same command I just replace 6666 with 8172. And this time on port 8172 I can access the application. In fact this is new to me. I was not aware 666 port is restricted. Good to know. Anyways so we have our application running in Kubernetes as well. Please take some time through go uh to go

through the Kubernetes manifest while I explained you it's not that easy especially lot of people have questions about database how to deploy database in Kubernetes is it possible it's definitely possible in fact it's not a rocket science you just deploy it as a basic deployment only thing is that additionally you have to create persistent volume claim and you have to create a storage class okay now finally we are only left with one thing. You all know what it is. It is the CI/CD process. But even till now, we

considered a lot of security best practices. In fact, in every step we did that when we started, we started with infrastructure creation apart from running the application locally. During the infrastructure creation, we did not directly create EKS. We consider security best practice that is we created VPC. Okay, let's write it down here. What all security measures did we take till now? One, we created VPC virtual private cloud. Then we created private subnet. We did not expose Kubernetes API to outside.

Okay, this is about infrastructure creation. Then we worked on the Docker files. While working on the Docker file, we used multi-stage build. Another security best practice. We ran the containers as nonroot users. Then in the docker compos as well, we took care of the secrets, right? And then when we were dealing with Kubernetes manifest, we used network policies for security best practice and to make sure DB is not compromised with any other external access. Similarly, back end as well. Yeah. So these are some

things that we took care till now. The final thing is CI/CD pipeline and usually in CI/CD you can do a lot of things in the CI/CD pipeline the dev sec ops practices let me first tell you what are the things that we're going to consider okay even before writing the CI/CD pipeline the first thing when it comes to GitHub action I mean when it comes to this pipeline we are going to use GitHub actions why GitHub actions is very very popular. It is gaining a lot of significance and especially for

public projects. This is the go-to solution. If you're hosting your repository in GitHub, I would say just go with GitHub actions. It can be GitHub enterprise or any kind of GitHub installation. Then within the GitHub actions, we will use multiple stages in our pipeline. Today we will start with the basic linting check. Linting check is basically like checking the semantics uh syntax related things verifying if there are any additional spaces or any basic security failures. Then we will look at I mean then we have

a stage called SC software composition analysis. This will verify if the dependencies that you used in the application build process any of the dependency if it is vulnerable if there is any CV attached to it or if there is any security threat with respect to the dependency version that you used. It can be dependency related to your front end back end everything will be covered. After software composition analysis, we will look into another stage where we will build the docker image and during the docker image build stage, we will

create the images for front end as well as images for the back end. Once the image is created, we will perform image scanning where we will go with treeway another security best practice. And once this is done, we'll also uh perform lenting on the docker file because in the docker file you can go with a lot of security inefficiencies or you can do issues with respect to docker not best practices right and then I'll use another thing called checkout. Check out is for security scanning of the

manifest. If you followed our dev secop zero to hero playlist, all of them are already covered. You know, I'm just taking that extra effort in this end to end CI/CD pipeline to cover all the steps in one place. We covered check out in our dev sec ops 0 to her. We covered image scanning, we covered SCA, we covered linting. But anyways what checkout does it performs security scanning on the DevOps manifest for example the Kubernetes manifest the Terraform manifest but Abishek what can be issues

with the manifest I'll give you a simple example let's say using Terraform you create an S3 bucket maybe because you are new to Terraform you copied the manifest from somewhere you are creating S3 bucket but eventually the S3 bucket will be public because of the configuration that you used. Now if you create this S3 bucket in production and if you have some information in the S3 bucket because of your lack of experience the bucket is public it is exposed to the hackers. Who can prevent this? Czechov can

prevent this. So check out will scan your terraform manifest. It will say hey your terrorform manifests are good but they are not very secure. If you execute them, it will create resources that are not secure. Similarly, it can go through Kubernetes manifests as well. Maybe used latest tag for the image. It will say, "Hey, latest tag is not good. Go with a particular version or you used maybe you used health check where you did not follow a particular standard. This is where Czecho comes into picture.

And then we will update the Kubernetes manifest file. So we created a Kubernetes manifest file, right? When the Docker image is complete and when all of these steps are complete, the Kubernetes manifest should be updated with the new image that is created. Right? Developer created a pull request, all these steps are executed or developer created a commit, all these steps are executed. Obviously a new version of the Docker image is created. You have to make sure the image is updated in the K8 manifest and then

commit the K8 manifest back. This is what we are going to look at using GitHub actions. Cool. So let's go step by step. There are close to 10 to 11 uh jobs in the CI/CD pipeline that we are going to write but that's okay. I already have the workflows here. I'll just delete it for you so that I'll do it from the scratch. Okay. Uh create a folder called GitHub. This is the first thing that you do. Within the GitHub, create a folder called workflows. Okay, let me do it. New folder,

workflows. And within this you can put anything as the name of your workflow file. I'll just call it as ci.l or you can say cicd.l or you can say anything else as well. Cool. What is the first thing that you write in the uh CICD file GitHub actions file start with the name of the workflow. I'll just call it as journey dev sec ops workflow. Cool. Now on which action the CI/CD should be executed? I'll say execute it on the push action on all the branches in the repository. Okay, if you only want the CI file to

execute on a particular branch, you can decide that as well. But I'm saying on all the branches and then I will say execute it on the pull request as well. And again pull request on any branch. Branches colon star. Good. Okay, this is perfect. Even if someone copies this CI/CD pipeline because you used star as the branch, it will work for them. Next thing. So the next thing is to define the environment variables. If you're using any environment variable within the GitHub actions, this is the

right place to define it. What I'm going to do instead of using the Docker registry, see when we created the Docker compost file, when we created the Docker images through it, even through the Docker file, we got the images locally. We tagged them and we push them to the Docker registry. But in real time when you work in your company you might not use the docker registry. What you might use you might use ghcr GitHub container registry. So it is a best practice any image that is created through cicd

pipeline we will also use github actions container registry. For that you just have to provide three variables here. One name of the docker registry ghcr.io Just like docker.io name of the back end image gcr.io followed by github repository name. So what will this turn to? gcr.io followed by journey which is the name of my repository followed by journey back end. So in this particular location the backend image will be stored in the GitHub records. Similarly this is a repository for my front end image.

Cool. So we defined this environment variables. Now the actual thing jobs each thing that we discussed here is basically a job. Linting is a job. SCI is a job. Build is a job. Scanning again linting of the manifest files. Check out update commit everything is a job. So you have to define the jobs field in YAML. And you can start with stage one. You can use something like this to say. Now for easy identification you can use a comment and say I'm starting with the first stage that is the lint stage.

Basically when it comes to GitHub actions just like docker compost file there is a specific syntax that you will follow uh you might write 10 you might write 15 you might write 20 but you will follow this step where the first thing that you do let's say we are dealing with the linting job you will provide the name of the job anything then you will say what is the runner for this in GitHub actions you have two things right one is self-hosted runner and one is the runner that you can bring personal runner. Okay. So if you are

using the self-hosted runner uh sorry the hosted runner and the self-hosted runner hosted runner let's say you want to go with the GitHub hosted one you can say Ubuntu latest. So it will spin up a runner for you and this linting job will run on this virtual machine. You don't have to create it. You just have to say what is the operating system that you need a distribution that you need and then if you want to continue this job on error now in our case we have 10 different jobs right do you want to fail

the entire CI/CD pipeline if this job is failure or do you want to continue I want to continue because I know my CI/CD pipeline will fail so I'll just say continue on error as true. Okay. So this will pass uh I mean this will not pass the particular job but it will move to the next step and then start with the steps. That's it. So these three are common for all the 11 jobs that we are going to create today and within the steps we have the order of the steps to follow. For example, to perform linting, I'll just

say what is the name of the step and I will use a GitHub action uses the check out action. So check out action will check out the code for us. Then next step I will set up again every time you create a step provide a name for it and use the GitHub action. You don't have to write the code. There are hundreds of GitHub actions out there. For example, to set up NodeJS, you can use this GitHub action action/ setup node. This will install NodeJS on the virtual machine. So, this will check out the code. This will install NodeJS.

Then, we will install dependencies, right? And finally, we will run the linting check. Okay, let's put this in action. So, first for linting, this is how we are going to write. Okay. So these are the things paste it. So linting name of it lint code runs on Ubuntu latest strategy matrix for component back end and front end. That means execute the same thing two times. One is within the back end folder and one is within the front end. I mean for front end as well as the back end application. What do you

need to execute within the steps? You define that Check out the code. Set up the NodeJS and once the NodeJS is set up, install the dependencies and run the linting. Okay, my bad. So here I'll just say install the dependencies and run linting. That's it. This is the first job. Similarly, we'll proceed with the second job. So the second job is to uh verify software composition analysis to verify if there are any dependency issues. For that this is the job. Okay, let's take some spaces and this is the

one I'll walk you through them. SCA so I'm saying name of the software composition testing as dependency audit runs on Ubuntu latest needs linting that means only execute this step after this one I'm just defining the order okay so this will be the first one that is executed this will be the next one that will be executed in the strategy again front end and back end because we have two microservices in the same repository instead of writing the same states It's two times we are saying for front end and for back end

and this is a folder name for front uh front end. This is a folder name for back end. This will be replaced here once for front end package.json once for back backend package.json. Okay. So check out the code. Setup NodeJS. Install the dependencies and perform the auditing on the dependencies. Okay. perform auditing on front end dependencies and backend dependencies. How will you do it? Using the npm audit command. If you're using Python, you can use pip audit. Right? If you're using Java, you can use a different command.

If you're using Go, you just have to change the command. That's it. Okay. Next thing, the build building of the Docker images. to build the Docker images. These are the steps that we're going to follow. Let me quickly put them here and explain it to you because this is huge. If I have to write it, this will take a lot of time. In fact, this might take up to 3 to 4 hours. So, instead of that, I'll walk you through the steps. First thing, build is the name of the job. Again, runs on Ubuntu needs sea. So

I'm defining the order of the jobs to execute matrix front end as well as the back end. So front end docker file and back end docker file. Two times this will be executed. Check out the code. Set up docker on the virtual machine. I mean install docker. Then log to the github container registry just like how we docker login. Then run the docker image. basically create the docker image and this should be the name of the docker image. If the matrix is being executed for front end the name of the

image would be gh.io front end journey front end. This will be the name of the image. If these steps are executed for back end gh.io journey followed by journey back end. Okay, cool. And then we are just pushing the images to the gcr.io again for both of the things front end as well as the back end. Once the docker images is complete, we have to scan the docker image for which we will use and these are the steps for TV image scanning. Not only TV, there are multiple options that you can follow but

TV is very very popular. So the name of the job is image scan where we are choosing a runner. For every job you have to choose a runner. Okay. Every time you create a job you have to go with a runner. This time the runner name is again Ubuntu latest steps. Check out the code. You must be seeing something. The steps are quite repetitive. Every time we are checking the code every time you know we are following the same things like choosing a runner continues on error. Yeah, that's what GitHub actions is all about.

That's why I say if you practice two to three GitHub actions on two to three applications, your job is done. You will never forget the GitHub actions again. At least the structure. And this time we are using tree. So this is the action for trib where we just have to provide the image name journey front end journey back end and it will perform the scanning and it will tell you if there are any critical issues if there are any high severity issues on the image that we created. Okay. Then once the image scanning is done we will

perform security scanning on the manifest. As I told you using check out you can scan kubernetes manifest you can scan terraform manifest that is what we are going to do security scan is the name of it Ubuntu latest check out the source code in the first step then install check out on the virtual machine using this action check out action and we are providing the location of the directory first perform on the terraform directory Okay, run the check out command on the Terraform manifest and let us know if there are

any issues. Then run the check out on the Kubernetes manifest within the K8 folder and let us know if there are any issues. Okay. Finally, we'll perform Docker linting before we execute the update Kubernetes manifest. What are we going to do in Docker linting? Just like source code linting, we are going to verify if there is any obvious issues in the Docker file. Right? Finally, updating the Kubernetes manifest because a new Docker image is created on the developers pull request. This is what we are going to do. Update

K8 manifest. For the K8 manifest, every time the image is created, what we will do? We will head to the journey. We will replace the name of the image here from abishek fi journey front end which is currently the name right on the repository. This is the current name abishek fi journey front end. What we will do on every action we will replace the name from abishek fi journey back end to the new image gh.io IO front end followed by journey front end and the GitHub commit id right so that every time a new image is created that is what

the script here is doing I have the script in the github repository it's basic sedd commands right so you can go through the uh sedd command and understand what I'm doing so this is the sd command that is taking the action cool now let's verify what I'll do is um on the DevOps branch. I will push a dummy commit and I will show you how a new image is being created and we will also see what are the security vulnerabilities on this application which we'll identify through dev sec ops

pipeline and also security vulnerabilities in the infrastructure manifest. So all these steps will be executed. All of these actions will be executed on a single commit and they will tell you if there are any security issues and also it will update the Kubernetes manifest with the latest image. Perfect. So let's push a dummy change here to the DevOps branch. I will pick up the readme file and let's add at the end of the manifest a simple white space. In your case also you can do the same

thing but on your repository. You cannot write on my repository or let me just remove this line. Okay. Go back and commit changes. Okay. So the changes are committed. Now the CI/CD pipeline should be automatically triggered. It's just taking some time to uh refresh. But the commit should be done by now. I don't know. There should there is some bug in the UI. There was an error. File could not be edited. Let's see why. Okay, I'll do it again. I'll go back refresh and uh okay I think the issue is with GitHub

itself. GitHub is not responding well. Let's try again in one of the files. Go back to journey. Go back to readme.yamel. Sorry, read me.md. Edit. And let me update the last line here. Oh, in fact the commit was successful. Is it? No. Okay. Remove this line. Uh, delete the white spaces and go up commit. Let's see. Okay. Instantly CI/CD pipeline is triggered. We committed to DevOps branch. So the changes are triggered on the DevOps. I'm keeping the main absolutely clean for you so that you

know you will look at the main branch. It will not have any DevOps manifest for you. You have to work on it from the scratch. If you have any questions, you can refer to the DevOps branch or you can refer to the video. Okay. So here CI/CD pipeline must be in progress. Let's refresh and see. Let's go to the commits. Uh so this is the one that we committed. remove personal node from readme.md. You can see that 12 jobs are executed, two of them are still running but out of these 12, two have already failed and

other things have successfully passed. Let's see what failed and what passed so that we get a very clear picture. Right? So what all things passed? The first thing that passed is the uh dependency audit on the front end. So the dependencies that we are using for front end in the package do.json it is saying you are all using the good version even for back end what we used in the package.json JSON what developer used is absolutely fine then in the front end application linting is good in the

docker file also linting is good update kubernetes manifest so it is saying the kubernetes manifest file is updated with the new version of the image let's see that but what failed IA security scan failed so it is saying there is some issue with the I think it failed for terapform let's see and Even docker lint has failed. Let's see why docker lint failed. So it says see there you go I explained this even during writing the docker file this will fail. So it says apk add package the one

that you used in the backend docker file. Okay you did not provide the version. This is what I was talking about. Yeah, I'll go to my this one and explain you in the back end docker file if you remember I said at this point of time when you're using apk add dumb binet you ideally have to pin it to a version if not you are doing a security issue tomorrow by default this is going to latest right if you're not providing anything so today the latest can be something tomorrow the latest can be other thing

so every time the docker image your back end is built it is different. So there can be a security issue. Today it is fine, tomorrow it can be not good. So that is what the CA also complained here. It said you have to go ahead and fix it. And what else failed? Okay, this is one security failure and apart from that another security failure is for docker linting. Uh let's see I security security scan as well. I think we just checked about the docker linting. Now this is about IC security scan where it said okay in the

terraform that you used there are it did not complain about one mistake it complained about several mistakes in the terraform manifest assertion error config passer. We used a list where it is saying you have to go with the string for which one? If you scroll up, you can find exact reference as well where exactly it failed. Cool. So this is for Terraform manifest. The check off failed. Then what else? The image scanning failed for back end as well as front end. The one that we already expected why we have been using

outdated versions in the Docker file. Just to show you it was an intentional decision. I used note 20 um the image which is vulnerable in the docker files both the front end as well as the back end note 20 Alpine note 20 Alpine that's why it said hey the TV said hey you are using old versions so your application is vulnerable to security so these are the four pipelines uh four jobs that failed and rest of all are successful now it is up to you and it is up to your develop ment team. If someone creates a

pull request, do you want to merge or do you want to ask the developer or DevOps engineer to go back and fix it? Every time someone creates a pull request, this is going to be executed. Of course, here I directly created a commit. But in general, nobody creates a direct commit to the main branch. They would create a pull request. So on the pull request, you will see these things passed, these things failed. Now it is up to you. Do you want to accept or do you want to fail? In fact, if you don't want these

jobs to be executed, if you don't want Kubernetes manifest to be updated, you want Kubernetes manifest to be updated with the latest version only if all the other stages are successful. Then what you can do? Just go back to your GitHub actions. Go back to the CI and remove this line here. For now we said continue on error true on all the things. Remove continue on error true and you will notice the failures will stop the further execution. This is also a good practice. This is also a dev sec ops practice where only

run the stages if the previous steps are successful. Only run the docker build if the security scanning and docker linting is complete. only update the Kubernetes manifest if rest of the stages are good. Let's do the final check. We will go to the source code. I can actually see it from here itself because the pipeline execution is complete. The Kubernetes manifest is updated with the latest version and in fact it committed to the repository as well. See the commit. So previously this was the commit gcr.io. This was the

image name. Now it is updated to the latest image name 4757747. What is it? This is the commit ID. So the new Kubernetes version is tagged with this commit ID. If you rerun this again a commit will be created but the new commit the new image will have a different commit ID. You can verify that. Now change something again. Maybe you can update the readme file one more time. You will see one more time in the DevOps branch. You have to make change in the DevOps branch, right? Update the readme file.

You will notice the Kubernetes manifest file will be updated. And from the GHCR io, the one that we have right now, image, okay, from this one, everything will remain the same. Only this tag will be updated. This will be updated with the commit ID. Why? That's what we wrote in the GitHub action script in the last lines of the GitHub actions. That is exactly what we wrote. And this script will remain the same. You might be using Python application, you might be using uh Java application, anything that you want to this will

remain the same. Perfect. So this is about end to end dev sec ops. Now if you want to complete this like Abishek I want to deploy every latest version onto the Kubernetes cluster you can also do it. What you will do just head to the Argo CD documentation. So Argo CD docs Argo CD is well integrated with GitHub. So every time there is a new commit Argo CD can automatically identify the commit and deploy that to your Kubernetes cluster. For that just install Argo CD on the Kubernetes cluster. So what it is

doing I'll explain you that part as well. So GitHub actions is creating a new image and it is updating the new image in the K8 manifest folder. Argo CD can read it and my bad. Argo CD can read it and what it can do it can pick up the new Kubernetes manifest version and deploy it to your Kubernetes cluster. This is the capability of Argo CD. So you can build an end toend CI/CD process. CI is implemented by GitHub actions. CD is implemented by Argo CD. What is the bridge between CI and CD? The shell

script that we wrote in the last lines of our GitHub actions file. So this one this is acting as a bridge. Once all the stages of CA are complete the shell script is updating the Kubernetes manifest with the latest version. Argo CD looks at that GitHub repository. It will look at the commit like this. So it will keep looking at this file. It will see if the image is updated. Okay, it identified an image change. It will deploy the new images onto the Kubernetes cluster. right in our case onto the EKS cluster.

I can show you that as well. So now Argo CD is installed. If you just do cubectl get uh all hyphen n argo cd you will find all the resources that are created in the argo cd namespace. There will be bunch of resources but what you should be interested in the first one cubectl get secrets n argo cd you will have something called argo cd initial admin secret just fetch the argod secret from here because you want to connect to argod cubectl edit secret name of the secret hyphen n argo cd this This is in the base 64

encoded format. Copy it. I'll close it here and run cubectl or directly also. Echo pipe base 64 - decode. Great. So this is the password. Now how to connect to Argo CD? cubectl get SVC hyphen and argo CD. So this is the Argo CD server. Cubectl port forward SVC Argo CD server. Choose a random port again. We'll this time go with 71 colon 80 is the service port of Argo CD. - n argo cd - address 0.0.0.0. Now we'll copy the password here. Okay. Go back. Local host col 71. Oh, my bad. I added one extra one.

Now why are we doing cubectl port forward all the time? Because we are using a free uh not free but we are using an EKS cluster on AWS free tire. If you keep using load balancer or if you if you keep using ingress it will incur a lot of cost for you. Instead cubectl is free of cost. That's why we are going with it. But otherwise in real time you have to create an ingress or you have to create gateway API for it. Admin is the username and this is the password the one that we got from the terminal. Now setting up Argo CD is a

cakewalk. Just create a new application. Okay, I'll call this as journey application. Name of the project is default. Sync policy is automatic. When you say sync policy is automatic, it will start looking for a repository. Which repository it should look for? This is a repo. Okay. Copy. I'll paste it here. Repository URL. Which path it has to look for? It has to look for the KH path. Which branch it has to look for? I will say just look for DevOps branch. Okay. Because CI/CD is updating DevOps branch. Cluster URL.

Go with the local host. Argo CD is installed on EKS cluster. I'm saying just update the same cluster name space. Let's say the journey name space. Perfect. This is all click. Just click on the create button. Argo city will automatically sync. It will start deploying all the resources that are in the K8 file. And now if you make a simple change. Okay. So right now in the DevOps branch in the K8 folder in the journey. Well, let's see what is the version. Image colon. Okay, right now the image version is

4757. Okay, it starts with 4757. Let's make a simple change. Let's see for end to end dev secops CI/CD pipeline process is working as expected. I'll add an empty space. Commit changes. commit. Okay. Now, automatically CI/CD pipeline should run on the DevOps branch which is running. It should automatically run all the stages. It should update the Kubernetes manifest and Argo CD should deploy it to the cluster because we enabled automatic sync. But you have to note one thing, it will take 3 minutes of time. The default

automatic sync time is 180 seconds. So once the GitHub commit is done, you have to be patient for up to 180 seconds. Okay? Sometimes it might happen pretty fast for you, but otherwise you have to wait for 180 seconds. I waited for 2 to 3 minutes. Now the pipeline is complete in the K8 folder. If you look at image tag, it would not be 4 uh 7 something like that. Now it is 75DA 1DA and Argo CD it is already progressing the change. You can see it is creating a new replica set and it is updating the pod version for back end as

well as the front end. Right? So this is how end to end CI/CD works in the dev secops practice. We not only looked at end toend CI/CD but we looked at end toend project from dev secops point of view. Before I conclude this video, I just want to tell you at the very first site this might look overwhelming to you because I covered everything in one video. Ideally, this should be a playlist where I have to take each and every step and I have to cover each and every step as part of an individual lecture. That's what we did in our Udemy

project. Right? If you look at my Udemy DevOps project, I took even more complicated project and I explained the same things for 14 hours because I had to go through each and every step and explain it for you. This time we did it in one single lecture. We did it for dev sec ops from security focus. So at the initial site this might be overwhelming. Please watch the video again if you're stuck at some point or start looking at the GitHub repository and start implementing on your site. Thank you so much for watching today's

video. If possible, please share it with others because videos like this deserves an appreciation. See you all in the next video. Take care.

